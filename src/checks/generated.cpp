#include "checks/generated.hpp"
#include <iostream>

template<typename T>
std::enable_if_t<std::is_arithmetic_v<T>, bool>
check_zero(const T &value, const std::string &name) {
    if (value != 0) {
        std::cout << name << " is non-zero" << std::endl;
        return false;
    }
    return true;
}

template<typename T, std::size_t N>
bool check_zero(const T (&arr)[N], const std::string &name) {
    bool all_zero = true;
    for (std::size_t i = 0; i < N; ++i) {
        all_zero &= check_zero(arr[i], name + "[" + std::to_string(i) + "]");
    }
    return all_zero;
}

bool check_zero(const PINT &obj, const std::string &name) {
    if (obj != nullptr) {
        std::cout << name << " is non-zero" << std::endl;
        return false;
    }
    return true;
}

bool check_zero(const FRDCNTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RWAIT, name + " " + "Random Read Waitstate");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FRDCNTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FBAC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.VREADST, name + " " + "VREAD Setup Time Count");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FBAC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FBFALLBACK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BNKPWR0, name + " " + "Bank Power Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FBFALLBACK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FBPRDY_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BANKRDY, name + " " + "Flash Bank Active Power State");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PUMPRDY, name + " " + "Flash Pump Active Power Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FBPRDY_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FPAC1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PMPPWR, name + " " + "Charge Pump Fallback Power Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PSLEEP, name + " " + "Pump Sleep Down Count");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FPAC1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FMSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PSUSP, name + " " + "Program Suspend.");
    all_zero &= check_zero(obj.ESUSP, name + " " + "Erase Suspend.");
    all_zero &= check_zero(obj.VOLTSTAT, name + " " + "Flash Pump Power Status");
    all_zero &= check_zero(obj.CSTAT, name + " " + "Command Fail Status");
    all_zero &= check_zero(obj.INVDAT, name + " " + "Invalid Data");
    all_zero &= check_zero(obj.PGM, name + " " + "Program Operation Status");
    all_zero &= check_zero(obj.ERS, name + " " + "Erase Operation Status");
    all_zero &= check_zero(obj.BUSY, name + " " + "Busy Bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.EV, name + " " + "Erase Verify Status");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PGV, name + " " + "Programming Verify Status");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FMSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FRD_INTF_CTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PREFETCH_EN, name + " " + "Prefetch Enable");
    all_zero &= check_zero(obj.DATA_CACHE_EN, name + " " + "Data Cache Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FRD_INTF_CTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FLASH_CTRL_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FRDCNTL, name + " " + "Flash Read Control Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FBAC, name + " " + "Flash Bank Access Control Register");
    all_zero &= check_zero(obj.FBFALLBACK, name + " " + "Flash Bank Fallback Power Register");
    all_zero &= check_zero(obj.FBPRDY, name + " " + "Flash Bank Pump Ready Register");
    all_zero &= check_zero(obj.FPAC1, name + " " + "Flash Pump Access Control Register 1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FMSTAT, name + " " + "Flash Module Status Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FRD_INTF_CTRL, name + " " + "Flash Read Interface Control Register");

    return all_zero;
}

bool check_zero(const ECC_ENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "Enable ECC");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECC_ENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_STATUS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FAIL_0_L, name + " " + "Lower 64bits Single Bit Error Corrected Value 0");
    all_zero &= check_zero(obj.FAIL_1_L, name + " " + "Lower 64bits Single Bit Error Corrected Value 1");
    all_zero &= check_zero(obj.UNC_ERR_L, name + " " + "Lower 64 bits Uncorrectable error occurred");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FAIL_0_H, name + " " + "Upper 64bits Single Bit Error Corrected Value 0");
    all_zero &= check_zero(obj.FAIL_1_H, name + " " + "Upper 64bits Single Bit Error Corrected Value 1");
    all_zero &= check_zero(obj.UNC_ERR_H, name + " " + "Upper 64 bits Uncorrectable error occurred");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_STATUS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_POS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ERR_POS_L, name + " " + "Bit Position of Single bit Error in lower 64 bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ERR_TYPE_L, name + " " + "Error Type in lower 64 bits");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.ERR_POS_H, name + " " + "Bit Position of Single bit Error in upper 64 bits");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.ERR_TYPE_H, name + " " + "Error Type in upper 64 bits");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_POS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_STATUS_CLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FAIL_0_L_CLR, name + " " + "Lower 64bits Single Bit Error Corrected Value 0 Clear");
    all_zero &= check_zero(obj.FAIL_1_L_CLR, name + " " + "Lower 64bits Single Bit Error Corrected Value 1 Clear");
    all_zero &= check_zero(obj.UNC_ERR_L_CLR, name + " " + "Lower 64 bits Uncorrectable error occurred Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FAIL_0_H_CLR, name + " " + "Upper 64bits Single Bit Error Corrected Value 0 Clear");
    all_zero &= check_zero(obj.FAIL_1_H_CLR, name + " " + "Upper 64bits Single Bit Error Corrected Value 1 Clear");
    all_zero &= check_zero(obj.UNC_ERR_H_CLR, name + " " + "Upper 64 bits Uncorrectable error occurred Clear");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_STATUS_CLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_CNT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ERR_CNT, name + " " + "Error counter");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_CNT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_THRESHOLD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ERR_THRESHOLD, name + " " + "Error Threshold");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_THRESHOLD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_INTFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SINGLE_ERR_INTFLG, name + " " + "Single Error Interrupt Flag");
    all_zero &= check_zero(obj.UNC_ERR_INTFLG, name + " " + "Uncorrectable Interrupt Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_INTFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ERR_INTCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SINGLE_ERR_INTCLR, name + " " + "Single Error Interrupt Flag Clear");
    all_zero &= check_zero(obj.UNC_ERR_INTCLR, name + " " + "Uncorrectable Interrupt Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ERR_INTCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FADDR_TEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADDRL, name + " " + "ECC Address Low");
    all_zero &= check_zero(obj.ADDRH, name + " " + "ECC Address High");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FADDR_TEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FECC_TEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECC, name + " " + "ECC Control Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FECC_TEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FECC_CTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECC_TEST_EN, name + " " + "Enable ECC Test Logic");
    all_zero &= check_zero(obj.ECC_SELECT, name + " " + "ECC Bit Select");
    all_zero &= check_zero(obj.DO_ECC_CALC, name + " " + "Enable ECC Calculation");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FECC_CTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FECC_STATUS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SINGLE_ERR, name + " " + "Test Result is Single Bit Error");
    all_zero &= check_zero(obj.UNC_ERR, name + " " + "Test Result is Uncorrectable Error");
    all_zero &= check_zero(obj.DATA_ERR_POS, name + " " + "Holds Bit Position of Error");
    all_zero &= check_zero(obj.ERR_TYPE, name + " " + "Holds Bit Position of 8 Check Bits of Error");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FECC_STATUS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FLASH_ECC_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECC_ENABLE, name + " " + "ECC Enable");
    all_zero &= check_zero(obj.SINGLE_ERR_ADDR_LOW, name + " " + "Single Error Address Low");
    all_zero &= check_zero(obj.SINGLE_ERR_ADDR_HIGH, name + " " + "Single Error Address High");
    all_zero &= check_zero(obj.UNC_ERR_ADDR_LOW, name + " " + "Uncorrectable Error Address Low");
    all_zero &= check_zero(obj.UNC_ERR_ADDR_HIGH, name + " " + "Uncorrectable Error Address High");
    all_zero &= check_zero(obj.ERR_STATUS, name + " " + "Error Status");
    all_zero &= check_zero(obj.ERR_POS, name + " " + "Error Position");
    all_zero &= check_zero(obj.ERR_STATUS_CLR, name + " " + "Error Status Clear");
    all_zero &= check_zero(obj.ERR_CNT, name + " " + "Error Control");
    all_zero &= check_zero(obj.ERR_THRESHOLD, name + " " + "Error Threshold");
    all_zero &= check_zero(obj.ERR_INTFLG, name + " " + "Error Interrupt Flag");
    all_zero &= check_zero(obj.ERR_INTCLR, name + " " + "Error Interrupt Flag Clear");
    all_zero &= check_zero(obj.FDATAH_TEST, name + " " + "Data High Test");
    all_zero &= check_zero(obj.FDATAL_TEST, name + " " + "Data Low Test");
    all_zero &= check_zero(obj.FADDR_TEST, name + " " + "ECC Test Address");
    all_zero &= check_zero(obj.FECC_TEST, name + " " + "ECC Test Address");
    all_zero &= check_zero(obj.FECC_CTRL, name + " " + "ECC Control");
    all_zero &= check_zero(obj.FOUTH_TEST, name + " " + "Test Data Out High");
    all_zero &= check_zero(obj.FOUTL_TEST, name + " " + "Test Data Out Low");
    all_zero &= check_zero(obj.FECC_STATUS, name + " " + "ECC Status");

    return all_zero;
}

bool check_zero(const PUMPREQUEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PUMP_OWNERSHIP, name + " " + "Flash Pump Request Semaphore between CPU1 and CPU2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Key Qualifier for writes to this register");

    return all_zero;
}

bool check_zero(const PUMPREQUEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FLASH_PUMP_SEMAPHORE_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PUMPREQUEST, name + " " + "Flash programming semaphore PUMP request register");

    return all_zero;
}

bool check_zero(const DxLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOCK_D0, name + " " + "D0 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_D1, name + " " + "D1 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DxCOMMIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.COMMIT_D0, name + " " + "D0 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_D1, name + " " + "D1 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxCOMMIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DxACCPROT0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_D0, name + " " + "Fetch Protection For D0 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_D0, name + " " + "CPU WR Protection For D0 RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_D1, name + " " + "Fetch Protection For D1 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_D1, name + " " + "CPU WR Protection For D1 RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxACCPROT0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DxTEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TEST_M0, name + " " + "Selects the different modes for M0 RAM");
    all_zero &= check_zero(obj.TEST_M1, name + " " + "Selects the different modes for M1 RAM");
    all_zero &= check_zero(obj.TEST_D0, name + " " + "Selects the different modes for D0 RAM");
    all_zero &= check_zero(obj.TEST_D1, name + " " + "Selects the different modes for D1 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxTEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DxINIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INIT_M0, name + " " + "RAM Initialization control for M0 RAM.");
    all_zero &= check_zero(obj.INIT_M1, name + " " + "RAM Initialization control for M1 RAM.");
    all_zero &= check_zero(obj.INIT_D0, name + " " + "RAM Initialization control for D0 RAM.");
    all_zero &= check_zero(obj.INIT_D1, name + " " + "RAM Initialization control for D1 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxINIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DxINITDONE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INITDONE_M0, name + " " + "RAM Initialization status for M0 RAM.");
    all_zero &= check_zero(obj.INITDONE_M1, name + " " + "RAM Initialization status for M1 RAM.");
    all_zero &= check_zero(obj.INITDONE_D0, name + " " + "RAM Initialization status for D0 RAM.");
    all_zero &= check_zero(obj.INITDONE_D1, name + " " + "RAM Initialization status for D1 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DxINITDONE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK_LS0, name + " " + "LS0 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_LS1, name + " " + "LS1 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_LS2, name + " " + "LS2 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_LS3, name + " " + "LS3 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_LS4, name + " " + "LS4 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_LS5, name + " " + "LS5 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxCOMMIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMMIT_LS0, name + " " + "LS0 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_LS1, name + " " + "LS1 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_LS2, name + " " + "LS2 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_LS3, name + " " + "LS3 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_LS4, name + " " + "LS4 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_LS5, name + " " + "LS5 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxCOMMIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxMSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MSEL_LS0, name + " " + "Master Select for LS0 RAM");
    all_zero &= check_zero(obj.MSEL_LS1, name + " " + "Master Select for LS1 RAM");
    all_zero &= check_zero(obj.MSEL_LS2, name + " " + "Master Select for LS2 RAM");
    all_zero &= check_zero(obj.MSEL_LS3, name + " " + "Master Select for LS3 RAM");
    all_zero &= check_zero(obj.MSEL_LS4, name + " " + "Master Select for LS4 RAM");
    all_zero &= check_zero(obj.MSEL_LS5, name + " " + "Master Select for LS5 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxMSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxCLAPGM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLAPGM_LS0, name + " " + "Selects LS0 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.CLAPGM_LS1, name + " " + "Selects LS1 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.CLAPGM_LS2, name + " " + "Selects LS2 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.CLAPGM_LS3, name + " " + "Selects LS3 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.CLAPGM_LS4, name + " " + "Selects LS4 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.CLAPGM_LS5, name + " " + "Selects LS5 RAM as program vs data memory for CLA");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxCLAPGM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxACCPROT0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_LS0, name + " " + "Fetch Protection For LS0 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS0, name + " " + "CPU WR Protection For LS0 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_LS1, name + " " + "Fetch Protection For LS1 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS1, name + " " + "CPU WR Protection For LS1 RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_LS2, name + " " + "Fetch Protection For LS2 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS2, name + " " + "CPU WR Protection For LS2 RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_LS3, name + " " + "Fetch Protection For LS3 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS3, name + " " + "CPU WR Protection For LS3 RAM");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxACCPROT0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxACCPROT1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_LS4, name + " " + "Fetch Protection For LS4 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS4, name + " " + "CPU WR Protection For LS4 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_LS5, name + " " + "Fetch Protection For LS5 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_LS5, name + " " + "CPU WR Protection For LS5 RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxACCPROT1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxTEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TEST_LS0, name + " " + "Selects the different modes for LS0 RAM");
    all_zero &= check_zero(obj.TEST_LS1, name + " " + "Selects the different modes for LS1 RAM");
    all_zero &= check_zero(obj.TEST_LS2, name + " " + "Selects the different modes for LS2 RAM");
    all_zero &= check_zero(obj.TEST_LS3, name + " " + "Selects the different modes for LS3 RAM");
    all_zero &= check_zero(obj.TEST_LS4, name + " " + "Selects the different modes for LS4 RAM");
    all_zero &= check_zero(obj.TEST_LS5, name + " " + "Selects the different modes for LS5 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxTEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxINIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INIT_LS0, name + " " + "RAM Initialization control for LS0 RAM.");
    all_zero &= check_zero(obj.INIT_LS1, name + " " + "RAM Initialization control for LS1 RAM.");
    all_zero &= check_zero(obj.INIT_LS2, name + " " + "RAM Initialization control for LS2 RAM.");
    all_zero &= check_zero(obj.INIT_LS3, name + " " + "RAM Initialization control for LS3 RAM.");
    all_zero &= check_zero(obj.INIT_LS4, name + " " + "RAM Initialization control for LS4 RAM.");
    all_zero &= check_zero(obj.INIT_LS5, name + " " + "RAM Initialization control for LS5 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxINIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LSxINITDONE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INITDONE_LS0, name + " " + "RAM Initialization status for LS0 RAM.");
    all_zero &= check_zero(obj.INITDONE_LS1, name + " " + "RAM Initialization status for LS1 RAM.");
    all_zero &= check_zero(obj.INITDONE_LS2, name + " " + "RAM Initialization status for LS2 RAM.");
    all_zero &= check_zero(obj.INITDONE_LS3, name + " " + "RAM Initialization status for LS3 RAM.");
    all_zero &= check_zero(obj.INITDONE_LS4, name + " " + "RAM Initialization status for LS4 RAM.");
    all_zero &= check_zero(obj.INITDONE_LS5, name + " " + "RAM Initialization status for LS5 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LSxINITDONE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK_GS0, name + " " + "GS0 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS1, name + " " + "GS1 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS2, name + " " + "GS2 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS3, name + " " + "GS3 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS4, name + " " + "GS4 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS5, name + " " + "GS5 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS6, name + " " + "GS6 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS7, name + " " + "GS7 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS8, name + " " + "GS8 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS9, name + " " + "GS9 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS10, name + " " + "GS10 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS11, name + " " + "GS11 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS12, name + " " + "GS12 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS13, name + " " + "GS13 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS14, name + " " + "GS14 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.LOCK_GS15, name + " " + "GS15 RAM access protection and master select fields lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxCOMMIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMMIT_GS0, name + " " + "GS0 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS1, name + " " + "GS1 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS2, name + " " + "GS2 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS3, name + " " + "GS3 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS4, name + " " + "GS4 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS5, name + " " + "GS5 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS6, name + " " + "GS6 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS7, name + " " + "GS7 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS8, name + " " + "GS8 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS9, name + " " + "GS9 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS10, name + " " + "GS10 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS11, name + " " + "GS11 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS12, name + " " + "GS12 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS13, name + " " + "GS13 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS14, name + " " + "GS14 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.COMMIT_GS15, name + " " + "GS15 RAM access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxCOMMIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxMSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MSEL_GS0, name + " " + "Master Select for GS0 RAM");
    all_zero &= check_zero(obj.MSEL_GS1, name + " " + "Master Select for GS1 RAM");
    all_zero &= check_zero(obj.MSEL_GS2, name + " " + "Master Select for GS2 RAM");
    all_zero &= check_zero(obj.MSEL_GS3, name + " " + "Master Select for GS3 RAM");
    all_zero &= check_zero(obj.MSEL_GS4, name + " " + "Master Select for GS4 RAM");
    all_zero &= check_zero(obj.MSEL_GS5, name + " " + "Master Select for GS5 RAM");
    all_zero &= check_zero(obj.MSEL_GS6, name + " " + "Master Select for GS6 RAM");
    all_zero &= check_zero(obj.MSEL_GS7, name + " " + "Master Select for GS7 RAM");
    all_zero &= check_zero(obj.MSEL_GS8, name + " " + "Master Select for GS8 RAM");
    all_zero &= check_zero(obj.MSEL_GS9, name + " " + "Master Select for GS9 RAM");
    all_zero &= check_zero(obj.MSEL_GS10, name + " " + "Master Select for GS10 RAM");
    all_zero &= check_zero(obj.MSEL_GS11, name + " " + "Master Select for GS11 RAM");
    all_zero &= check_zero(obj.MSEL_GS12, name + " " + "Master Select for GS12 RAM");
    all_zero &= check_zero(obj.MSEL_GS13, name + " " + "Master Select for GS13 RAM");
    all_zero &= check_zero(obj.MSEL_GS14, name + " " + "Master Select for GS14 RAM");
    all_zero &= check_zero(obj.MSEL_GS15, name + " " + "Master Select for GS15 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxMSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxACCPROT0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_GS0, name + " " + "Fetch Protection For GS0 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS0, name + " " + "CPU WR Protection For GS0 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS0, name + " " + "DMA WR Protection For GS0 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS1, name + " " + "Fetch Protection For GS1 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS1, name + " " + "CPU WR Protection For GS1 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS1, name + " " + "DMA WR Protection For GS1 RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS2, name + " " + "Fetch Protection For GS2 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS2, name + " " + "CPU WR Protection For GS2 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS2, name + " " + "DMA WR Protection For GS2 RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS3, name + " " + "Fetch Protection For GS3 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS3, name + " " + "CPU WR Protection For GS3 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS3, name + " " + "DMA WR Protection For GS3 RAM");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxACCPROT0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxACCPROT1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_GS4, name + " " + "Fetch Protection For GS4 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS4, name + " " + "CPU WR Protection For GS4 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS4, name + " " + "DMA WR Protection For GS4 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS5, name + " " + "Fetch Protection For GS5 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS5, name + " " + "CPU WR Protection For GS5 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS5, name + " " + "DMA WR Protection For GS5RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS6, name + " " + "Fetch Protection For GS6 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS6, name + " " + "CPU WR Protection For GS6 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS6, name + " " + "DMA WR Protection For GS6RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS7, name + " " + "Fetch Protection For GS7 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS7, name + " " + "CPU WR Protection For GS7 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS7, name + " " + "DMA WR Protection For GS7RAM");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxACCPROT1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxACCPROT2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_GS8, name + " " + "Fetch Protection For GS8 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS8, name + " " + "CPU WR Protection For GS8 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS8, name + " " + "DMA WR Protection For GS8 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS9, name + " " + "Fetch Protection For GS9 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS9, name + " " + "CPU WR Protection For GS9 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS9, name + " " + "DMA WR Protection For GS9RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS10, name + " " + "Fetch Protection For GS10 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS10, name + " " + "CPU WR Protection For GS10 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS10, name + " " + "DMA WR Protection For GS10RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS11, name + " " + "Fetch Protection For GS11 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS11, name + " " + "CPU WR Protection For GS11 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS11, name + " " + "DMA WR Protection For GS11RAM");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxACCPROT2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxACCPROT3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_GS12, name + " " + "Fetch Protection For GS12 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS12, name + " " + "CPU WR Protection For GS12 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS12, name + " " + "DMA WR Protection For GS12 RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS13, name + " " + "Fetch Protection For GS13 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS13, name + " " + "CPU WR Protection For GS13 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS13, name + " " + "DMA WR Protection For GS13RAM");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS14, name + " " + "Fetch Protection For GS14 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS14, name + " " + "CPU WR Protection For GS14 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS14, name + " " + "DMA WR Protection For GS14RAM");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FETCHPROT_GS15, name + " " + "Fetch Protection For GS15 RAM");
    all_zero &= check_zero(obj.CPUWRPROT_GS15, name + " " + "CPU WR Protection For GS15 RAM");
    all_zero &= check_zero(obj.DMAWRPROT_GS15, name + " " + "DMA WR Protection For GS15RAM");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxACCPROT3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxTEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TEST_GS0, name + " " + "Selects the different modes for GS0 RAM");
    all_zero &= check_zero(obj.TEST_GS1, name + " " + "Selects the different modes for GS1 RAM");
    all_zero &= check_zero(obj.TEST_GS2, name + " " + "Selects the different modes for GS2 RAM");
    all_zero &= check_zero(obj.TEST_GS3, name + " " + "Selects the different modes for GS3 RAM");
    all_zero &= check_zero(obj.TEST_GS4, name + " " + "Selects the different modes for GS4 RAM");
    all_zero &= check_zero(obj.TEST_GS5, name + " " + "Selects the different modes for GS5 RAM");
    all_zero &= check_zero(obj.TEST_GS6, name + " " + "Selects the different modes for GS6 RAM");
    all_zero &= check_zero(obj.TEST_GS7, name + " " + "Selects the different modes for GS7 RAM");
    all_zero &= check_zero(obj.TEST_GS8, name + " " + "Selects the different modes for GS8 RAM");
    all_zero &= check_zero(obj.TEST_GS9, name + " " + "Selects the different modes for GS9 RAM");
    all_zero &= check_zero(obj.TEST_GS10, name + " " + "Selects the different modes for GS10 RAM");
    all_zero &= check_zero(obj.TEST_GS11, name + " " + "Selects the different modes for GS11 RAM");
    all_zero &= check_zero(obj.TEST_GS12, name + " " + "Selects the different modes for GS12 RAM");
    all_zero &= check_zero(obj.TEST_GS13, name + " " + "Selects the different modes for GS13 RAM");
    all_zero &= check_zero(obj.TEST_GS14, name + " " + "Selects the different modes for GS14 RAM");
    all_zero &= check_zero(obj.TEST_GS15, name + " " + "Selects the different modes for GS15 RAM");

    return all_zero;
}

bool check_zero(const GSxTEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxINIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INIT_GS0, name + " " + "RAM Initialization control for GS0 RAM.");
    all_zero &= check_zero(obj.INIT_GS1, name + " " + "RAM Initialization control for GS1 RAM.");
    all_zero &= check_zero(obj.INIT_GS2, name + " " + "RAM Initialization control for GS2 RAM.");
    all_zero &= check_zero(obj.INIT_GS3, name + " " + "RAM Initialization control for GS3 RAM.");
    all_zero &= check_zero(obj.INIT_GS4, name + " " + "RAM Initialization control for GS4 RAM.");
    all_zero &= check_zero(obj.INIT_GS5, name + " " + "RAM Initialization control for GS5 RAM.");
    all_zero &= check_zero(obj.INIT_GS6, name + " " + "RAM Initialization control for GS6 RAM.");
    all_zero &= check_zero(obj.INIT_GS7, name + " " + "RAM Initialization control for GS7 RAM.");
    all_zero &= check_zero(obj.INIT_GS8, name + " " + "RAM Initialization control for GS8 RAM.");
    all_zero &= check_zero(obj.INIT_GS9, name + " " + "RAM Initialization control for GS9 RAM.");
    all_zero &= check_zero(obj.INIT_GS10, name + " " + "RAM Initialization control for GS10 RAM.");
    all_zero &= check_zero(obj.INIT_GS11, name + " " + "RAM Initialization control for GS11 RAM.");
    all_zero &= check_zero(obj.INIT_GS12, name + " " + "RAM Initialization control for GS12 RAM.");
    all_zero &= check_zero(obj.INIT_GS13, name + " " + "RAM Initialization control for GS13 RAM.");
    all_zero &= check_zero(obj.INIT_GS14, name + " " + "RAM Initialization control for GS14 RAM.");
    all_zero &= check_zero(obj.INIT_GS15, name + " " + "RAM Initialization control for GS15 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxINIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GSxINITDONE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INITDONE_GS0, name + " " + "RAM Initialization status for GS0 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS1, name + " " + "RAM Initialization status for GS1 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS2, name + " " + "RAM Initialization status for GS2 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS3, name + " " + "RAM Initialization status for GS3 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS4, name + " " + "RAM Initialization status for GS4 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS5, name + " " + "RAM Initialization status for GS5 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS6, name + " " + "RAM Initialization status for GS6 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS7, name + " " + "RAM Initialization status for GS7 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS8, name + " " + "RAM Initialization status for GS8 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS9, name + " " + "RAM Initialization status for GS9 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS10, name + " " + "RAM Initialization status for GS10 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS11, name + " " + "RAM Initialization status for GS11 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS12, name + " " + "RAM Initialization status for GS12 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS13, name + " " + "RAM Initialization status for GS13 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS14, name + " " + "RAM Initialization status for GS14 RAM.");
    all_zero &= check_zero(obj.INITDONE_GS15, name + " " + "RAM Initialization status for GS15 RAM.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GSxINITDONE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MSGxTEST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TEST_CPUTOCPU, name + " " + "CPU to CPU Mode Select");
    all_zero &= check_zero(obj.TEST_CPUTOCLA1, name + " " + "CPU to CLA1 MSG RAM Mode Select");
    all_zero &= check_zero(obj.TEST_CLA1TOCPU, name + " " + "CLA1 to CPU MSG RAM Mode Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MSGxTEST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MSGxINIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INIT_CPUTOCPU, name + " " + "Initialization control for CPU to CPU MSG RAM");
    all_zero &= check_zero(obj.INIT_CPUTOCLA1, name + " " + "Initialization control for CPUTOCLA1 MSG RAM");
    all_zero &= check_zero(obj.INIT_CLA1TOCPU, name + " " + "Initialization control for CLA1TOCPU MSG RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MSGxINIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MSGxINITDONE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INITDONE_CPUTOCPU, name + " " + "Initialization status for CPU to CPU MSG RAM");
    all_zero &= check_zero(obj.INITDONE_CPUTOCLA1, name + " " + "Initialization status for CPU to CLA1 MSG RAM");
    all_zero &= check_zero(obj.INITDONE_CLA1TOCPU, name + " " + "Initialization status for CLA1 to CPU MSG RAM");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MSGxINITDONE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MEM_CFG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DxLOCK, name + " " + "Dedicated RAM Config Lock Register");
    all_zero &= check_zero(obj.DxCOMMIT, name + " " + "Dedicated RAM Config Lock Commit Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.DxACCPROT0, name + " " + "Dedicated RAM Config Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.DxTEST, name + " " + "Dedicated RAM TEST Register");
    all_zero &= check_zero(obj.DxINIT, name + " " + "Dedicated RAM Init Register");
    all_zero &= check_zero(obj.DxINITDONE, name + " " + "Dedicated RAM InitDone Status Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.LSxLOCK, name + " " + "Local Shared RAM Config Lock Register");
    all_zero &= check_zero(obj.LSxCOMMIT, name + " " + "Local Shared RAM Config Lock Commit Register");
    all_zero &= check_zero(obj.LSxMSEL, name + " " + "Local Shared RAM Master Sel Register");
    all_zero &= check_zero(obj.LSxCLAPGM, name + " " + "Local Shared RAM Prog/Exe control Register");
    all_zero &= check_zero(obj.LSxACCPROT0, name + " " + "Local Shared RAM Config Register 0");
    all_zero &= check_zero(obj.LSxACCPROT1, name + " " + "Local Shared RAM Config Register 1");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.LSxTEST, name + " " + "Local Shared RAM TEST Register");
    all_zero &= check_zero(obj.LSxINIT, name + " " + "Local Shared RAM Init Register");
    all_zero &= check_zero(obj.LSxINITDONE, name + " " + "Local Shared RAM InitDone Status Register");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.GSxLOCK, name + " " + "Global Shared RAM Config Lock Register");
    all_zero &= check_zero(obj.GSxCOMMIT, name + " " + "Global Shared RAM Config Lock Commit Register");
    all_zero &= check_zero(obj.GSxMSEL, name + " " + "Global Shared RAM Master Sel Register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.GSxACCPROT0, name + " " + "Global Shared RAM Config Register 0");
    all_zero &= check_zero(obj.GSxACCPROT1, name + " " + "Global Shared RAM Config Register 1");
    all_zero &= check_zero(obj.GSxACCPROT2, name + " " + "Global Shared RAM Config Register 2");
    all_zero &= check_zero(obj.GSxACCPROT3, name + " " + "Global Shared RAM Config Register 3");
    all_zero &= check_zero(obj.GSxTEST, name + " " + "Global Shared RAM TEST Register");
    all_zero &= check_zero(obj.GSxINIT, name + " " + "Global Shared RAM Init Register");
    all_zero &= check_zero(obj.GSxINITDONE, name + " " + "Global Shared RAM InitDone Status Register");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.MSGxTEST, name + " " + "Message RAM TEST Register");
    all_zero &= check_zero(obj.MSGxINIT, name + " " + "Message RAM Init Register");
    all_zero &= check_zero(obj.MSGxINITDONE, name + " " + "Message RAM InitDone Status Register");

    return all_zero;
}

bool check_zero(const EMIF1LOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK_EMIF1, name + " " + "EMIF1 access protection and master select fields lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF1LOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF1COMMIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMMIT_EMIF1, name + " " + "EMIF1 access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF1COMMIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF1MSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MSEL_EMIF1, name + " " + "Master Select for EMIF1.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "KEY to enable the write into MSEL_EMIF1 bits");

    return all_zero;
}

bool check_zero(const EMIF1MSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF1ACCPROT0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_EMIF1, name + " " + "Fetch Protection For EMIF1");
    all_zero &= check_zero(obj.CPUWRPROT_EMIF1, name + " " + "CPU WR Protection For EMIF1");
    all_zero &= check_zero(obj.DMAWRPROT_EMIF1, name + " " + "DMA WR Protection For EMIF1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF1ACCPROT0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF1_CONFIG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EMIF1LOCK, name + " " + "EMIF1 Config Lock Register");
    all_zero &= check_zero(obj.EMIF1COMMIT, name + " " + "EMIF1 Config Lock Commit Register");
    all_zero &= check_zero(obj.EMIF1MSEL, name + " " + "EMIF1 Master Sel Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EMIF1ACCPROT0, name + " " + "EMIF1 Config Register 0");

    return all_zero;
}

bool check_zero(const EMIF2LOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK_EMIF2, name + " " + "EMIF2 access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF2LOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF2COMMIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMMIT_EMIF2, name + " " + "EMIF2 access protection and master select permanent lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF2COMMIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF2ACCPROT0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FETCHPROT_EMIF2, name + " " + "Fetch Protection For EMIF2");
    all_zero &= check_zero(obj.CPUWRPROT_EMIF2, name + " " + "CPU WR Protection For EMIF2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const EMIF2ACCPROT0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF2_CONFIG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EMIF2LOCK, name + " " + "EMIF2 Config Lock Register");
    all_zero &= check_zero(obj.EMIF2COMMIT, name + " " + "EMIF2 Config Lock Commit Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EMIF2ACCPROT0, name + " " + "EMIF2 Config Register 0");

    return all_zero;
}

bool check_zero(const NMAVFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUREAD, name + " " + "Non Master CPU Read Access Violation Flag");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Non Master CPU Write Access Violation Flag");
    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Non Master CPU Fetch Access Violation Flag");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Non Master DMA Write Access Violation Flag");
    all_zero &= check_zero(obj.CLA1READ, name + " " + "Non Master CLA1 Read Access Violation Flag");
    all_zero &= check_zero(obj.CLA1WRITE, name + " " + "Non Master CLA1 Write Access Violation Flag");
    all_zero &= check_zero(obj.CLA1FETCH, name + " " + "Non Master CLA1 Fetch Access Violation Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMAVFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMAVSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUREAD, name + " " + "Non Master CPU Read Access Violation Flag Set");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Non Master CPU Write Access Violation Flag Set");
    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Non Master CPU Fetch Access Violation Flag Set");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Non Master DMA Write Access Violation Flag Set");
    all_zero &= check_zero(obj.CLA1READ, name + " " + "Non Master CLA1 Read Access Violation Flag Set");
    all_zero &= check_zero(obj.CLA1WRITE, name + " " + "Non Master CLA1 Write Access Violation Flag Set");
    all_zero &= check_zero(obj.CLA1FETCH, name + " " + "Non Master CLA1 Fetch Access Violation Flag Set");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMAVSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMAVCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUREAD, name + " " + "Non Master CPU Read Access Violation Flag Clear");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Non Master CPU Write Access Violation Flag Clear");
    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Non Master CPU Fetch Access Violation Flag Clear");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Non Master DMA Write Access Violation Flag Clear");
    all_zero &= check_zero(obj.CLA1READ, name + " " + "Non Master CLA1 Read Access Violation Flag Clear");
    all_zero &= check_zero(obj.CLA1WRITE, name + " " + "Non Master CLA1 Write Access Violation Flag Clear");
    all_zero &= check_zero(obj.CLA1FETCH, name + " " + "Non Master CLA1 Fetch Access Violation Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMAVCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMAVINTEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUREAD, name + " " + "Non Master CPU Read Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Non Master CPU Write Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Non Master CPU Fetch Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Non Master DMA Write Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CLA1READ, name + " " + "Non Master CLA1 Read Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CLA1WRITE, name + " " + "Non Master CLA1 Write Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CLA1FETCH, name + " " + "Non Master CLA1 Fetch Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMAVINTEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MAVFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Master CPU Fetch Access Violation Flag");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Master CPU Write Access Violation Flag");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Master DMA Write Access Violation Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MAVFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MAVSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Master CPU Fetch Access Violation Flag Set");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Master CPU Write Access Violation Flag Set");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Master DMA Write Access Violation Flag Set");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MAVSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MAVCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Master CPU Fetch Access Violation Flag Clear");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Master CPU Write Access Violation Flag Clear");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Master DMA Write Access Violation Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MAVCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MAVINTEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUFETCH, name + " " + "Master CPU Fetch Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.CPUWRITE, name + " " + "Master CPU Write Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.DMAWRITE, name + " " + "Master DMA Write Access Violation Interrupt Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MAVINTEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ACCESS_PROTECTION_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.NMAVFLG, name + " " + "Non-Master Access Violation Flag Register");
    all_zero &= check_zero(obj.NMAVSET, name + " " + "Non-Master Access Violation Flag Set Register");
    all_zero &= check_zero(obj.NMAVCLR, name + " " + "Non-Master Access Violation Flag Clear Register");
    all_zero &= check_zero(obj.NMAVINTEN, name + " " + "Non-Master Access Violation Interrupt Enable Register");
    all_zero &= check_zero(obj.NMCPURDAVADDR, name + " " + "Non-Master CPU Read Access Violation Address");
    all_zero &= check_zero(obj.NMCPUWRAVADDR, name + " " + "Non-Master CPU Write Access Violation Address");
    all_zero &= check_zero(obj.NMCPUFAVADDR, name + " " + "Non-Master CPU Fetch Access Violation Address");
    all_zero &= check_zero(obj.NMDMAWRAVADDR, name + " " + "Non-Master DMA Write Access Violation Address");
    all_zero &= check_zero(obj.NMCLA1RDAVADDR, name + " " + "Non-Master CLA1 Read Access Violation Address");
    all_zero &= check_zero(obj.NMCLA1WRAVADDR, name + " " + "Non-Master CLA1 Write Access Violation Address");
    all_zero &= check_zero(obj.NMCLA1FAVADDR, name + " " + "Non-Master CLA1 Fetch Access Violation Address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MAVFLG, name + " " + "Master Access Violation Flag Register");
    all_zero &= check_zero(obj.MAVSET, name + " " + "Master Access Violation Flag Set Register");
    all_zero &= check_zero(obj.MAVCLR, name + " " + "Master Access Violation Flag Clear Register");
    all_zero &= check_zero(obj.MAVINTEN, name + " " + "Master Access Violation Interrupt Enable Register");
    all_zero &= check_zero(obj.MCPUFAVADDR, name + " " + "Master CPU Fetch Access Violation Address");
    all_zero &= check_zero(obj.MCPUWRAVADDR, name + " " + "Master CPU Write Access Violation Address");
    all_zero &= check_zero(obj.MDMAWRAVADDR, name + " " + "Master  DMA Write Access Violation Address");

    return all_zero;
}

bool check_zero(const UCERRFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Uncorrectable Read Error Flag");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Uncorrectable Read Error Flag");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Uncorrectable Read Error Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const UCERRFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const UCERRSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Uncorrectable Read Error Flag Set");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Uncorrectable Read Error Flag Set");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Uncorrectable Read Error Flag Set");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const UCERRSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const UCERRCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Uncorrectable Read Error Flag Clear");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Uncorrectable Read Error Flag Clear");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Uncorrectable Read Error Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const UCERRCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CERRFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Correctable Read Error Flag");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Correctable Read Error Flag");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Correctable Read Error Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CERRFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CERRSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Correctable Read Error Flag Set");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Correctable Read Error Flag Set");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Correctable Read Error Flag Set");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CERRSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CERRCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPURDERR, name + " " + "CPU Correctable Read Error Flag Clear");
    all_zero &= check_zero(obj.DMARDERR, name + " " + "DMA Correctable Read Error Flag Clear");
    all_zero &= check_zero(obj.CLA1RDERR, name + " " + "CLA1 Correctable Read Error Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CERRCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CEINTFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CEINTFLAG, name + " " + "Total corrected error count exceeded threshold flag.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CEINTFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CEINTCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CEINTCLR, name + " " + "CPU Corrected Error Threshold Exceeded Error Clear.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CEINTCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CEINTSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CEINTSET, name + " " + "Total corrected error count exceeded flag set.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CEINTSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CEINTEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CEINTEN, name + " " + "CPU/DMA Correctable Error Interrupt Enable.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CEINTEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MEMORY_ERROR_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.UCERRFLG, name + " " + "Uncorrectable Error Flag Register");
    all_zero &= check_zero(obj.UCERRSET, name + " " + "Uncorrectable Error Flag Set Register");
    all_zero &= check_zero(obj.UCERRCLR, name + " " + "Uncorrectable Error Flag Clear Register");
    all_zero &= check_zero(obj.UCCPUREADDR, name + " " + "Uncorrectable CPU Read Error Address");
    all_zero &= check_zero(obj.UCDMAREADDR, name + " " + "Uncorrectable DMA Read Error Address");
    all_zero &= check_zero(obj.UCCLA1READDR, name + " " + "Uncorrectable CLA1 Read Error Address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CERRFLG, name + " " + "Correctable Error Flag Register");
    all_zero &= check_zero(obj.CERRSET, name + " " + "Correctable Error Flag Set Register");
    all_zero &= check_zero(obj.CERRCLR, name + " " + "Correctable Error Flag Clear Register");
    all_zero &= check_zero(obj.CCPUREADDR, name + " " + "Correctable CPU Read Error Address");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CERRCNT, name + " " + "Correctable Error Count Register");
    all_zero &= check_zero(obj.CERRTHRES, name + " " + "Correctable Error Threshold Value Register");
    all_zero &= check_zero(obj.CEINTFLG, name + " " + "Correctable Error Interrupt Flag Status Register");
    all_zero &= check_zero(obj.CEINTCLR, name + " " + "Correctable Error Interrupt Flag Clear Register");
    all_zero &= check_zero(obj.CEINTSET, name + " " + "Correctable Error Interrupt Flag Set Register");
    all_zero &= check_zero(obj.CEINTEN, name + " " + "Correctable Error Interrupt Enable Register");

    return all_zero;
}

bool check_zero(const ROMWAITSTATE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WSDISABLE, name + " " + "C28x ROM Wait State Enable/Disable Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ROMWAITSTATE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ROM_WAIT_STATE_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ROMWAITSTATE, name + " " + "ROM Wait State Configuration Register");

    return all_zero;
}

bool check_zero(const ROMPREFETCH_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PFENABLE, name + " " + "ROM Prefetch Enable/Disable Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ROMPREFETCH_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ROM_PREFETCH_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ROMPREFETCH, name + " " + "ROM Prefetch Configuration Register");

    return all_zero;
}

bool check_zero(const NMICFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.NMIE, name + " " + "Global NMI Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMICFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMIFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.NMIINT, name + " " + "NMI Interrupt Flag");
    all_zero &= check_zero(obj.CLOCKFAIL, name + " " + "Clock Fail Interrupt Flag");
    all_zero &= check_zero(obj.RAMUNCERR, name + " " + "RAM Uncorrectable Error NMI Flag");
    all_zero &= check_zero(obj.FLUNCERR, name + " " + "Flash Uncorrectable Error NMI Flag");
    all_zero &= check_zero(obj.CPU1HWBISTERR, name + " " + "HW BIST Error NMI Flag");
    all_zero &= check_zero(obj.CPU2HWBISTERR, name + " " + "HW BIST Error NMI Flag");
    all_zero &= check_zero(obj.PIEVECTERR, name + " " + "PIE Vector Fetch Error Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLBNMI, name + " " + "Configurable Logic Block NMI Flag");
    all_zero &= check_zero(obj.CPU2WDRSn, name + " " + "CPU2 WDRSn Reset Indication Flag");
    all_zero &= check_zero(obj.CPU2NMIWDRSn, name + " " + "CPU2 NMIWDRSn Reset Indication Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMIFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMIFLGCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.NMIINT, name + " " + "NMIINT Flag Clear");
    all_zero &= check_zero(obj.CLOCKFAIL, name + " " + "CLOCKFAIL Flag Clear");
    all_zero &= check_zero(obj.RAMUNCERR, name + " " + "RAMUNCERR Flag Clear");
    all_zero &= check_zero(obj.FLUNCERR, name + " " + "FLUNCERR Flag Clear");
    all_zero &= check_zero(obj.CPU1HWBISTERR, name + " " + "CPU1HWBISTERR Flag Clear");
    all_zero &= check_zero(obj.CPU2HWBISTERR, name + " " + "CPU2HWBISTERR Flag Clear");
    all_zero &= check_zero(obj.PIEVECTERR, name + " " + "PIEVECTERR Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLBNMI, name + " " + "CLBNMI Flag Clear");
    all_zero &= check_zero(obj.CPU2WDRSn, name + " " + "CPU2WDRSn Flag Clear");
    all_zero &= check_zero(obj.CPU2NMIWDRSn, name + " " + "CPU2NMIWDRSn Flag Clear");
    all_zero &= check_zero(obj.OVF, name + " " + "OVF Flag Clear");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMIFLGCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMIFLGFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLOCKFAIL, name + " " + "CLOCKFAIL Flag Force");
    all_zero &= check_zero(obj.RAMUNCERR, name + " " + "RAMUNCERR Flag Force");
    all_zero &= check_zero(obj.FLUNCERR, name + " " + "FLUNCERR Flag Force");
    all_zero &= check_zero(obj.CPU1HWBISTERR, name + " " + "CPU1HWBISTERR Flag Force");
    all_zero &= check_zero(obj.CPU2HWBISTERR, name + " " + "CPU2HWBISTERR Flag Force");
    all_zero &= check_zero(obj.PIEVECTERR, name + " " + "PIEVECTERR Flag Force");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLBNMI, name + " " + "CLBNMI Flag Force");
    all_zero &= check_zero(obj.CPU2WDRSn, name + " " + "CPU2WDRSn Flag Force");
    all_zero &= check_zero(obj.CPU2NMIWDRSn, name + " " + "CPU2NMIWDRSn Flag Force");
    all_zero &= check_zero(obj.OVF, name + " " + "OVF Flag Force");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMIFLGFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMISHDFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLOCKFAIL, name + " " + "Shadow CLOCKFAIL Flag");
    all_zero &= check_zero(obj.RAMUNCERR, name + " " + "Shadow RAMUNCERR Flag");
    all_zero &= check_zero(obj.FLUNCERR, name + " " + "Shadow FLUNCERR Flag");
    all_zero &= check_zero(obj.CPU1HWBISTERR, name + " " + "Shadow CPU1HWBISTERR Flag");
    all_zero &= check_zero(obj.CPU2HWBISTERR, name + " " + "Shadow CPU2HWBISTERR Flag");
    all_zero &= check_zero(obj.PIEVECTERR, name + " " + "Shadow PIEVECTERR Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLBNMI, name + " " + "Shadow CLBNMI Flag");
    all_zero &= check_zero(obj.CPU2WDRSn, name + " " + "Shadow CPU2WDRSn Flag");
    all_zero &= check_zero(obj.CPU2NMIWDRSn, name + " " + "Shadow CPU2NMIWDRSn Flag");
    all_zero &= check_zero(obj.OVF, name + " " + "Shadow OVF Flag");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const NMISHDFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const NMI_INTRUPT_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.NMICFG, name + " " + "NMI Configuration Register");
    all_zero &= check_zero(obj.NMIFLG, name + " " + "NMI Flag Register (XRSn Clear)");
    all_zero &= check_zero(obj.NMIFLGCLR, name + " " + "NMI Flag Clear Register");
    all_zero &= check_zero(obj.NMIFLGFRC, name + " " + "NMI Flag Force Register");
    all_zero &= check_zero(obj.NMIWDCNT, name + " " + "NMI Watchdog Counter Register");
    all_zero &= check_zero(obj.NMIWDPRD, name + " " + "NMI Watchdog Period Register");
    all_zero &= check_zero(obj.NMISHDFLG, name + " " + "NMI Shadow Flag Register");

    return all_zero;
}

bool check_zero(const MCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HARDRESET, name + " " + "Hard Reset");
    all_zero &= check_zero(obj.SOFTRESET, name + " " + "Soft Reset");
    all_zero &= check_zero(obj.IACKE, name + " " + "IACK enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MIFR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Flag");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Flag");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Flag");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Flag");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Flag");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Flag");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Flag");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MIFR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MIOVF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Overflow Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MIOVF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MIFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Force");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Force");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Force");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Force");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Force");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Force");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Force");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Force");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MIFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MICLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Flag Clear");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MICLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MICLROVF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Overflow Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MICLROVF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MIER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Interrupt Enable");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Interrupt Enable");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Interrupt Enable");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Interrupt Enable");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Interrupt Enable");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Interrupt Enable");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Interrupt Enable");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Interrupt Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MIER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MIRUN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1, name + " " + "Task 1 Run Status");
    all_zero &= check_zero(obj.INT2, name + " " + "Task 2 Run Status");
    all_zero &= check_zero(obj.INT3, name + " " + "Task 3 Run Status");
    all_zero &= check_zero(obj.INT4, name + " " + "Task 4 Run Status");
    all_zero &= check_zero(obj.INT5, name + " " + "Task 5 Run Status");
    all_zero &= check_zero(obj.INT6, name + " " + "Task 6 Run Status");
    all_zero &= check_zero(obj.INT7, name + " " + "Task 7 Run Status");
    all_zero &= check_zero(obj.INT8, name + " " + "Task 8 Run Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MIRUN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const _MSTF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LVF, name + " " + "Latched Overflow Flag");
    all_zero &= check_zero(obj.LUF, name + " " + "Latched Underflow Flag");
    all_zero &= check_zero(obj.NF, name + " " + "Negative Float Flag");
    all_zero &= check_zero(obj.ZF, name + " " + "Zero Float Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TF, name + " " + "Test Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.RNDF32, name + " " + "Round 32-bit Floating-Point Mode");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.MEALLOW, name + " " + "MEALLOW Status");
    all_zero &= check_zero(obj._RPC, name + " " + "Return PC");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const _MSTF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.i32, name + " " + "i32");
    all_zero &= check_zero(obj.f32, name + " " + "f32");

    return all_zero;
}

bool check_zero(const CLA_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MVECT1, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT2, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT3, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT4, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT5, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT6, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT7, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.MVECT8, name + " " + "Task Interrupt Vector");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MCTL, name + " " + "Control Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.MIFR, name + " " + "Interrupt Flag Register");
    all_zero &= check_zero(obj.MIOVF, name + " " + "Interrupt Overflow Flag Register");
    all_zero &= check_zero(obj.MIFRC, name + " " + "Interrupt Force Register");
    all_zero &= check_zero(obj.MICLR, name + " " + "Interrupt Flag Clear Register");
    all_zero &= check_zero(obj.MICLROVF, name + " " + "Interrupt Overflow Flag Clear Register");
    all_zero &= check_zero(obj.MIER, name + " " + "Interrupt Enable Register");
    all_zero &= check_zero(obj.MIRUN, name + " " + "Interrupt Run Status Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj._MPC, name + " " + "CLA Program Counter");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj._MAR0, name + " " + "CLA Auxiliary Register 0");
    all_zero &= check_zero(obj._MAR1, name + " " + "CLA Auxiliary Register 1");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj._MSTF, name + " " + "CLA Floating-Point Status Register");
    all_zero &= check_zero(obj._MR0, name + " " + "CLA Floating-Point Result Register 0");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj._MR1, name + " " + "CLA Floating-Point Result Register 1");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj._MR2, name + " " + "CLA Floating-Point Result Register 2");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj._MR3, name + " " + "CLA Floating-Point Result Register 3");

    return all_zero;
}

bool check_zero(const SOFTINTEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TASK1, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK2, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK3, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK4, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK5, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK6, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK7, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.TASK8, name + " " + "Configure Software Interrupt or End of Task interrupt.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTINTEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTINTFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TASK1, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK2, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK3, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK4, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK5, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK6, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK7, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.TASK8, name + " " + "Force CLA software interrupt for the corresponding task.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTINTFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLA_SOFTINT_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOFTINTEN, name + " " + "CLA Software Interrupt Enable Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTINTFRC, name + " " + "CLA Software Interrupt Force Register");

    return all_zero;
}

bool check_zero(const RCSR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MINOR_REVISION, name + " " + "Minor Revision.");
    all_zero &= check_zero(obj.MAJOR_REVISION, name + " " + "Major Revision.");
    all_zero &= check_zero(obj.MODULE_ID, name + " " + "EMIF module ID.");
    all_zero &= check_zero(obj.FR, name + " " + "EMIF is running in full rate or half rate.");
    all_zero &= check_zero(obj.BE, name + " " + "EMIF endian mode.");

    return all_zero;
}

bool check_zero(const RCSR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ASYNC_WCCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MAX_EXT_WAIT, name + " " + "Maximum Extended Wait cycles.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.WP0, name + " " + "Polarity for EMxWAIT.");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ASYNC_WCCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDRAM_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PAGESIGE, name + " " + "Page Size.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.IBANK, name + " " + "Internal Bank setup of SDRAM devices.");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.BIT_11_9_LOCK, name + " " + "Bits 11 to 9 are writable only if this bit is set.");
    all_zero &= check_zero(obj.CL, name + " " + "CAS Latency.");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.NM, name + " " + "Narrow Mode.");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.PDWR, name + " " + "Perform refreshes during Power Down.");
    all_zero &= check_zero(obj.PD, name + " " + "Power Down.");
    all_zero &= check_zero(obj.SR, name + " " + "Self Refresh.");

    return all_zero;
}

bool check_zero(const SDRAM_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDRAM_RCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.REFRESH_RATE, name + " " + "Refresh Rate.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDRAM_RCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ASYNC_CS2_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ASIZE, name + " " + "Asynchronous Memory Size.");
    all_zero &= check_zero(obj.TA, name + " " + "Turn Around cycles.");
    all_zero &= check_zero(obj.R_HOLD, name + " " + "Read Strobe Hold cycles.");
    all_zero &= check_zero(obj.R_STROBE, name + " " + "Read Strobe Duration cycles.");
    all_zero &= check_zero(obj.R_SETUP, name + " " + "Read Strobe Setup cycles.");
    all_zero &= check_zero(obj.W_HOLD, name + " " + "Write Strobe Hold cycles.");
    all_zero &= check_zero(obj.W_STROBE, name + " " + "Write Strobe Duration cycles.");
    all_zero &= check_zero(obj.W_SETUP, name + " " + "Write Strobe Setup cycles.");
    all_zero &= check_zero(obj.EW, name + " " + "Extend Wait mode.");
    all_zero &= check_zero(obj.SS, name + " " + "Select Strobe mode.");

    return all_zero;
}

bool check_zero(const ASYNC_CS2_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ASYNC_CS3_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ASIZE, name + " " + "Asynchronous Memory Size.");
    all_zero &= check_zero(obj.TA, name + " " + "Turn Around cycles.");
    all_zero &= check_zero(obj.R_HOLD, name + " " + "Read Strobe Hold cycles.");
    all_zero &= check_zero(obj.R_STROBE, name + " " + "Read Strobe Duration cycles.");
    all_zero &= check_zero(obj.R_SETUP, name + " " + "Read Strobe Setup cycles.");
    all_zero &= check_zero(obj.W_HOLD, name + " " + "Write Strobe Hold cycles.");
    all_zero &= check_zero(obj.W_STROBE, name + " " + "Write Strobe Duration cycles.");
    all_zero &= check_zero(obj.W_SETUP, name + " " + "Write Strobe Setup cycles.");
    all_zero &= check_zero(obj.EW, name + " " + "Extend Wait mode.");
    all_zero &= check_zero(obj.SS, name + " " + "Select Strobe mode.");

    return all_zero;
}

bool check_zero(const ASYNC_CS3_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ASYNC_CS4_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ASIZE, name + " " + "Asynchronous Memory Size.");
    all_zero &= check_zero(obj.TA, name + " " + "Turn Around cycles.");
    all_zero &= check_zero(obj.R_HOLD, name + " " + "Read Strobe Hold cycles.");
    all_zero &= check_zero(obj.R_STROBE, name + " " + "Read Strobe Duration cycles.");
    all_zero &= check_zero(obj.R_SETUP, name + " " + "Read Strobe Setup cycles.");
    all_zero &= check_zero(obj.W_HOLD, name + " " + "Write Strobe Hold cycles.");
    all_zero &= check_zero(obj.W_STROBE, name + " " + "Write Strobe Duration cycles.");
    all_zero &= check_zero(obj.W_SETUP, name + " " + "Write Strobe Setup cycles.");
    all_zero &= check_zero(obj.EW, name + " " + "Extend Wait mode.");
    all_zero &= check_zero(obj.SS, name + " " + "Select Strobe mode.");

    return all_zero;
}

bool check_zero(const ASYNC_CS4_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDRAM_TR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.T_RRD, name + " " + "Activate to Activate timing for different bank.");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.T_RC, name + " " + "Activate to Activate timing .");
    all_zero &= check_zero(obj.T_RAS, name + " " + "Activate to Precharge timing.");
    all_zero &= check_zero(obj.T_WR, name + " " + "Last Write to Precharge timing.");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.T_RCD, name + " " + "Activate to Read/Write timing.");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.T_RP, name + " " + "Precharge to Activate/Refresh timing.");
    all_zero &= check_zero(obj.T_RFC, name + " " + "Refresh/Load Mode to Refresh/Activate timing");

    return all_zero;
}

bool check_zero(const SDRAM_TR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDR_EXT_TMNG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.T_XS, name + " " + "Self Refresh exit to new command timing.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDR_EXT_TMNG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const INT_RAW_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AT, name + " " + "Asynchronous Timeout.");
    all_zero &= check_zero(obj.LT, name + " " + "Line Trap.");
    all_zero &= check_zero(obj.WR, name + " " + "Wait Rise.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INT_RAW_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const INT_MSK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AT_MASKED, name + " " + "Asynchronous Timeout.");
    all_zero &= check_zero(obj.LT_MASKED, name + " " + "Line Trap.");
    all_zero &= check_zero(obj.WR_MASKED, name + " " + "Wait Rise.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INT_MSK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const INT_MSK_SET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AT_MASK_SET, name + " " + "Asynchronous Timeout.");
    all_zero &= check_zero(obj.LT_MASK_SET, name + " " + "Line Trap.");
    all_zero &= check_zero(obj.WR_MASK_SET, name + " " + "Wait Rise.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INT_MSK_SET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const INT_MSK_CLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AT_MASK_CLR, name + " " + "Asynchronous Timeout.");
    all_zero &= check_zero(obj.LT_MASK_CLR, name + " " + "Line Trap.");
    all_zero &= check_zero(obj.WR_MASK_CLR, name + " " + "Wait Rise.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INT_MSK_CLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EMIF_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RCSR, name + " " + "Revision Code and Status Register");
    all_zero &= check_zero(obj.ASYNC_WCCR, name + " " + "Async Wait Cycle Config Register");
    all_zero &= check_zero(obj.SDRAM_CR, name + " " + "SDRAM (EMxCS0n) Config Register");
    all_zero &= check_zero(obj.SDRAM_RCR, name + " " + "SDRAM Refresh Control Register");
    all_zero &= check_zero(obj.ASYNC_CS2_CR, name + " " + "Async 1 (EMxCS2n) Config Register");
    all_zero &= check_zero(obj.ASYNC_CS3_CR, name + " " + "Async 2 (EMxCS3n) Config Register");
    all_zero &= check_zero(obj.ASYNC_CS4_CR, name + " " + "Async 3 (EMxCS4n) Config Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDRAM_TR, name + " " + "SDRAM Timing Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TOTAL_SDRAM_AR, name + " " + "Total SDRAM Accesses Register");
    all_zero &= check_zero(obj.TOTAL_SDRAM_ACTR, name + " " + "Total SDRAM Activate Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDR_EXT_TMNG, name + " " + "SDRAM SR/PD Exit Timing Register");
    all_zero &= check_zero(obj.INT_RAW, name + " " + "Interrupt Raw Register");
    all_zero &= check_zero(obj.INT_MSK, name + " " + "Interrupt Masked Register");
    all_zero &= check_zero(obj.INT_MSK_SET, name + " " + "Interrupt Mask Set Register");
    all_zero &= check_zero(obj.INT_MSK_CLR, name + " " + "Interrupt Mask Clear Register");

    return all_zero;
}

bool check_zero(const DRR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HWLB, name + " " + "High word low byte");
    all_zero &= check_zero(obj.HWHB, name + " " + "High word high byte");

    return all_zero;
}

bool check_zero(const DRR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DRR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LWLB, name + " " + "Low word low byte");
    all_zero &= check_zero(obj.LWHB, name + " " + "Low word high byte");

    return all_zero;
}

bool check_zero(const DRR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DXR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HWLB, name + " " + "High word low byte");
    all_zero &= check_zero(obj.HWHB, name + " " + "High word high byte");

    return all_zero;
}

bool check_zero(const DXR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DXR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LWLB, name + " " + "Low word low byte");
    all_zero &= check_zero(obj.LWHB, name + " " + "Low word high byte");

    return all_zero;
}

bool check_zero(const DXR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPCR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XRST, name + " " + "Transmitter reset");
    all_zero &= check_zero(obj.XRDY, name + " " + "Transmitter ready");
    all_zero &= check_zero(obj.XEMPTY, name + " " + "Transmitter empty");
    all_zero &= check_zero(obj.XSYNCERR, name + " " + "Transmit sync error INT flag");
    all_zero &= check_zero(obj.XINTM, name + " " + "Transmit Interupt mode bits");
    all_zero &= check_zero(obj.GRST, name + " " + "Sample rate generator reset");
    all_zero &= check_zero(obj.FRST, name + " " + "Frame sync logic reset");
    all_zero &= check_zero(obj.SOFT, name + " " + "SOFT bit");
    all_zero &= check_zero(obj.FREE, name + " " + "FREE bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPCR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPCR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RRST, name + " " + "Receiver reset");
    all_zero &= check_zero(obj.RRDY, name + " " + "Receiver ready");
    all_zero &= check_zero(obj.RFULL, name + " " + "Receiver full");
    all_zero &= check_zero(obj.RSYNCERR, name + " " + "Receive sync error INT flag");
    all_zero &= check_zero(obj.RINTM, name + " " + "Receive Interupt mode bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.DXENA, name + " " + "DX delay enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLKSTP, name + " " + "Clock stop mode");
    all_zero &= check_zero(obj.RJUST, name + " " + "Rx sign extension and justification mode");
    all_zero &= check_zero(obj.DLB, name + " " + "Digital loopback");

    return all_zero;
}

bool check_zero(const SPCR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RCR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RDATDLY, name + " " + "Receive data delay");
    all_zero &= check_zero(obj.RFIG, name + " " + "Receive frame sync ignore");
    all_zero &= check_zero(obj.RCOMPAND, name + " " + "Receive Companding Mode selects");
    all_zero &= check_zero(obj.RWDLEN2, name + " " + "Receive word length 2");
    all_zero &= check_zero(obj.RFRLEN2, name + " " + "Receive Frame length 2");
    all_zero &= check_zero(obj.RPHASE, name + " " + "Receive Phase");

    return all_zero;
}

bool check_zero(const RCR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RCR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RWDLEN1, name + " " + "Receive word length 1");
    all_zero &= check_zero(obj.RFRLEN1, name + " " + "Receive Frame length 1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const RCR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XCR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XDATDLY, name + " " + "Transmit data delay");
    all_zero &= check_zero(obj.XFIG, name + " " + "Transmit frame sync ignore");
    all_zero &= check_zero(obj.XCOMPAND, name + " " + "Transmit Companding Mode selects");
    all_zero &= check_zero(obj.XWDLEN2, name + " " + "Transmit word length 2");
    all_zero &= check_zero(obj.XFRLEN2, name + " " + "Transmit Frame length 2");
    all_zero &= check_zero(obj.XPHASE, name + " " + "Transmit Phase");

    return all_zero;
}

bool check_zero(const XCR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XCR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.XWDLEN1, name + " " + "Transmit word length 1");
    all_zero &= check_zero(obj.XFRLEN1, name + " " + "Transmit Frame length 1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XCR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SRGR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FPER, name + " " + "Frame-sync period");
    all_zero &= check_zero(obj.FSGM, name + " " + "Frame sync generator mode");
    all_zero &= check_zero(obj.CLKSM, name + " " + "Sample rate generator mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GSYNC, name + " " + "CLKG sync");

    return all_zero;
}

bool check_zero(const SRGR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SRGR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKGDV, name + " " + "CLKG divider");
    all_zero &= check_zero(obj.FWID, name + " " + "Frame width");

    return all_zero;
}

bool check_zero(const SRGR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MCR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XMCM, name + " " + "Transmit data delay");
    all_zero &= check_zero(obj.XCBLK, name + " " + "Transmit frame sync ignore");
    all_zero &= check_zero(obj.XPABLK, name + " " + "Transmit Companding Mode selects");
    all_zero &= check_zero(obj.XPBBLK, name + " " + "Transmit word length 2");
    all_zero &= check_zero(obj.XMCME, name + " " + "Transmit Frame length 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MCR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MCR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RMCM, name + " " + "Receive multichannel mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RCBLK, name + " " + "eceive current block");
    all_zero &= check_zero(obj.RPABLK, name + " " + "Receive partition A Block");
    all_zero &= check_zero(obj.RPBBLK, name + " " + "Receive partition B Block");
    all_zero &= check_zero(obj.RMCME, name + " " + "Receive multi-channel enhance mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MCR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKRP, name + " " + "Receive Clock polarity");
    all_zero &= check_zero(obj.CLKXP, name + " " + "Transmit clock polarity");
    all_zero &= check_zero(obj.FSRP, name + " " + "Receive Frame synchronization polarity");
    all_zero &= check_zero(obj.FSXP, name + " " + "Transmit Frame synchronization polarity");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCLKME, name + " " + "Sample clock mode selection");
    all_zero &= check_zero(obj.CLKRM, name + " " + "Receiver Clock Mode");
    all_zero &= check_zero(obj.CLKXM, name + " " + "Transmit Clock Mode.");
    all_zero &= check_zero(obj.FSRM, name + " " + "Receive Frame Synchronization Mode");
    all_zero &= check_zero(obj.FSXM, name + " " + "Transmit Frame Synchronization Mode");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MFFINT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XINT, name + " " + "Enable for Receive Interrupt");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RINT, name + " " + "Enable for transmit Interrupt");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MFFINT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const McBSP_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DRR2, name + " " + "Data receive register bits 31-16");
    all_zero &= check_zero(obj.DRR1, name + " " + "Data receive register bits 15-0");
    all_zero &= check_zero(obj.DXR2, name + " " + "Data transmit register bits 31-16");
    all_zero &= check_zero(obj.DXR1, name + " " + "Data transmit register bits 15-0");
    all_zero &= check_zero(obj.SPCR2, name + " " + "Serial port control register 2");
    all_zero &= check_zero(obj.SPCR1, name + " " + "Serial port control register 1");
    all_zero &= check_zero(obj.RCR2, name + " " + "Receive Control register 2");
    all_zero &= check_zero(obj.RCR1, name + " " + "Receive Control register 1");
    all_zero &= check_zero(obj.XCR2, name + " " + "Transmit Control register 2");
    all_zero &= check_zero(obj.XCR1, name + " " + "Transmit Control register 1");
    all_zero &= check_zero(obj.SRGR2, name + " " + "Sample rate generator register 2");
    all_zero &= check_zero(obj.SRGR1, name + " " + "Sample rate generator register 1");
    all_zero &= check_zero(obj.MCR2, name + " " + "Multi-channel control register 2");
    all_zero &= check_zero(obj.MCR1, name + " " + "Multi-channel control register 1");
    all_zero &= check_zero(obj.RCERA, name + " " + "Receive channel enable partition A");
    all_zero &= check_zero(obj.RCERB, name + " " + "Receive channel enable partition B");
    all_zero &= check_zero(obj.XCERA, name + " " + "Transmit channel enable partition A");
    all_zero &= check_zero(obj.XCERB, name + " " + "Transmit channel enable partition B");
    all_zero &= check_zero(obj.PCR, name + " " + "Pin Control register");
    all_zero &= check_zero(obj.RCERC, name + " " + "Receive channel enable partition C");
    all_zero &= check_zero(obj.RCERD, name + " " + "Receive channel enable partition D");
    all_zero &= check_zero(obj.XCERC, name + " " + "Transmit channel enable partition C");
    all_zero &= check_zero(obj.XCERD, name + " " + "Transmit channel enable partition D");
    all_zero &= check_zero(obj.RCERE, name + " " + "Receive channel enable partition E");
    all_zero &= check_zero(obj.RCERF, name + " " + "Receive channel enable partition F");
    all_zero &= check_zero(obj.XCERE, name + " " + "Transmit channel enable partition E");
    all_zero &= check_zero(obj.XCERF, name + " " + "Transmit channel enable partition F");
    all_zero &= check_zero(obj.RCERG, name + " " + "Receive channel enable partition G");
    all_zero &= check_zero(obj.RCERH, name + " " + "Receive channel enable partition H");
    all_zero &= check_zero(obj.XCERG, name + " " + "Transmit channel enable partition G");
    all_zero &= check_zero(obj.XCERH, name + " " + "Transmit channel enable partition H");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MFFINT, name + " " + "Interrupt enable");

    return all_zero;
}

bool check_zero(const PIE_VECT_TABLE &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PIE1_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE2_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE3_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE4_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE5_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE6_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE7_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE8_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE9_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE10_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE11_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE12_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.PIE13_RESERVED_INT, name + " " + "Reserved");
    all_zero &= check_zero(obj.TIMER1_INT, name + " " + "CPU Timer 1 Interrupt");
    all_zero &= check_zero(obj.TIMER2_INT, name + " " + "CPU Timer 2 Interrupt");
    all_zero &= check_zero(obj.DATALOG_INT, name + " " + "Datalogging Interrupt");
    all_zero &= check_zero(obj.RTOS_INT, name + " " + "RTOS Interrupt");
    all_zero &= check_zero(obj.EMU_INT, name + " " + "Emulation Interrupt");
    all_zero &= check_zero(obj.NMI_INT, name + " " + "Non-Maskable Interrupt");
    all_zero &= check_zero(obj.ILLEGAL_INT, name + " " + "Illegal Operation Trap");
    all_zero &= check_zero(obj.USER1_INT, name + " " + "User Defined Trap 1");
    all_zero &= check_zero(obj.USER2_INT, name + " " + "User Defined Trap 2");
    all_zero &= check_zero(obj.USER3_INT, name + " " + "User Defined Trap 3");
    all_zero &= check_zero(obj.USER4_INT, name + " " + "User Defined Trap 4");
    all_zero &= check_zero(obj.USER5_INT, name + " " + "User Defined Trap 5");
    all_zero &= check_zero(obj.USER6_INT, name + " " + "User Defined Trap 6");
    all_zero &= check_zero(obj.USER7_INT, name + " " + "User Defined Trap 7");
    all_zero &= check_zero(obj.USER8_INT, name + " " + "User Defined Trap 8");
    all_zero &= check_zero(obj.USER9_INT, name + " " + "User Defined Trap 9");
    all_zero &= check_zero(obj.USER10_INT, name + " " + "User Defined Trap 10");
    all_zero &= check_zero(obj.USER11_INT, name + " " + "User Defined Trap 11");
    all_zero &= check_zero(obj.USER12_INT, name + " " + "User Defined Trap 12");
    all_zero &= check_zero(obj.ADCA1_INT, name + " " + "1.1 - ADCA Interrupt 1");
    all_zero &= check_zero(obj.ADCB1_INT, name + " " + "1.2 - ADCB Interrupt 1");
    all_zero &= check_zero(obj.ADCC1_INT, name + " " + "1.3 - ADCC Interrupt 1");
    all_zero &= check_zero(obj.XINT1_INT, name + " " + "1.4 - XINT1 Interrupt");
    all_zero &= check_zero(obj.XINT2_INT, name + " " + "1.5 - XINT2 Interrupt");
    all_zero &= check_zero(obj.ADCD1_INT, name + " " + "1.6 - ADCD Interrupt 1");
    all_zero &= check_zero(obj.TIMER0_INT, name + " " + "1.7 - Timer 0 Interrupt");
    all_zero &= check_zero(obj.WAKE_INT, name + " " + "1.8 - Standby and Halt Wakeup Interrupt");
    all_zero &= check_zero(obj.EPWM1_TZ_INT, name + " " + "2.1 - ePWM1 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM2_TZ_INT, name + " " + "2.2 - ePWM2 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM3_TZ_INT, name + " " + "2.3 - ePWM3 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM4_TZ_INT, name + " " + "2.4 - ePWM4 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM5_TZ_INT, name + " " + "2.5 - ePWM5 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM6_TZ_INT, name + " " + "2.6 - ePWM6 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM7_TZ_INT, name + " " + "2.7 - ePWM7 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM8_TZ_INT, name + " " + "2.8 - ePWM8 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM1_INT, name + " " + "3.1 - ePWM1 Interrupt");
    all_zero &= check_zero(obj.EPWM2_INT, name + " " + "3.2 - ePWM2 Interrupt");
    all_zero &= check_zero(obj.EPWM3_INT, name + " " + "3.3 - ePWM3 Interrupt");
    all_zero &= check_zero(obj.EPWM4_INT, name + " " + "3.4 - ePWM4 Interrupt");
    all_zero &= check_zero(obj.EPWM5_INT, name + " " + "3.5 - ePWM5 Interrupt");
    all_zero &= check_zero(obj.EPWM6_INT, name + " " + "3.6 - ePWM6 Interrupt");
    all_zero &= check_zero(obj.EPWM7_INT, name + " " + "3.7 - ePWM7 Interrupt");
    all_zero &= check_zero(obj.EPWM8_INT, name + " " + "3.8 - ePWM8 Interrupt");
    all_zero &= check_zero(obj.ECAP1_INT, name + " " + "4.1 - eCAP1 Interrupt");
    all_zero &= check_zero(obj.ECAP2_INT, name + " " + "4.2 - eCAP2 Interrupt");
    all_zero &= check_zero(obj.ECAP3_INT, name + " " + "4.3 - eCAP3 Interrupt");
    all_zero &= check_zero(obj.ECAP4_INT, name + " " + "4.4 - eCAP4 Interrupt");
    all_zero &= check_zero(obj.ECAP5_INT, name + " " + "4.5 - eCAP5 Interrupt");
    all_zero &= check_zero(obj.ECAP6_INT, name + " " + "4.6 - eCAP6 Interrupt");
    all_zero &= check_zero(obj.PIE14_RESERVED_INT, name + " " + "4.7 - Reserved");
    all_zero &= check_zero(obj.PIE15_RESERVED_INT, name + " " + "4.8 - Reserved");
    all_zero &= check_zero(obj.EQEP1_INT, name + " " + "5.1 - eQEP1 Interrupt");
    all_zero &= check_zero(obj.EQEP2_INT, name + " " + "5.2 - eQEP2 Interrupt");
    all_zero &= check_zero(obj.EQEP3_INT, name + " " + "5.3 - eQEP3 Interrupt");
    all_zero &= check_zero(obj.PIE16_RESERVED_INT, name + " " + "5.4 - Reserved");
    all_zero &= check_zero(obj.PIE17_RESERVED_INT, name + " " + "5.5 - Reserved");
    all_zero &= check_zero(obj.PIE18_RESERVED_INT, name + " " + "5.6 - Reserved");
    all_zero &= check_zero(obj.PIE19_RESERVED_INT, name + " " + "5.7 - Reserved");
    all_zero &= check_zero(obj.PIE20_RESERVED_INT, name + " " + "5.8 - Reserved");
    all_zero &= check_zero(obj.SPIA_RX_INT, name + " " + "6.1 - SPIA Receive Interrupt");
    all_zero &= check_zero(obj.SPIA_TX_INT, name + " " + "6.2 - SPIA Transmit Interrupt");
    all_zero &= check_zero(obj.SPIB_RX_INT, name + " " + "6.3 - SPIB Receive Interrupt");
    all_zero &= check_zero(obj.SPIB_TX_INT, name + " " + "6.4 - SPIB Transmit Interrupt");
    all_zero &= check_zero(obj.MCBSPA_RX_INT, name + " " + "6.5 - McBSPA Receive Interrupt");
    all_zero &= check_zero(obj.MCBSPA_TX_INT, name + " " + "6.6 - McBSPA Transmit Interrupt");
    all_zero &= check_zero(obj.MCBSPB_RX_INT, name + " " + "6.7 - McBSPB Receive Interrupt");
    all_zero &= check_zero(obj.MCBSPB_TX_INT, name + " " + "6.8 - McBSPB Transmit Interrupt");
    all_zero &= check_zero(obj.DMA_CH1_INT, name + " " + "7.1 - DMA Channel 1 Interrupt");
    all_zero &= check_zero(obj.DMA_CH2_INT, name + " " + "7.2 - DMA Channel 2 Interrupt");
    all_zero &= check_zero(obj.DMA_CH3_INT, name + " " + "7.3 - DMA Channel 3 Interrupt");
    all_zero &= check_zero(obj.DMA_CH4_INT, name + " " + "7.4 - DMA Channel 4 Interrupt");
    all_zero &= check_zero(obj.DMA_CH5_INT, name + " " + "7.5 - DMA Channel 5 Interrupt");
    all_zero &= check_zero(obj.DMA_CH6_INT, name + " " + "7.6 - DMA Channel 6 Interrupt");
    all_zero &= check_zero(obj.PIE21_RESERVED_INT, name + " " + "7.7 - Reserved");
    all_zero &= check_zero(obj.PIE22_RESERVED_INT, name + " " + "7.8 - Reserved");
    all_zero &= check_zero(obj.I2CA_INT, name + " " + "8.1 - I2CA Interrupt 1");
    all_zero &= check_zero(obj.I2CA_FIFO_INT, name + " " + "8.2 - I2CA Interrupt 2");
    all_zero &= check_zero(obj.I2CB_INT, name + " " + "8.3 - I2CB Interrupt 1");
    all_zero &= check_zero(obj.I2CB_FIFO_INT, name + " " + "8.4 - I2CB Interrupt 2");
    all_zero &= check_zero(obj.SCIC_RX_INT, name + " " + "8.5 - SCIC Receive Interrupt");
    all_zero &= check_zero(obj.SCIC_TX_INT, name + " " + "8.6 - SCIC Transmit Interrupt");
    all_zero &= check_zero(obj.SCID_RX_INT, name + " " + "8.7 - SCID Receive Interrupt");
    all_zero &= check_zero(obj.SCID_TX_INT, name + " " + "8.8 - SCID Transmit Interrupt");
    all_zero &= check_zero(obj.SCIA_RX_INT, name + " " + "9.1 - SCIA Receive Interrupt");
    all_zero &= check_zero(obj.SCIA_TX_INT, name + " " + "9.2 - SCIA Transmit Interrupt");
    all_zero &= check_zero(obj.SCIB_RX_INT, name + " " + "9.3 - SCIB Receive Interrupt");
    all_zero &= check_zero(obj.SCIB_TX_INT, name + " " + "9.4 - SCIB Transmit Interrupt");
    all_zero &= check_zero(obj.CANA0_INT, name + " " + "9.5 - CANA Interrupt 0");
    all_zero &= check_zero(obj.CANA1_INT, name + " " + "9.6 - CANA Interrupt 1");
    all_zero &= check_zero(obj.CANB0_INT, name + " " + "9.7 - CANB Interrupt 0");
    all_zero &= check_zero(obj.CANB1_INT, name + " " + "9.8 - CANB Interrupt 1");
    all_zero &= check_zero(obj.ADCA_EVT_INT, name + " " + "10.1 - ADCA Event Interrupt");
    all_zero &= check_zero(obj.ADCA2_INT, name + " " + "10.2 - ADCA Interrupt 2");
    all_zero &= check_zero(obj.ADCA3_INT, name + " " + "10.3 - ADCA Interrupt 3");
    all_zero &= check_zero(obj.ADCA4_INT, name + " " + "10.4 - ADCA Interrupt 4");
    all_zero &= check_zero(obj.ADCB_EVT_INT, name + " " + "10.5 - ADCB Event Interrupt");
    all_zero &= check_zero(obj.ADCB2_INT, name + " " + "10.6 - ADCB Interrupt 2");
    all_zero &= check_zero(obj.ADCB3_INT, name + " " + "10.7 - ADCB Interrupt 3");
    all_zero &= check_zero(obj.ADCB4_INT, name + " " + "10.8 - ADCB Interrupt 4");
    all_zero &= check_zero(obj.CLA1_1_INT, name + " " + "11.1 - CLA1 Interrupt 1");
    all_zero &= check_zero(obj.CLA1_2_INT, name + " " + "11.2 - CLA1 Interrupt 2");
    all_zero &= check_zero(obj.CLA1_3_INT, name + " " + "11.3 - CLA1 Interrupt 3");
    all_zero &= check_zero(obj.CLA1_4_INT, name + " " + "11.4 - CLA1 Interrupt 4");
    all_zero &= check_zero(obj.CLA1_5_INT, name + " " + "11.5 - CLA1 Interrupt 5");
    all_zero &= check_zero(obj.CLA1_6_INT, name + " " + "11.6 - CLA1 Interrupt 6");
    all_zero &= check_zero(obj.CLA1_7_INT, name + " " + "11.7 - CLA1 Interrupt 7");
    all_zero &= check_zero(obj.CLA1_8_INT, name + " " + "11.8 - CLA1 Interrupt 8");
    all_zero &= check_zero(obj.XINT3_INT, name + " " + "12.1 - XINT3 Interrupt");
    all_zero &= check_zero(obj.XINT4_INT, name + " " + "12.2 - XINT4 Interrupt");
    all_zero &= check_zero(obj.XINT5_INT, name + " " + "12.3 - XINT5 Interrupt");
    all_zero &= check_zero(obj.PIE23_RESERVED_INT, name + " " + "12.4 - Reserved");
    all_zero &= check_zero(obj.PIE24_RESERVED_INT, name + " " + "12.5 - Reserved");
    all_zero &= check_zero(obj.VCU_INT, name + " " + "12.6 - VCU Interrupt");
    all_zero &= check_zero(obj.FPU_OVERFLOW_INT, name + " " + "12.7 - FPU Overflow Interrupt");
    all_zero &= check_zero(obj.FPU_UNDERFLOW_INT, name + " " + "12.8 - FPU Underflow Interrupt");
    all_zero &= check_zero(obj.PIE25_RESERVED_INT, name + " " + "1.9 - Reserved");
    all_zero &= check_zero(obj.PIE26_RESERVED_INT, name + " " + "1.10 - Reserved");
    all_zero &= check_zero(obj.PIE27_RESERVED_INT, name + " " + "1.11 - Reserved");
    all_zero &= check_zero(obj.PIE28_RESERVED_INT, name + " " + "1.12 - Reserved");
    all_zero &= check_zero(obj.IPC0_INT, name + " " + "1.13 - IPC Interrupt 0");
    all_zero &= check_zero(obj.IPC1_INT, name + " " + "1.14 - IPC Interrupt 1");
    all_zero &= check_zero(obj.IPC2_INT, name + " " + "1.15 - IPC Interrupt 2");
    all_zero &= check_zero(obj.IPC3_INT, name + " " + "1.16 - IPC Interrupt 3");
    all_zero &= check_zero(obj.EPWM9_TZ_INT, name + " " + "2.9 - ePWM9 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM10_TZ_INT, name + " " + "2.10 - ePWM10 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM11_TZ_INT, name + " " + "2.11 - ePWM11 Trip Zone Interrupt");
    all_zero &= check_zero(obj.EPWM12_TZ_INT, name + " " + "2.12 - ePWM12 Trip Zone Interrupt");
    all_zero &= check_zero(obj.PIE29_RESERVED_INT, name + " " + "2.13 - Reserved");
    all_zero &= check_zero(obj.PIE30_RESERVED_INT, name + " " + "2.14 - Reserved");
    all_zero &= check_zero(obj.PIE31_RESERVED_INT, name + " " + "2.15 - Reserved");
    all_zero &= check_zero(obj.PIE32_RESERVED_INT, name + " " + "2.16 - Reserved");
    all_zero &= check_zero(obj.EPWM9_INT, name + " " + "3.9 - ePWM9 Interrupt");
    all_zero &= check_zero(obj.EPWM10_INT, name + " " + "3.10 - ePWM10 Interrupt");
    all_zero &= check_zero(obj.EPWM11_INT, name + " " + "3.11 - ePWM11 Interrupt");
    all_zero &= check_zero(obj.EPWM12_INT, name + " " + "3.12 - ePWM12 Interrupt");
    all_zero &= check_zero(obj.PIE33_RESERVED_INT, name + " " + "3.13 - Reserved");
    all_zero &= check_zero(obj.PIE34_RESERVED_INT, name + " " + "3.14 - Reserved");
    all_zero &= check_zero(obj.PIE35_RESERVED_INT, name + " " + "3.15 - Reserved");
    all_zero &= check_zero(obj.PIE36_RESERVED_INT, name + " " + "3.16 - Reserved");
    all_zero &= check_zero(obj.PIE37_RESERVED_INT, name + " " + "4.9 - Reserved");
    all_zero &= check_zero(obj.PIE38_RESERVED_INT, name + " " + "4.10 - Reserved");
    all_zero &= check_zero(obj.PIE39_RESERVED_INT, name + " " + "4.11 - Reserved");
    all_zero &= check_zero(obj.PIE40_RESERVED_INT, name + " " + "4.12 - Reserved");
    all_zero &= check_zero(obj.PIE41_RESERVED_INT, name + " " + "4.13 - Reserved");
    all_zero &= check_zero(obj.PIE42_RESERVED_INT, name + " " + "4.14 - Reserved");
    all_zero &= check_zero(obj.PIE43_RESERVED_INT, name + " " + "4.15 - Reserved");
    all_zero &= check_zero(obj.PIE44_RESERVED_INT, name + " " + "4.16 - Reserved");
    all_zero &= check_zero(obj.SD1_INT, name + " " + "5.9 - SD1 Interrupt");
    all_zero &= check_zero(obj.SD2_INT, name + " " + "5.10 - SD2 Interrupt");
    all_zero &= check_zero(obj.PIE45_RESERVED_INT, name + " " + "5.11 - Reserved");
    all_zero &= check_zero(obj.PIE46_RESERVED_INT, name + " " + "5.12 - Reserved");
    all_zero &= check_zero(obj.PIE47_RESERVED_INT, name + " " + "5.13 - Reserved");
    all_zero &= check_zero(obj.PIE48_RESERVED_INT, name + " " + "5.14 - Reserved");
    all_zero &= check_zero(obj.PIE49_RESERVED_INT, name + " " + "5.15 - Reserved");
    all_zero &= check_zero(obj.PIE50_RESERVED_INT, name + " " + "5.16 - Reserved");
    all_zero &= check_zero(obj.SPIC_RX_INT, name + " " + "6.9 - SPIC Receive Interrupt");
    all_zero &= check_zero(obj.SPIC_TX_INT, name + " " + "6.10 - SPIC Transmit Interrupt");
    all_zero &= check_zero(obj.PIE51_RESERVED_INT, name + " " + "6.11 - Reserved");
    all_zero &= check_zero(obj.PIE52_RESERVED_INT, name + " " + "6.12 - Reserved");
    all_zero &= check_zero(obj.PIE53_RESERVED_INT, name + " " + "6.13 - Reserved");
    all_zero &= check_zero(obj.PIE54_RESERVED_INT, name + " " + "6.14 - Reserved");
    all_zero &= check_zero(obj.PIE55_RESERVED_INT, name + " " + "6.15 - Reserved");
    all_zero &= check_zero(obj.PIE56_RESERVED_INT, name + " " + "6.16 - Reserved");
    all_zero &= check_zero(obj.PIE57_RESERVED_INT, name + " " + "7.9 - Reserved");
    all_zero &= check_zero(obj.PIE58_RESERVED_INT, name + " " + "7.10 - Reserved");
    all_zero &= check_zero(obj.PIE59_RESERVED_INT, name + " " + "7.11 - Reserved");
    all_zero &= check_zero(obj.PIE60_RESERVED_INT, name + " " + "7.12 - Reserved");
    all_zero &= check_zero(obj.PIE61_RESERVED_INT, name + " " + "7.13 - Reserved");
    all_zero &= check_zero(obj.PIE62_RESERVED_INT, name + " " + "7.14 - Reserved");
    all_zero &= check_zero(obj.PIE63_RESERVED_INT, name + " " + "7.15 - Reserved");
    all_zero &= check_zero(obj.PIE64_RESERVED_INT, name + " " + "7.16 - Reserved");
    all_zero &= check_zero(obj.PIE65_RESERVED_INT, name + " " + "8.9 - Reserved");
    all_zero &= check_zero(obj.PIE66_RESERVED_INT, name + " " + "8.10 - Reserved");
    all_zero &= check_zero(obj.PIE67_RESERVED_INT, name + " " + "8.11 - Reserved");
    all_zero &= check_zero(obj.PIE68_RESERVED_INT, name + " " + "8.12 - Reserved");
    all_zero &= check_zero(obj.PIE69_RESERVED_INT, name + " " + "8.13 - Reserved");
    all_zero &= check_zero(obj.PIE70_RESERVED_INT, name + " " + "8.14 - Reserved");
    // all_zero &= check_zero(obj.UPPA_INT, name + " " + "8.15 - uPPA Interrupt");
    // all_zero &= check_zero(obj.PIE72_RESERVED_INT, name + " " + "8.16 - Reserved");
    // all_zero &= check_zero(obj.PIE71_RESERVED_INT, name + " " + "8.15 - Reserved");
    // all_zero &= check_zero(obj.PIE72_RESERVED_INT, name + " " + "8.16 - Reserved");
    all_zero &= check_zero(obj.PIE73_RESERVED_INT, name + " " + "9.9 - Reserved");
    all_zero &= check_zero(obj.PIE74_RESERVED_INT, name + " " + "9.10 - Reserved");
    all_zero &= check_zero(obj.PIE75_RESERVED_INT, name + " " + "9.11 - Reserved");
    all_zero &= check_zero(obj.PIE76_RESERVED_INT, name + " " + "9.12 - Reserved");
    all_zero &= check_zero(obj.PIE77_RESERVED_INT, name + " " + "9.13 - Reserved");
    all_zero &= check_zero(obj.PIE78_RESERVED_INT, name + " " + "9.14 - Reserved");
    // all_zero &= check_zero(obj.USBA_INT, name + " " + "9.15 - USBA Interrupt");
    // all_zero &= check_zero(obj.PIE79_RESERVED_INT, name + " " + "9.15 - Reserved");
    all_zero &= check_zero(obj.PIE80_RESERVED_INT, name + " " + "9.16 - Reserved");
    all_zero &= check_zero(obj.ADCC_EVT_INT, name + " " + "10.9 - ADCC Event Interrupt");
    all_zero &= check_zero(obj.ADCC2_INT, name + " " + "10.10 - ADCC Interrupt 2");
    all_zero &= check_zero(obj.ADCC3_INT, name + " " + "10.11 - ADCC Interrupt 3");
    all_zero &= check_zero(obj.ADCC4_INT, name + " " + "10.12 - ADCC Interrupt 4");
    all_zero &= check_zero(obj.ADCD_EVT_INT, name + " " + "10.13 - ADCD Event Interrupt");
    all_zero &= check_zero(obj.ADCD2_INT, name + " " + "10.14 - ADCD Interrupt 2");
    all_zero &= check_zero(obj.ADCD3_INT, name + " " + "10.15 - ADCD Interrupt 3");
    all_zero &= check_zero(obj.ADCD4_INT, name + " " + "10.16 - ADCD Interrupt 4");
    all_zero &= check_zero(obj.PIE81_RESERVED_INT, name + " " + "11.9 - Reserved");
    all_zero &= check_zero(obj.PIE82_RESERVED_INT, name + " " + "11.10 - Reserved");
    all_zero &= check_zero(obj.PIE83_RESERVED_INT, name + " " + "11.11 - Reserved");
    all_zero &= check_zero(obj.PIE84_RESERVED_INT, name + " " + "11.12 - Reserved");
    all_zero &= check_zero(obj.PIE85_RESERVED_INT, name + " " + "11.13 - Reserved");
    all_zero &= check_zero(obj.PIE86_RESERVED_INT, name + " " + "11.14 - Reserved");
    all_zero &= check_zero(obj.PIE87_RESERVED_INT, name + " " + "11.15 - Reserved");
    all_zero &= check_zero(obj.PIE88_RESERVED_INT, name + " " + "11.16 - Reserved");
    all_zero &= check_zero(obj.EMIF_ERROR_INT, name + " " + "12.9 - EMIF Error Interrupt");
    all_zero &= check_zero(obj.RAM_CORRECTABLE_ERROR_INT, name + " " + "12.10 - RAM Correctable Error Interrupt");
    all_zero &= check_zero(obj.FLASH_CORRECTABLE_ERROR_INT, name + " " + "12.11 - Flash Correctable Error Interrupt");
    all_zero &= check_zero(obj.RAM_ACCESS_VIOLATION_INT, name + " " + "12.12 - RAM Access Violation Interrupt");
    all_zero &= check_zero(obj.SYS_PLL_SLIP_INT, name + " " + "12.13 - System PLL Slip Interrupt");
    all_zero &= check_zero(obj.AUX_PLL_SLIP_INT, name + " " + "12.14 - Auxiliary PLL Slip Interrupt");
    all_zero &= check_zero(obj.CLA_OVERFLOW_INT, name + " " + "12.15 - CLA Overflow Interrupt");
    all_zero &= check_zero(obj.CLA_UNDERFLOW_INT, name + " " + "12.16 - CLA Underflow Interrupt");

    return all_zero;
}

bool check_zero(const Z1_LINKPOINTER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LINKPOINTER, name + " " + "Zone1 LINK Pointer.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_LINKPOINTER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_OTPSECLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PSWDLOCK, name + " " + "Zone1 Password Lock.");
    all_zero &= check_zero(obj.CRCLOCK, name + " " + "Zone1 CRC Lock.");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_OTPSECLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_BOOTCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.KEY, name + " " + "OTP Boot Key");
    all_zero &= check_zero(obj.BMODE, name + " " + "OTP Boot Mode");
    all_zero &= check_zero(obj.BOOTPIN0, name + " " + "OTP Boot Pin 0 Mapping");
    all_zero &= check_zero(obj.BOOTPIN1, name + " " + "OTP Boot Pin 1 Mapping");

    return all_zero;
}

bool check_zero(const Z1_BOOTCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ALLZERO, name + " " + "CSMPSWD All Zeros");
    all_zero &= check_zero(obj.ALLONE, name + " " + "CSMPSWD All Ones");
    all_zero &= check_zero(obj.UNSECURE, name + " " + "CSMPSWD Match CSMKEY");
    all_zero &= check_zero(obj.ARMED, name + " " + "CSM Armed");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FORCESEC, name + " " + "Force Secure");

    return all_zero;
}

bool check_zero(const Z1_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_GRABSECTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GRAB_SECTA, name + " " + "Grab Flash Sector A");
    all_zero &= check_zero(obj.GRAB_SECTB, name + " " + "Grab Flash Sector B");
    all_zero &= check_zero(obj.GRAB_SECTC, name + " " + "Grab Flash Sector C");
    all_zero &= check_zero(obj.GRAB_SECTD, name + " " + "Grab Flash Sector D");
    all_zero &= check_zero(obj.GRAB_SECTE, name + " " + "Grab Flash Sector E");
    all_zero &= check_zero(obj.GRAB_SECTF, name + " " + "Grab Flash Sector F");
    all_zero &= check_zero(obj.GRAB_SECTG, name + " " + "Grab Flash Sector G");
    all_zero &= check_zero(obj.GRAB_SECTH, name + " " + "Grab Flash Sector H");
    all_zero &= check_zero(obj.GRAB_SECTI, name + " " + "Grab Flash Sector I");
    all_zero &= check_zero(obj.GRAB_SECTJ, name + " " + "Grab Flash Sector J");
    all_zero &= check_zero(obj.GRAB_SECTK, name + " " + "Grab Flash Sector K");
    all_zero &= check_zero(obj.GRAB_SECTL, name + " " + "Grab Flash Sector L");
    all_zero &= check_zero(obj.GRAB_SECTM, name + " " + "Grab Flash Sector M");
    all_zero &= check_zero(obj.GRAB_SECTN, name + " " + "Grab Flash Sector N");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_GRABSECTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_GRABRAMR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GRAB_RAM0, name + " " + "Grab RAM LS0");
    all_zero &= check_zero(obj.GRAB_RAM1, name + " " + "Grab RAM LS1");
    all_zero &= check_zero(obj.GRAB_RAM2, name + " " + "Grab RAM LS2");
    all_zero &= check_zero(obj.GRAB_RAM3, name + " " + "Grab RAM LS3");
    all_zero &= check_zero(obj.GRAB_RAM4, name + " " + "Grab RAM LS4");
    all_zero &= check_zero(obj.GRAB_RAM5, name + " " + "Grab RAM LS5");
    all_zero &= check_zero(obj.GRAB_RAM6, name + " " + "Grab RAM D0");
    all_zero &= check_zero(obj.GRAB_RAM7, name + " " + "Grab RAM D1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GRAB_CLA1, name + " " + "Grab CLA1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_GRABRAMR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_EXEONLYSECTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EXEONLY_SECTA, name + " " + "Execute-Only Flash Sector A");
    all_zero &= check_zero(obj.EXEONLY_SECTB, name + " " + "Execute-Only Flash Sector B");
    all_zero &= check_zero(obj.EXEONLY_SECTC, name + " " + "Execute-Only Flash Sector C");
    all_zero &= check_zero(obj.EXEONLY_SECTD, name + " " + "Execute-Only Flash Sector D");
    all_zero &= check_zero(obj.EXEONLY_SECTE, name + " " + "Execute-Only Flash Sector E");
    all_zero &= check_zero(obj.EXEONLY_SECTF, name + " " + "Execute-Only Flash Sector F");
    all_zero &= check_zero(obj.EXEONLY_SECTG, name + " " + "Execute-Only Flash Sector G");
    all_zero &= check_zero(obj.EXEONLY_SECTH, name + " " + "Execute-Only Flash Sector H");
    all_zero &= check_zero(obj.EXEONLY_SECTI, name + " " + "Execute-Only Flash Sector I");
    all_zero &= check_zero(obj.EXEONLY_SECTJ, name + " " + "Execute-Only Flash Sector J");
    all_zero &= check_zero(obj.EXEONLY_SECTK, name + " " + "Execute-Only Flash Sector K");
    all_zero &= check_zero(obj.EXEONLY_SECTL, name + " " + "Execute-Only Flash Sector L");
    all_zero &= check_zero(obj.EXEONLY_SECTM, name + " " + "Execute-Only Flash Sector M");
    all_zero &= check_zero(obj.EXEONLY_SECTN, name + " " + "Execute-Only Flash Sector N");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_EXEONLYSECTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z1_EXEONLYRAMR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EXEONLY_RAM0, name + " " + "Execute-Only RAM LS0");
    all_zero &= check_zero(obj.EXEONLY_RAM1, name + " " + "Execute-Only RAM LS1");
    all_zero &= check_zero(obj.EXEONLY_RAM2, name + " " + "Execute-Only RAM LS2");
    all_zero &= check_zero(obj.EXEONLY_RAM3, name + " " + "Execute-Only RAM LS3");
    all_zero &= check_zero(obj.EXEONLY_RAM4, name + " " + "Execute-Only RAM LS4");
    all_zero &= check_zero(obj.EXEONLY_RAM5, name + " " + "Execute-Only RAM LS5");
    all_zero &= check_zero(obj.EXEONLY_RAM6, name + " " + "Execute-Only RAM D0");
    all_zero &= check_zero(obj.EXEONLY_RAM7, name + " " + "Execute-Only RAM D1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z1_EXEONLYRAMR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCSM_Z1_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.Z1_LINKPOINTER, name + " " + "Zone 1 Link Pointer");
    all_zero &= check_zero(obj.Z1_OTPSECLOCK, name + " " + "Zone 1 OTP Secure JTAG lock");
    all_zero &= check_zero(obj.Z1_BOOTCTRL, name + " " + "Boot Mode");
    all_zero &= check_zero(obj.Z1_LINKPOINTERERR, name + " " + "Link Pointer Error");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.Z1_CSMKEY0, name + " " + "Zone 1 CSM Key 0");
    all_zero &= check_zero(obj.Z1_CSMKEY1, name + " " + "Zone 1 CSM Key 1");
    all_zero &= check_zero(obj.Z1_CSMKEY2, name + " " + "Zone 1 CSM Key 2");
    all_zero &= check_zero(obj.Z1_CSMKEY3, name + " " + "Zone 1 CSM Key 3");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.Z1_CR, name + " " + "Zone 1 CSM Control Register");
    all_zero &= check_zero(obj.Z1_GRABSECTR, name + " " + "Zone 1 Grab Flash Sectors Register");
    all_zero &= check_zero(obj.Z1_GRABRAMR, name + " " + "Zone 1 Grab RAM Blocks Register");
    all_zero &= check_zero(obj.Z1_EXEONLYSECTR, name + " " + "Zone 1 Flash Execute_Only Sector Register");
    all_zero &= check_zero(obj.Z1_EXEONLYRAMR, name + " " + "Zone 1 RAM Execute_Only Block Register");

    return all_zero;
}

bool check_zero(const Z2_LINKPOINTER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LINKPOINTER, name + " " + "Zone2 LINK Pointer.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_LINKPOINTER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_OTPSECLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PSWDLOCK, name + " " + "Zone2 Password Lock.");
    all_zero &= check_zero(obj.CRCLOCK, name + " " + "Zone2 CRC Lock.");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_OTPSECLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_BOOTCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.KEY, name + " " + "OTP Boot Key");
    all_zero &= check_zero(obj.BMODE, name + " " + "OTP Boot Mode");
    all_zero &= check_zero(obj.BOOTPIN0, name + " " + "OTP Boot Pin 0 Mapping");
    all_zero &= check_zero(obj.BOOTPIN1, name + " " + "OTP Boot Pin 1 Mapping");

    return all_zero;
}

bool check_zero(const Z2_BOOTCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ALLZERO, name + " " + "CSMPSWD All Zeros");
    all_zero &= check_zero(obj.ALLONE, name + " " + "CSMPSWD All Ones");
    all_zero &= check_zero(obj.UNSECURE, name + " " + "CSMPSWD Match CSMKEY");
    all_zero &= check_zero(obj.ARMED, name + " " + "CSM Armed");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.FORCESEC, name + " " + "Force Secure");

    return all_zero;
}

bool check_zero(const Z2_CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_GRABSECTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GRAB_SECTA, name + " " + "Grab Flash Sector A");
    all_zero &= check_zero(obj.GRAB_SECTB, name + " " + "Grab Flash Sector B");
    all_zero &= check_zero(obj.GRAB_SECTC, name + " " + "Grab Flash Sector C");
    all_zero &= check_zero(obj.GRAB_SECTD, name + " " + "Grab Flash Sector D");
    all_zero &= check_zero(obj.GRAB_SECTE, name + " " + "Grab Flash Sector E");
    all_zero &= check_zero(obj.GRAB_SECTF, name + " " + "Grab Flash Sector F");
    all_zero &= check_zero(obj.GRAB_SECTG, name + " " + "Grab Flash Sector G");
    all_zero &= check_zero(obj.GRAB_SECTH, name + " " + "Grab Flash Sector H");
    all_zero &= check_zero(obj.GRAB_SECTI, name + " " + "Grab Flash Sector I");
    all_zero &= check_zero(obj.GRAB_SECTJ, name + " " + "Grab Flash Sector J");
    all_zero &= check_zero(obj.GRAB_SECTK, name + " " + "Grab Flash Sector K");
    all_zero &= check_zero(obj.GRAB_SECTL, name + " " + "Grab Flash Sector L");
    all_zero &= check_zero(obj.GRAB_SECTM, name + " " + "Grab Flash Sector M");
    all_zero &= check_zero(obj.GRAB_SECTN, name + " " + "Grab Flash Sector N");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_GRABSECTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_GRABRAMR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GRAB_RAM0, name + " " + "Grab RAM LS0");
    all_zero &= check_zero(obj.GRAB_RAM1, name + " " + "Grab RAM LS1");
    all_zero &= check_zero(obj.GRAB_RAM2, name + " " + "Grab RAM LS2");
    all_zero &= check_zero(obj.GRAB_RAM3, name + " " + "Grab RAM LS3");
    all_zero &= check_zero(obj.GRAB_RAM4, name + " " + "Grab RAM LS4");
    all_zero &= check_zero(obj.GRAB_RAM5, name + " " + "Grab RAM LS5");
    all_zero &= check_zero(obj.GRAB_RAM6, name + " " + "Grab RAM D0");
    all_zero &= check_zero(obj.GRAB_RAM7, name + " " + "Grab RAM D1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GRAB_CLA1, name + " " + "Grab CLA1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_GRABRAMR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_EXEONLYSECTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EXEONLY_SECTA, name + " " + "Execute-Only Flash Sector A");
    all_zero &= check_zero(obj.EXEONLY_SECTB, name + " " + "Execute-Only Flash Sector B");
    all_zero &= check_zero(obj.EXEONLY_SECTC, name + " " + "Execute-Only Flash Sector C");
    all_zero &= check_zero(obj.EXEONLY_SECTD, name + " " + "Execute-Only Flash Sector D");
    all_zero &= check_zero(obj.EXEONLY_SECTE, name + " " + "Execute-Only Flash Sector E");
    all_zero &= check_zero(obj.EXEONLY_SECTF, name + " " + "Execute-Only Flash Sector F");
    all_zero &= check_zero(obj.EXEONLY_SECTG, name + " " + "Execute-Only Flash Sector G");
    all_zero &= check_zero(obj.EXEONLY_SECTH, name + " " + "Execute-Only Flash Sector H");
    all_zero &= check_zero(obj.EXEONLY_SECTI, name + " " + "Execute-Only Flash Sector I");
    all_zero &= check_zero(obj.EXEONLY_SECTJ, name + " " + "Execute-Only Flash Sector J");
    all_zero &= check_zero(obj.EXEONLY_SECTK, name + " " + "Execute-Only Flash Sector K");
    all_zero &= check_zero(obj.EXEONLY_SECTL, name + " " + "Execute-Only Flash Sector L");
    all_zero &= check_zero(obj.EXEONLY_SECTM, name + " " + "Execute-Only Flash Sector M");
    all_zero &= check_zero(obj.EXEONLY_SECTN, name + " " + "Execute-Only Flash Sector N");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_EXEONLYSECTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const Z2_EXEONLYRAMR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EXEONLY_RAM0, name + " " + "Execute-Only RAM LS0");
    all_zero &= check_zero(obj.EXEONLY_RAM1, name + " " + "Execute-Only RAM LS1");
    all_zero &= check_zero(obj.EXEONLY_RAM2, name + " " + "Execute-Only RAM LS2");
    all_zero &= check_zero(obj.EXEONLY_RAM3, name + " " + "Execute-Only RAM LS3");
    all_zero &= check_zero(obj.EXEONLY_RAM4, name + " " + "Execute-Only RAM LS4");
    all_zero &= check_zero(obj.EXEONLY_RAM5, name + " " + "Execute-Only RAM LS5");
    all_zero &= check_zero(obj.EXEONLY_RAM6, name + " " + "Execute-Only RAM D0");
    all_zero &= check_zero(obj.EXEONLY_RAM7, name + " " + "Execute-Only RAM D1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const Z2_EXEONLYRAMR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCSM_Z2_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.Z2_LINKPOINTER, name + " " + "Zone 2 Link Pointer");
    all_zero &= check_zero(obj.Z2_OTPSECLOCK, name + " " + "Zone 2 OTP Secure JTAG lock");
    all_zero &= check_zero(obj.Z2_BOOTCTRL, name + " " + "Boot Mode");
    all_zero &= check_zero(obj.Z2_LINKPOINTERERR, name + " " + "Link Pointer Error");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.Z2_CSMKEY0, name + " " + "Zone 2 CSM Key 0");
    all_zero &= check_zero(obj.Z2_CSMKEY1, name + " " + "Zone 2 CSM Key 1");
    all_zero &= check_zero(obj.Z2_CSMKEY2, name + " " + "Zone 2 CSM Key 2");
    all_zero &= check_zero(obj.Z2_CSMKEY3, name + " " + "Zone 2 CSM Key 3");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.Z2_CR, name + " " + "Zone 2 CSM Control Register");
    all_zero &= check_zero(obj.Z2_GRABSECTR, name + " " + "Zone 2 Grab Flash Sectors Register");
    all_zero &= check_zero(obj.Z2_GRABRAMR, name + " " + "Zone 2 Grab RAM Blocks Register");
    all_zero &= check_zero(obj.Z2_EXEONLYSECTR, name + " " + "Zone 2 Flash Execute_Only Sector Register");
    all_zero &= check_zero(obj.Z2_EXEONLYRAMR, name + " " + "Zone 2 RAM Execute_Only Block Register");

    return all_zero;
}

bool check_zero(const FLSEM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEM, name + " " + "Flash Semaphore Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Semaphore Key");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FLSEM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SECTSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.STATUS_SECTA, name + " " + "Zone Status Flash Sector A");
    all_zero &= check_zero(obj.STATUS_SECTB, name + " " + "Zone Status Flash Sector B");
    all_zero &= check_zero(obj.STATUS_SECTC, name + " " + "Zone Status Flash Sector C");
    all_zero &= check_zero(obj.STATUS_SECTD, name + " " + "Zone Status Flash Sector D");
    all_zero &= check_zero(obj.STATUS_SECTE, name + " " + "Zone Status Flash Sector E");
    all_zero &= check_zero(obj.STATUS_SECTF, name + " " + "Zone Status Flash Sector F");
    all_zero &= check_zero(obj.STATUS_SECTG, name + " " + "Zone Status Flash Sector G");
    all_zero &= check_zero(obj.STATUS_SECTH, name + " " + "Zone Status Flash Sector H");
    all_zero &= check_zero(obj.STATUS_SECTI, name + " " + "Zone Status Flash Sector I");
    all_zero &= check_zero(obj.STATUS_SECTJ, name + " " + "Zone Status Flash Sector J");
    all_zero &= check_zero(obj.STATUS_SECTK, name + " " + "Zone Status Flash Sector K");
    all_zero &= check_zero(obj.STATUS_SECTL, name + " " + "Zone Status Flash Sector L");
    all_zero &= check_zero(obj.STATUS_SECTM, name + " " + "Zone Status Flash Sector M");
    all_zero &= check_zero(obj.STATUS_SECTN, name + " " + "Zone Status Flash Sector N");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SECTSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RAMSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.STATUS_RAM0, name + " " + "Zone Status RAM LS0");
    all_zero &= check_zero(obj.STATUS_RAM1, name + " " + "Zone Status RAM LS1");
    all_zero &= check_zero(obj.STATUS_RAM2, name + " " + "Zone Status RAM LS2");
    all_zero &= check_zero(obj.STATUS_RAM3, name + " " + "Zone Status RAM LS3");
    all_zero &= check_zero(obj.STATUS_RAM4, name + " " + "Zone Status RAM LS4");
    all_zero &= check_zero(obj.STATUS_RAM5, name + " " + "Zone Status RAM LS5");
    all_zero &= check_zero(obj.STATUS_RAM6, name + " " + "Zone Status RAM D0");
    all_zero &= check_zero(obj.STATUS_RAM7, name + " " + "Zone Status RAM D1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.STATUS_CLA1, name + " " + "Zone Status CLA1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const RAMSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCSM_COMMON_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FLSEM, name + " " + "Flash Wrapper Semaphore Register");
    all_zero &= check_zero(obj.SECTSTAT, name + " " + "Sectors Status Register");
    all_zero &= check_zero(obj.RAMSTAT, name + " " + "RAM Status Register");

    return all_zero;
}

bool check_zero(const MODE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PERINTSEL, name + " " + "Peripheral Interrupt and Sync Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.OVRINTE, name + " " + "Overflow Interrupt Enable");
    all_zero &= check_zero(obj.PERINTE, name + " " + "Peripheral Interrupt Enable");
    all_zero &= check_zero(obj.CHINTMODE, name + " " + "Channel Interrupt Mode");
    all_zero &= check_zero(obj.ONESHOT, name + " " + "One Shot Mode Bit");
    all_zero &= check_zero(obj.CONTINUOUS, name + " " + "Continuous Mode Bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.DATASIZE, name + " " + "Data Size Mode Bit");
    all_zero &= check_zero(obj.CHINTE, name + " " + "Channel Interrupt Enable Bit");

    return all_zero;
}

bool check_zero(const MODE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CONTROL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RUN, name + " " + "Run Bit");
    all_zero &= check_zero(obj.HALT, name + " " + "Halt Bit");
    all_zero &= check_zero(obj.SOFTRESET, name + " " + "Soft Reset Bit");
    all_zero &= check_zero(obj.PERINTFRC, name + " " + "Interrupt Force Bit");
    all_zero &= check_zero(obj.PERINTCLR, name + " " + "Interrupt Clear Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.ERRCLR, name + " " + "Error Clear Bit");
    all_zero &= check_zero(obj.PERINTFLG, name + " " + "Interrupt Flag Bit");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRANSFERSTS, name + " " + "Transfer Status Bit");
    all_zero &= check_zero(obj.BURSTSTS, name + " " + "Burst Status Bit");
    all_zero &= check_zero(obj.RUNSTS, name + " " + "Run Status Bit");
    all_zero &= check_zero(obj.OVRFLG, name + " " + "Overflow Flag Bit");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CONTROL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const BURST_SIZE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BURSTSIZE, name + " " + "Burst Transfer Size");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const BURST_SIZE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const BURST_COUNT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BURSTCOUNT, name + " " + "Burst Transfer Size");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const BURST_COUNT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CH_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MODE, name + " " + "Mode Register");
    all_zero &= check_zero(obj.CONTROL, name + " " + "Control Register");
    all_zero &= check_zero(obj.BURST_SIZE, name + " " + "Burst Size Register");
    all_zero &= check_zero(obj.BURST_COUNT, name + " " + "Burst Count Register");
    all_zero &= check_zero(obj.SRC_BURST_STEP, name + " " + "Source Burst Step Register");
    all_zero &= check_zero(obj.DST_BURST_STEP, name + " " + "Destination Burst Step Register");
    all_zero &= check_zero(obj.TRANSFER_SIZE, name + " " + "Transfer Size Register");
    all_zero &= check_zero(obj.TRANSFER_COUNT, name + " " + "Transfer Count Register");
    all_zero &= check_zero(obj.SRC_TRANSFER_STEP, name + " " + "Source Transfer Step Register");
    all_zero &= check_zero(obj.DST_TRANSFER_STEP, name + " " + "Destination Transfer Step Register");
    all_zero &= check_zero(obj.SRC_WRAP_SIZE, name + " " + "Source Wrap Size Register");
    all_zero &= check_zero(obj.SRC_WRAP_COUNT, name + " " + "Source Wrap Count Register");
    all_zero &= check_zero(obj.SRC_WRAP_STEP, name + " " + "Source Wrap Step Register");
    all_zero &= check_zero(obj.DST_WRAP_SIZE, name + " " + "Destination Wrap Size Register");
    all_zero &= check_zero(obj.DST_WRAP_COUNT, name + " " + "Destination Wrap Count Register");
    all_zero &= check_zero(obj.DST_WRAP_STEP, name + " " + "Destination Wrap Step Register");
    all_zero &= check_zero(obj.SRC_BEG_ADDR_SHADOW, name + " " + "Source Begin Address Shadow Register");
    all_zero &= check_zero(obj.SRC_ADDR_SHADOW, name + " " + "Source Address Shadow Register");
    all_zero &= check_zero(obj.SRC_BEG_ADDR_ACTIVE, name + " " + "Source Begin Address Active Register");
    all_zero &= check_zero(obj.SRC_ADDR_ACTIVE, name + " " + "Source Address Active Register");
    all_zero &= check_zero(obj.DST_BEG_ADDR_SHADOW, name + " " + "Destination Begin Address Shadow Register");
    all_zero &= check_zero(obj.DST_ADDR_SHADOW, name + " " + "Destination Address Shadow Register");
    all_zero &= check_zero(obj.DST_BEG_ADDR_ACTIVE, name + " " + "Destination Begin Address Active Register");
    all_zero &= check_zero(obj.DST_ADDR_ACTIVE, name + " " + "Destination Address Active Register");

    return all_zero;
}

bool check_zero(const DMACTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HARDRESET, name + " " + "Hard Reset Bit");
    all_zero &= check_zero(obj.PRIORITYRESET, name + " " + "Priority Reset Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DMACTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DEBUGCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FREE, name + " " + "Debug Mode Bit");

    return all_zero;
}

bool check_zero(const DEBUGCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PRIORITYCTRL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CH1PRIORITY, name + " " + "Ch1 Priority Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PRIORITYCTRL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PRIORITYSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACTIVESTS, name + " " + "Active Channel Status Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ACTIVESTS_SHADOW, name + " " + "Active Channel Status Shadow Bits");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PRIORITYSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DMA_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DMACTRL, name + " " + "DMA Control Register");
    all_zero &= check_zero(obj.DEBUGCTRL, name + " " + "Debug Control Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PRIORITYCTRL1, name + " " + "Priority Control 1 Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PRIORITYSTAT, name + " " + "Priority Status Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.CH1, name + " " + "DMA Channel 1 Registers");
    all_zero &= check_zero(obj.CH2, name + " " + "DMA Channel 2 Registers");
    all_zero &= check_zero(obj.CH3, name + " " + "DMA Channel 3 Registers");
    all_zero &= check_zero(obj.CH4, name + " " + "DMA Channel 4 Registers");
    all_zero &= check_zero(obj.CH5, name + " " + "DMA Channel 5 Registers");
    all_zero &= check_zero(obj.CH6, name + " " + "DMA Channel 6 Registers");

    return all_zero;
}

bool check_zero(const SDIFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IFH1, name + " " + "High-level Interrupt flag Filter 1");
    all_zero &= check_zero(obj.IFL1, name + " " + "Low-Level Interrupt flag Filter 1");
    all_zero &= check_zero(obj.IFH2, name + " " + "High-level Interrupt flag Filter 2");
    all_zero &= check_zero(obj.IFL2, name + " " + "Low-Level Interrupt flag Filter 2");
    all_zero &= check_zero(obj.IFH3, name + " " + "High-level Interrupt flag Filter 3");
    all_zero &= check_zero(obj.IFL3, name + " " + "Low-Level Interrupt flag Filter 3");
    all_zero &= check_zero(obj.IFH4, name + " " + "High-level Interrupt flag Filter 4");
    all_zero &= check_zero(obj.IFL4, name + " " + "Low-Level Interrupt flag Filter 4");
    all_zero &= check_zero(obj.MF1, name + " " + "Modulator Failure for Filter 1");
    all_zero &= check_zero(obj.MF2, name + " " + "Modulator Failure for Filter 2");
    all_zero &= check_zero(obj.MF3, name + " " + "Modulator Failure for Filter 3");
    all_zero &= check_zero(obj.MF4, name + " " + "Modulator Failure for Filter 4");
    all_zero &= check_zero(obj.AF1, name + " " + "Acknowledge flag for Filter 1");
    all_zero &= check_zero(obj.AF2, name + " " + "Acknowledge flag for Filter 2");
    all_zero &= check_zero(obj.AF3, name + " " + "Acknowledge flag for Filter 3");
    all_zero &= check_zero(obj.AF4, name + " " + "Acknowledge flag for Filter 4");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MIF, name + " " + "Master Interrupt Flag");

    return all_zero;
}

bool check_zero(const SDIFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDIFLGCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IFH1, name + " " + "High-level Interrupt flag Filter 1");
    all_zero &= check_zero(obj.IFL1, name + " " + "Low-Level Interrupt flag Filter 1");
    all_zero &= check_zero(obj.IFH2, name + " " + "High-level Interrupt flag Filter 2");
    all_zero &= check_zero(obj.IFL2, name + " " + "Low-Level Interrupt flag Filter 2");
    all_zero &= check_zero(obj.IFH3, name + " " + "High-level Interrupt flag Filter 3");
    all_zero &= check_zero(obj.IFL3, name + " " + "Low-Level Interrupt flag Filter 3");
    all_zero &= check_zero(obj.IFH4, name + " " + "High-level Interrupt flag Filter 4");
    all_zero &= check_zero(obj.IFL4, name + " " + "Low-Level Interrupt flag Filter 4");
    all_zero &= check_zero(obj.MF1, name + " " + "Modulator Failure for Filter 1");
    all_zero &= check_zero(obj.MF2, name + " " + "Modulator Failure for Filter 2");
    all_zero &= check_zero(obj.MF3, name + " " + "Modulator Failure for Filter 3");
    all_zero &= check_zero(obj.MF4, name + " " + "Modulator Failure for Filter 4");
    all_zero &= check_zero(obj.AF1, name + " " + "Acknowledge flag for Filter 1");
    all_zero &= check_zero(obj.AF2, name + " " + "Acknowledge flag for Filter 2");
    all_zero &= check_zero(obj.AF3, name + " " + "Acknowledge flag for Filter 3");
    all_zero &= check_zero(obj.AF4, name + " " + "Acknowledge flag for Filter 4");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MIF, name + " " + "Master Interrupt Flag");

    return all_zero;
}

bool check_zero(const SDIFLGCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.MIE, name + " " + "Master Interrupt enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDMFILEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.MFE, name + " " + "Master Filter Enable.");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDMFILEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCTLPARM1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MOD, name + " " + "Delta-Sigma Modulator mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCTLPARM1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDFPARM1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DOSR, name + " " + "Data Filter Oversample Ratio= DOSR+1");
    all_zero &= check_zero(obj.FEN, name + " " + "Filter Enable");
    all_zero &= check_zero(obj.AE, name + " " + "Ack Enable");
    all_zero &= check_zero(obj.SST, name + " " + "Data Filter Structure (DataFast/1/2/3)");
    all_zero &= check_zero(obj.SDSYNCEN, name + " " + "Data FILTER Reset Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDDFPARM1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDPARM1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.DR, name + " " + "Data Representation (0/1 = 16/32b 2's complement)");
    all_zero &= check_zero(obj.SH, name + " " + "Shift Control (# bits to shift in 16b mode)");

    return all_zero;
}

bool check_zero(const SDDPARM1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPH1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HLT, name + " " + "High-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPH1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LLT, name + " " + "Low-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCPARM1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COSR, name + " " + "Comparator Oversample Ratio = COSR + 1");
    all_zero &= check_zero(obj.IEH, name + " " + "High-level interrupt enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Low-level interrupt enable");
    all_zero &= check_zero(obj.CS1_CS0, name + " " + "Comparator filter structure (Sincfast/Sinc1/Sinc2/Sinc3");
    all_zero &= check_zero(obj.MFIE, name + " " + "Modulator Failure Interrupt enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCPARM1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDATA1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA16, name + " " + "16-bit Data in 16b mode, Lo-order 16b in 32b mode");
    all_zero &= check_zero(obj.DATA32HI, name + " " + "Hi-order 16b in 32b mode");

    return all_zero;
}

bool check_zero(const SDDATA1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCTLPARM2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MOD, name + " " + "Delta-Sigma Modulator mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCTLPARM2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDFPARM2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DOSR, name + " " + "Data Filter Oversample Ratio= DOSR+1");
    all_zero &= check_zero(obj.FEN, name + " " + "Filter Enable");
    all_zero &= check_zero(obj.AE, name + " " + "Ack Enable");
    all_zero &= check_zero(obj.SST, name + " " + "Data Filter Structure (SincFast/1/2/3)");
    all_zero &= check_zero(obj.SDSYNCEN, name + " " + "Data FILTER Reset Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDDFPARM2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDPARM2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.DR, name + " " + "Data Representation (0/1 = 16/32b 2's complement)");
    all_zero &= check_zero(obj.SH, name + " " + "Shift Control (# bits to shift in 16b mode)");

    return all_zero;
}

bool check_zero(const SDDPARM2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPH2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HLT, name + " " + "High-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPH2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LLT, name + " " + "Low-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCPARM2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COSR, name + " " + "Comparator Oversample Ratio = COSR + 1");
    all_zero &= check_zero(obj.IEH, name + " " + "High-level interrupt enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Low-level interrupt enable");
    all_zero &= check_zero(obj.CS1_CS0, name + " " + "Comparator filter structure (Sincfast/Sinc1/Sinc2/Sinc3");
    all_zero &= check_zero(obj.MFIE, name + " " + "Modulator Failure Interrupt enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCPARM2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDATA2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA16, name + " " + "16-bit Data in 16b mode, Lo-order 16b in 32b mode");
    all_zero &= check_zero(obj.DATA32HI, name + " " + "Hi-order 16b in 32b mode");

    return all_zero;
}

bool check_zero(const SDDATA2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCTLPARM3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MOD, name + " " + "Delta-Sigma Modulator mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCTLPARM3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDFPARM3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DOSR, name + " " + "Data Filter Oversample Ratio= DOSR+1");
    all_zero &= check_zero(obj.FEN, name + " " + "Filter Enable");
    all_zero &= check_zero(obj.AE, name + " " + "Ack Enable");
    all_zero &= check_zero(obj.SST, name + " " + "Data filter structure (SincFast/1/2/3)");
    all_zero &= check_zero(obj.SDSYNCEN, name + " " + "Data FILTER Reset Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDDFPARM3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDPARM3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.DR, name + " " + "Data Representation (0/1 = 16/32b 2's complement)");
    all_zero &= check_zero(obj.SH, name + " " + "Shift Control (# bits to shift in 16b mode)");

    return all_zero;
}

bool check_zero(const SDDPARM3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPH3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HLT, name + " " + "High-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPH3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LLT, name + " " + "Low-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCPARM3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COSR, name + " " + "Comparator Oversample Ratio = COSR + 1");
    all_zero &= check_zero(obj.IEH, name + " " + "High-level interrupt enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Low-level interrupt enable");
    all_zero &= check_zero(obj.CS1_CS0, name + " " + "Comparator filter structure (Sincfast/Sinc1/Sinc2/Sinc3");
    all_zero &= check_zero(obj.MFIE, name + " " + "Modulator Failure Interrupt enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCPARM3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDATA3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA16, name + " " + "16-bit Data in 16b mode, Lo-order 16b in 32b mode");
    all_zero &= check_zero(obj.DATA32HI, name + " " + "Hi-order 16b in 32b mode");

    return all_zero;
}

bool check_zero(const SDDATA3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCTLPARM4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MOD, name + " " + "Delta-Sigma Modulator mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCTLPARM4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDFPARM4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DOSR, name + " " + "SINC Filter Oversample Ratio= DOSR+1");
    all_zero &= check_zero(obj.FEN, name + " " + "Filter Enable");
    all_zero &= check_zero(obj.AE, name + " " + "Ack Enable");
    all_zero &= check_zero(obj.SST, name + " " + "Data filter structure (SincFast/1/2/3)");
    all_zero &= check_zero(obj.SDSYNCEN, name + " " + "SINC FILTER Reset Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDDFPARM4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDPARM4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.DR, name + " " + "Data Representation (0/1 = 16/32b 2's complement)");
    all_zero &= check_zero(obj.SH, name + " " + "Shift Control (# bits to shift in 16b mode)");

    return all_zero;
}

bool check_zero(const SDDPARM4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPH4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HLT, name + " " + "High-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPH4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCMPL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LLT, name + " " + "Low-level threshold for the comparator filter output.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCMPL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDCPARM4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COSR, name + " " + "Comparator Oversample Ratio = COSR + 1");
    all_zero &= check_zero(obj.IEH, name + " " + "High-level interrupt enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Low-level interrupt enable");
    all_zero &= check_zero(obj.CS1_CS0, name + " " + "Comparator filter structure (Sincfast/Sinc1/Sinc2/Sinc3");
    all_zero &= check_zero(obj.MFIE, name + " " + "Modulator Failure Interrupt enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SDCPARM4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDDATA4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA16, name + " " + "16-bit Data in 16b mode, Lo-order 16b in 32b mode");
    all_zero &= check_zero(obj.DATA32HI, name + " " + "Hi-order 16b in 32b mode");

    return all_zero;
}

bool check_zero(const SDDATA4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SDFM_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SDIFLG, name + " " + "Interrupt Flag Register");
    all_zero &= check_zero(obj.SDIFLGCLR, name + " " + "Interrupt Flag Clear Register");
    all_zero &= check_zero(obj.SDCTL, name + " " + "SD Control Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDMFILEN, name + " " + "SD Master Filter Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDCTLPARM1, name + " " + "Control Parameter Register for Ch1");
    all_zero &= check_zero(obj.SDDFPARM1, name + " " + "Data Filter Parameter Register for Ch1");
    all_zero &= check_zero(obj.SDDPARM1, name + " " + "Integer Parameter Register for Ch1");
    all_zero &= check_zero(obj.SDCMPH1, name + " " + "High-level Threshold Register for Ch1");
    all_zero &= check_zero(obj.SDCMPL1, name + " " + "Low-level Threshold Register for Ch1");
    all_zero &= check_zero(obj.SDCPARM1, name + " " + "Comparator Parameter Register for Ch1");
    all_zero &= check_zero(obj.SDDATA1, name + " " + "Filter Data Register (16 or 32bit) for Ch1");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDCTLPARM2, name + " " + "Control Parameter Register for Ch2");
    all_zero &= check_zero(obj.SDDFPARM2, name + " " + "Data Filter Parameter Register for Ch2");
    all_zero &= check_zero(obj.SDDPARM2, name + " " + "Integer Parameter Register for Ch2");
    all_zero &= check_zero(obj.SDCMPH2, name + " " + "High-level Threshold Register for Ch2");
    all_zero &= check_zero(obj.SDCMPL2, name + " " + "Low-level Threshold Register for Ch2");
    all_zero &= check_zero(obj.SDCPARM2, name + " " + "Comparator Parameter Register for Ch2");
    all_zero &= check_zero(obj.SDDATA2, name + " " + "Filter Data Register (16 or 32bit) for Ch2");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDCTLPARM3, name + " " + "Control Parameter Register for Ch3");
    all_zero &= check_zero(obj.SDDFPARM3, name + " " + "Data Filter Parameter Register for Ch3");
    all_zero &= check_zero(obj.SDDPARM3, name + " " + "Integer Parameter Register for Ch3");
    all_zero &= check_zero(obj.SDCMPH3, name + " " + "High-level Threshold Register for Ch3");
    all_zero &= check_zero(obj.SDCMPL3, name + " " + "Low-level Threshold Register for Ch3");
    all_zero &= check_zero(obj.SDCPARM3, name + " " + "Comparator Parameter Register for Ch3");
    all_zero &= check_zero(obj.SDDATA3, name + " " + "Filter Data Register (16 or 32bit) for Ch3");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.SDCTLPARM4, name + " " + "Control Parameter Register for Ch4");
    all_zero &= check_zero(obj.SDDFPARM4, name + " " + "Data Filter Parameter Register for Ch4");
    all_zero &= check_zero(obj.SDDPARM4, name + " " + "Integer Parameter Register for Ch4");
    all_zero &= check_zero(obj.SDCMPH4, name + " " + "High-level Threshold Register for Ch4");
    all_zero &= check_zero(obj.SDCMPL4, name + " " + "Low-level Threshold Register for Ch4");
    all_zero &= check_zero(obj.SDCPARM4, name + " " + "Comparator Parameter Register for Ch4");
    all_zero &= check_zero(obj.SDDATA4, name + " " + "Filter Data Register (16 or 32bit) for Ch4");

    return all_zero;
}

bool check_zero(const QDECCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.QSP, name + " " + "QEPS input polarity");
    all_zero &= check_zero(obj.QIP, name + " " + "QEPI input polarity");
    all_zero &= check_zero(obj.QBP, name + " " + "QEPB input polarity");
    all_zero &= check_zero(obj.QAP, name + " " + "QEPA input polarity");
    all_zero &= check_zero(obj.IGATE, name + " " + "Index pulse gating option");
    all_zero &= check_zero(obj.SWAP, name + " " + "CLK/DIR Signal Source for Position Counter");
    all_zero &= check_zero(obj.XCR, name + " " + "External Clock Rate");
    all_zero &= check_zero(obj.SPSEL, name + " " + "Sync output pin selection");
    all_zero &= check_zero(obj.SOEN, name + " " + "Sync output-enable");
    all_zero &= check_zero(obj.QSRC, name + " " + "Position-counter source selection");

    return all_zero;
}

bool check_zero(const QDECCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QEPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WDE, name + " " + "QEP watchdog enable");
    all_zero &= check_zero(obj.UTE, name + " " + "QEP unit timer enable");
    all_zero &= check_zero(obj.QCLM, name + " " + "QEP capture latch mode");
    all_zero &= check_zero(obj.QPEN, name + " " + "Quadrature postotion counter enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Index event latch");
    all_zero &= check_zero(obj.SEL, name + " " + "Strobe event latch");
    all_zero &= check_zero(obj.SWI, name + " " + "Software init position counter");
    all_zero &= check_zero(obj.IEI, name + " " + "Index event init of position count");
    all_zero &= check_zero(obj.SEI, name + " " + "Strobe event init");
    all_zero &= check_zero(obj.PCRM, name + " " + "Postion counter reset");
    all_zero &= check_zero(obj.FREE_SOFT, name + " " + "Emulation mode");

    return all_zero;
}

bool check_zero(const QEPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QCAPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.UPPS, name + " " + "Unit position event prescaler");
    all_zero &= check_zero(obj.CCPS, name + " " + "eQEP capture timer clock prescaler");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CEN, name + " " + "Enable eQEP capture");

    return all_zero;
}

bool check_zero(const QCAPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QPOSCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PCSPW, name + " " + "Position compare sync pulse width");
    all_zero &= check_zero(obj.PCE, name + " " + "Position compare enable/disable");
    all_zero &= check_zero(obj.PCPOL, name + " " + "Polarity of sync output");
    all_zero &= check_zero(obj.PCLOAD, name + " " + "Position compare of shadow load");
    all_zero &= check_zero(obj.PCSHDW, name + " " + "Position compare of shadow enable");

    return all_zero;
}

bool check_zero(const QPOSCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QEINT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCE, name + " " + "Position counter error interrupt enable");
    all_zero &= check_zero(obj.QPE, name + " " + "Quadrature phase error interrupt enable");
    all_zero &= check_zero(obj.QDC, name + " " + "Quadrature direction change interrupt enable");
    all_zero &= check_zero(obj.WTO, name + " " + "Watchdog time out interrupt enable");
    all_zero &= check_zero(obj.PCU, name + " " + "Position counter underflow interrupt enable");
    all_zero &= check_zero(obj.PCO, name + " " + "Position counter overflow interrupt enable");
    all_zero &= check_zero(obj.PCR, name + " " + "Position-compare ready interrupt enable");
    all_zero &= check_zero(obj.PCM, name + " " + "Position-compare match interrupt enable");
    all_zero &= check_zero(obj.SEL, name + " " + "Strobe event latch interrupt enable");
    all_zero &= check_zero(obj.IEL, name + " " + "Index event latch interrupt enable");
    all_zero &= check_zero(obj.UTO, name + " " + "Unit time out interrupt enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const QEINT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "Global interrupt status flag");
    all_zero &= check_zero(obj.PCE, name + " " + "Position counter error interrupt flag");
    all_zero &= check_zero(obj.PHE, name + " " + "Quadrature phase error interrupt flag");
    all_zero &= check_zero(obj.QDC, name + " " + "Quadrature direction change interrupt flag");
    all_zero &= check_zero(obj.WTO, name + " " + "Watchdog timeout interrupt flag");
    all_zero &= check_zero(obj.PCU, name + " " + "Position counter underflow interrupt flag");
    all_zero &= check_zero(obj.PCO, name + " " + "Position counter overflow interrupt flag");
    all_zero &= check_zero(obj.PCR, name + " " + "Position-compare ready interrupt flag");
    all_zero &= check_zero(obj.PCM, name + " " + "eQEP compare match event interrupt flag");
    all_zero &= check_zero(obj.SEL, name + " " + "Strobe event latch interrupt flag");
    all_zero &= check_zero(obj.IEL, name + " " + "Index event latch interrupt flag");
    all_zero &= check_zero(obj.UTO, name + " " + "Unit time out interrupt flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const QFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "Global interrupt clear flag");
    all_zero &= check_zero(obj.PCE, name + " " + "Clear position counter error interrupt flag");
    all_zero &= check_zero(obj.PHE, name + " " + "Clear quadrature phase error interrupt flag");
    all_zero &= check_zero(obj.QDC, name + " " + "Clear quadrature direction change interrupt flag");
    all_zero &= check_zero(obj.WTO, name + " " + "Clear watchdog timeout interrupt flag");
    all_zero &= check_zero(obj.PCU, name + " " + "Clear position counter underflow interrupt flag");
    all_zero &= check_zero(obj.PCO, name + " " + "Clear position counter overflow interrupt flag");
    all_zero &= check_zero(obj.PCR, name + " " + "Clear position-compare ready interrupt flag");
    all_zero &= check_zero(obj.PCM, name + " " + "Clear eQEP compare match event interrupt flag");
    all_zero &= check_zero(obj.SEL, name + " " + "Clear strobe event latch interrupt flag");
    all_zero &= check_zero(obj.IEL, name + " " + "Clear index event latch interrupt flag");
    all_zero &= check_zero(obj.UTO, name + " " + "Clear unit time out interrupt flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const QCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCE, name + " " + "Force position counter error interrupt");
    all_zero &= check_zero(obj.PHE, name + " " + "Force quadrature phase error interrupt");
    all_zero &= check_zero(obj.QDC, name + " " + "Force quadrature direction change interrupt");
    all_zero &= check_zero(obj.WTO, name + " " + "Force watchdog time out interrupt");
    all_zero &= check_zero(obj.PCU, name + " " + "Force position counter underflow interrupt");
    all_zero &= check_zero(obj.PCO, name + " " + "Force position counter overflow interrupt");
    all_zero &= check_zero(obj.PCR, name + " " + "Force position-compare ready interrupt");
    all_zero &= check_zero(obj.PCM, name + " " + "Force position-compare match interrupt");
    all_zero &= check_zero(obj.SEL, name + " " + "Force strobe event latch interrupt");
    all_zero &= check_zero(obj.IEL, name + " " + "Force index event latch interrupt");
    all_zero &= check_zero(obj.UTO, name + " " + "Force unit time out interrupt");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const QFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const QEPSTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PCEF, name + " " + "Position counter error flag.");
    all_zero &= check_zero(obj.FIMF, name + " " + "First index marker flag");
    all_zero &= check_zero(obj.CDEF, name + " " + "Capture direction error flag");
    all_zero &= check_zero(obj.COEF, name + " " + "Capture overflow error flag");
    all_zero &= check_zero(obj.QDLF, name + " " + "eQEP direction latch flag");
    all_zero &= check_zero(obj.QDF, name + " " + "Quadrature direction flag");
    all_zero &= check_zero(obj.FIDF, name + " " + "The first index marker");
    all_zero &= check_zero(obj.UPEVNT, name + " " + "Unit position event flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const QEPSTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EQEP_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QPOSCNT, name + " " + "Position Counter");
    all_zero &= check_zero(obj.QPOSINIT, name + " " + "Position Counter Init");
    all_zero &= check_zero(obj.QPOSMAX, name + " " + "Maximum Position Count");
    all_zero &= check_zero(obj.QPOSCMP, name + " " + "Position Compare");
    all_zero &= check_zero(obj.QPOSILAT, name + " " + "Index Position Latch");
    all_zero &= check_zero(obj.QPOSSLAT, name + " " + "Strobe Position Latch");
    all_zero &= check_zero(obj.QPOSLAT, name + " " + "Position Latch");
    all_zero &= check_zero(obj.QUTMR, name + " " + "QEP Unit Timer");
    all_zero &= check_zero(obj.QUPRD, name + " " + "QEP Unit Period");
    all_zero &= check_zero(obj.QWDTMR, name + " " + "QEP Watchdog Timer");
    all_zero &= check_zero(obj.QWDPRD, name + " " + "QEP Watchdog Period");
    all_zero &= check_zero(obj.QDECCTL, name + " " + "Quadrature Decoder Control");
    all_zero &= check_zero(obj.QEPCTL, name + " " + "QEP Control");
    all_zero &= check_zero(obj.QCAPCTL, name + " " + "Qaudrature Capture Control");
    all_zero &= check_zero(obj.QPOSCTL, name + " " + "Position Compare Control");
    all_zero &= check_zero(obj.QEINT, name + " " + "QEP Interrupt Control");
    all_zero &= check_zero(obj.QFLG, name + " " + "QEP Interrupt Flag");
    all_zero &= check_zero(obj.QCLR, name + " " + "QEP Interrupt Clear");
    all_zero &= check_zero(obj.QFRC, name + " " + "QEP Interrupt Force");
    all_zero &= check_zero(obj.QEPSTS, name + " " + "QEP Status");
    all_zero &= check_zero(obj.QCTMR, name + " " + "QEP Capture Timer");
    all_zero &= check_zero(obj.QCPRD, name + " " + "QEP Capture Period");
    all_zero &= check_zero(obj.QCTMRLAT, name + " " + "QEP Capture Latch");
    all_zero &= check_zero(obj.QCPRDLAT, name + " " + "QEP Capture Period Latch");

    return all_zero;
}

bool check_zero(const ECCTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CAP1POL, name + " " + "Capture Event 1 Polarity select");
    all_zero &= check_zero(obj.CTRRST1, name + " " + "Counter Reset on Capture Event 1");
    all_zero &= check_zero(obj.CAP2POL, name + " " + "Capture Event 2 Polarity select");
    all_zero &= check_zero(obj.CTRRST2, name + " " + "Counter Reset on Capture Event 2");
    all_zero &= check_zero(obj.CAP3POL, name + " " + "Capture Event 3 Polarity select");
    all_zero &= check_zero(obj.CTRRST3, name + " " + "Counter Reset on Capture Event 3");
    all_zero &= check_zero(obj.CAP4POL, name + " " + "Capture Event 4 Polarity select");
    all_zero &= check_zero(obj.CTRRST4, name + " " + "Counter Reset on Capture Event 4");
    all_zero &= check_zero(obj.CAPLDEN, name + " " + "Enable Loading CAP1-4 regs on a Cap Event");
    all_zero &= check_zero(obj.PRESCALE, name + " " + "Event Filter prescale select");
    all_zero &= check_zero(obj.FREE_SOFT, name + " " + "Emulation mode");

    return all_zero;
}

bool check_zero(const ECCTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CONT_ONESHT, name + " " + "Continuous or one-shot");
    all_zero &= check_zero(obj.STOP_WRAP, name + " " + "Stop value for one-shot, Wrap for continuous");
    all_zero &= check_zero(obj.REARM, name + " " + "One-shot re-arm");
    all_zero &= check_zero(obj.TSCTRSTOP, name + " " + "TSCNT counter stop");
    all_zero &= check_zero(obj.SYNCI_EN, name + " " + "Counter sync-in select");
    all_zero &= check_zero(obj.SYNCO_SEL, name + " " + "Sync-out mode");
    all_zero &= check_zero(obj.SWSYNC, name + " " + "SW forced counter sync");
    all_zero &= check_zero(obj.CAP_APWM, name + " " + "CAP/APWM operating mode select");
    all_zero &= check_zero(obj.APWMPOL, name + " " + "APWM output polarity select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECEINT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CEVT1, name + " " + "Capture Event 1 Interrupt Enable");
    all_zero &= check_zero(obj.CEVT2, name + " " + "Capture Event 2 Interrupt Enable");
    all_zero &= check_zero(obj.CEVT3, name + " " + "Capture Event 3 Interrupt Enable");
    all_zero &= check_zero(obj.CEVT4, name + " " + "Capture Event 4 Interrupt Enable");
    all_zero &= check_zero(obj.CTROVF, name + " " + "Counter Overflow Interrupt Enable");
    all_zero &= check_zero(obj.CTR_EQ_PRD, name + " " + "Period Equal Interrupt Enable");
    all_zero &= check_zero(obj.CTR_EQ_CMP, name + " " + "Compare Equal Interrupt Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECEINT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "Global Flag");
    all_zero &= check_zero(obj.CEVT1, name + " " + "Capture Event 1 Interrupt Flag");
    all_zero &= check_zero(obj.CEVT2, name + " " + "Capture Event 2 Interrupt Flag");
    all_zero &= check_zero(obj.CEVT3, name + " " + "Capture Event 3 Interrupt Flag");
    all_zero &= check_zero(obj.CEVT4, name + " " + "Capture Event 4 Interrupt Flag");
    all_zero &= check_zero(obj.CTROVF, name + " " + "Counter Overflow Interrupt Flag");
    all_zero &= check_zero(obj.CTR_PRD, name + " " + "Period Equal Interrupt Flag");
    all_zero &= check_zero(obj.CTR_CMP, name + " " + "Compare Equal Interrupt Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "ECAP Global Interrupt Status Clear");
    all_zero &= check_zero(obj.CEVT1, name + " " + "Capture Event 1 Status Clear");
    all_zero &= check_zero(obj.CEVT2, name + " " + "Capture Event 2 Status Clear");
    all_zero &= check_zero(obj.CEVT3, name + " " + "Capture Event 3 Status Clear");
    all_zero &= check_zero(obj.CEVT4, name + " " + "Capture Event 4 Status Clear");
    all_zero &= check_zero(obj.CTROVF, name + " " + "Counter Overflow Status Clear");
    all_zero &= check_zero(obj.CTR_PRD, name + " " + "Period Equal Status Clear");
    all_zero &= check_zero(obj.CTR_CMP, name + " " + "Compare Equal Status Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CEVT1, name + " " + "Capture Event 1 Force Interrupt");
    all_zero &= check_zero(obj.CEVT2, name + " " + "Capture Event 2 Force Interrupt");
    all_zero &= check_zero(obj.CEVT3, name + " " + "Capture Event 3 Force Interrupt");
    all_zero &= check_zero(obj.CEVT4, name + " " + "Capture Event 4 Force Interrupt");
    all_zero &= check_zero(obj.CTROVF, name + " " + "Counter Overflow Force Interrupt");
    all_zero &= check_zero(obj.CTR_PRD, name + " " + "Period Equal Force Interrupt");
    all_zero &= check_zero(obj.CTR_CMP, name + " " + "Compare Equal Force Interrupt");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ECFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ECAP_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TSCTR, name + " " + "Time-Stamp Counter");
    all_zero &= check_zero(obj.CTRPHS, name + " " + "Counter Phase Offset Value Register");
    all_zero &= check_zero(obj.CAP1, name + " " + "Capture 1 Register");
    all_zero &= check_zero(obj.CAP2, name + " " + "Capture 2 Register");
    all_zero &= check_zero(obj.CAP3, name + " " + "Capture 3 Register");
    all_zero &= check_zero(obj.CAP4, name + " " + "Capture 4 Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ECCTL1, name + " " + "Capture Control Register 1");
    all_zero &= check_zero(obj.ECCTL2, name + " " + "Capture Control Register 2");
    all_zero &= check_zero(obj.ECEINT, name + " " + "Capture Interrupt Enable Register");
    all_zero &= check_zero(obj.ECFLG, name + " " + "Capture Interrupt Flag Register");
    all_zero &= check_zero(obj.ECCLR, name + " " + "Capture Interrupt Clear Register");
    all_zero &= check_zero(obj.ECFRC, name + " " + "Capture Interrupt Force Register");

    return all_zero;
}

bool check_zero(const DEVCFGLOCK1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUSEL0, name + " " + "Lock bit for CPUSEL0 register");
    all_zero &= check_zero(obj.CPUSEL1, name + " " + "Lock bit for CPUSEL1 register");
    all_zero &= check_zero(obj.CPUSEL2, name + " " + "Lock bit for CPUSEL2 register");
    all_zero &= check_zero(obj.CPUSEL3, name + " " + "Lock bit for CPUSEL3 register");
    all_zero &= check_zero(obj.CPUSEL4, name + " " + "Lock bit for CPUSEL4 register");
    all_zero &= check_zero(obj.CPUSEL5, name + " " + "Lock bit for CPUSEL5 register");
    all_zero &= check_zero(obj.CPUSEL6, name + " " + "Lock bit for CPUSEL6 register");
    all_zero &= check_zero(obj.CPUSEL7, name + " " + "Lock bit for CPUSEL7 register");
    all_zero &= check_zero(obj.CPUSEL8, name + " " + "Lock bit for CPUSEL8 register");
    all_zero &= check_zero(obj.CPUSEL9, name + " " + "Lock bit for CPUSEL9 register");
    all_zero &= check_zero(obj.CPUSEL10, name + " " + "Lock bit for CPUSEL10 register");
    all_zero &= check_zero(obj.CPUSEL11, name + " " + "Lock bit for CPUSEL11 register");
    all_zero &= check_zero(obj.CPUSEL12, name + " " + "Lock bit for CPUSEL12 register");
    all_zero &= check_zero(obj.CPUSEL13, name + " " + "Lock bit for CPUSEL13 register");
    all_zero &= check_zero(obj.CPUSEL14, name + " " + "Lock bit for CPUSEL14 register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DEVCFGLOCK1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PARTIDL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.QUAL, name + " " + "Qualification Status");
    all_zero &= check_zero(obj.PIN_COUNT, name + " " + "Device Pin Count");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.INSTASPIN, name + " " + "Motorware feature set");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.FLASH_SIZE, name + " " + "Flash size in KB");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.PARTID_FORMAT_REVISION, name + " " + "Revision of the PARTID format");

    return all_zero;
}

bool check_zero(const PARTIDL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PARTIDH_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FAMILY, name + " " + "Device family");
    all_zero &= check_zero(obj.PARTNO, name + " " + "Device part number");
    all_zero &= check_zero(obj.DEVICE_CLASS_ID, name + " " + "Device class ID");

    return all_zero;
}

bool check_zero(const PARTIDH_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SINGLE_CORE, name + " " + "Single Core vs Dual Core");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPU1_FPU_TMU, name + " " + "CPU1's FPU1+TMU1");
    all_zero &= check_zero(obj.CPU2_FPU_TMU, name + " " + "CPU2's FPU2+TMU2");
    all_zero &= check_zero(obj.CPU1_VCU, name + " " + "CPU1's VCU");
    all_zero &= check_zero(obj.CPU2_VCU, name + " " + "CPU2's VCU");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPU1_CLA1, name + " " + "CPU1.CLA1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPU2_CLA1, name + " " + "CPU2.CLA1");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EMIF1, name + " " + "EMIF1");
    all_zero &= check_zero(obj.EMIF2, name + " " + "EMIF2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWM1, name + " " + "EPWM1");
    all_zero &= check_zero(obj.EPWM2, name + " " + "EPWM2");
    all_zero &= check_zero(obj.EPWM3, name + " " + "EPWM3");
    all_zero &= check_zero(obj.EPWM4, name + " " + "EPWM4");
    all_zero &= check_zero(obj.EPWM5, name + " " + "EPWM5");
    all_zero &= check_zero(obj.EPWM6, name + " " + "EPWM6");
    all_zero &= check_zero(obj.EPWM7, name + " " + "EPWM7");
    all_zero &= check_zero(obj.EPWM8, name + " " + "EPWM8");
    all_zero &= check_zero(obj.EPWM9, name + " " + "EPWM9");
    all_zero &= check_zero(obj.EPWM10, name + " " + "EPWM10");
    all_zero &= check_zero(obj.EPWM11, name + " " + "EPWM11");
    all_zero &= check_zero(obj.EPWM12, name + " " + "EPWM12");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECAP1, name + " " + "ECAP1");
    all_zero &= check_zero(obj.ECAP2, name + " " + "ECAP2");
    all_zero &= check_zero(obj.ECAP3, name + " " + "ECAP3");
    all_zero &= check_zero(obj.ECAP4, name + " " + "ECAP4");
    all_zero &= check_zero(obj.ECAP5, name + " " + "ECAP5");
    all_zero &= check_zero(obj.ECAP6, name + " " + "ECAP6");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC5_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EQEP1, name + " " + "EQEP1");
    all_zero &= check_zero(obj.EQEP2, name + " " + "EQEP2");
    all_zero &= check_zero(obj.EQEP3, name + " " + "EQEP3");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC5_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLB1, name + " " + "CLB1");
    all_zero &= check_zero(obj.CLB2, name + " " + "CLB2");
    all_zero &= check_zero(obj.CLB3, name + " " + "CLB3");
    all_zero &= check_zero(obj.CLB4, name + " " + "CLB4");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SD1, name + " " + "SD1");
    all_zero &= check_zero(obj.SD2, name + " " + "SD2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCI_A, name + " " + "SCI_A");
    all_zero &= check_zero(obj.SCI_B, name + " " + "SCI_B");
    all_zero &= check_zero(obj.SCI_C, name + " " + "SCI_C");
    all_zero &= check_zero(obj.SCI_D, name + " " + "SCI_D");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPI_A, name + " " + "SPI_A");
    all_zero &= check_zero(obj.SPI_B, name + " " + "SPI_B");
    all_zero &= check_zero(obj.SPI_C, name + " " + "SPI_C");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC10_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.I2C_A, name + " " + "I2C_A");
    all_zero &= check_zero(obj.I2C_B, name + " " + "I2C_B");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC10_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CAN_A, name + " " + "CAN_A");
    all_zero &= check_zero(obj.CAN_B, name + " " + "CAN_B");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC12_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.McBSP_A, name + " " + "McBSP_A");
    all_zero &= check_zero(obj.McBSP_B, name + " " + "McBSP_B");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.USB_A, name + " " + "Decides the capability of the USB_A Module");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC12_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC13_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.uPP_A, name + " " + "uPP_A");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC13_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC14_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADC_A, name + " " + "ADC_A");
    all_zero &= check_zero(obj.ADC_B, name + " " + "ADC_B");
    all_zero &= check_zero(obj.ADC_C, name + " " + "ADC_C");
    all_zero &= check_zero(obj.ADC_D, name + " " + "ADC_D");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC14_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC15_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPSS1, name + " " + "CMPSS1");
    all_zero &= check_zero(obj.CMPSS2, name + " " + "CMPSS2");
    all_zero &= check_zero(obj.CMPSS3, name + " " + "CMPSS3");
    all_zero &= check_zero(obj.CMPSS4, name + " " + "CMPSS4");
    all_zero &= check_zero(obj.CMPSS5, name + " " + "CMPSS5");
    all_zero &= check_zero(obj.CMPSS6, name + " " + "CMPSS6");
    all_zero &= check_zero(obj.CMPSS7, name + " " + "CMPSS7");
    all_zero &= check_zero(obj.CMPSS8, name + " " + "CMPSS8");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC15_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC17_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.DAC_A, name + " " + "Buffered-DAC_A");
    all_zero &= check_zero(obj.DAC_B, name + " " + "Buffered-DAC_B");
    all_zero &= check_zero(obj.DAC_C, name + " " + "Buffered-DAC_C");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC17_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC18_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LS0_1, name + " " + "LS0_1");
    all_zero &= check_zero(obj.LS1_1, name + " " + "LS1_1");
    all_zero &= check_zero(obj.LS2_1, name + " " + "LS2_1");
    all_zero &= check_zero(obj.LS3_1, name + " " + "LS3_1");
    all_zero &= check_zero(obj.LS4_1, name + " " + "LS4_1");
    all_zero &= check_zero(obj.LS5_1, name + " " + "LS5_1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC18_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC19_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LS0_2, name + " " + "LS0_2");
    all_zero &= check_zero(obj.LS1_2, name + " " + "LS1_2");
    all_zero &= check_zero(obj.LS2_2, name + " " + "LS2_2");
    all_zero &= check_zero(obj.LS3_2, name + " " + "LS3_2");
    all_zero &= check_zero(obj.LS4_2, name + " " + "LS4_2");
    all_zero &= check_zero(obj.LS5_2, name + " " + "LS5_2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC19_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DC20_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GS0, name + " " + "GS0");
    all_zero &= check_zero(obj.GS1, name + " " + "GS1");
    all_zero &= check_zero(obj.GS2, name + " " + "GS2");
    all_zero &= check_zero(obj.GS3, name + " " + "GS3");
    all_zero &= check_zero(obj.GS4, name + " " + "GS4");
    all_zero &= check_zero(obj.GS5, name + " " + "GS5");
    all_zero &= check_zero(obj.GS6, name + " " + "GS6");
    all_zero &= check_zero(obj.GS7, name + " " + "GS7");
    all_zero &= check_zero(obj.GS8, name + " " + "GS8");
    all_zero &= check_zero(obj.GS9, name + " " + "GS9");
    all_zero &= check_zero(obj.GS10, name + " " + "GS10");
    all_zero &= check_zero(obj.GS11, name + " " + "GS11");
    all_zero &= check_zero(obj.GS12, name + " " + "GS12");
    all_zero &= check_zero(obj.GS13, name + " " + "GS13");
    all_zero &= check_zero(obj.GS14, name + " " + "GS14");
    all_zero &= check_zero(obj.GS15, name + " " + "GS15");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DC20_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PERCNF1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADC_A_MODE, name + " " + "ADC_A mode setting bit");
    all_zero &= check_zero(obj.ADC_B_MODE, name + " " + "ADC_B mode setting bit");
    all_zero &= check_zero(obj.ADC_C_MODE, name + " " + "ADC_C mode setting bit");
    all_zero &= check_zero(obj.ADC_D_MODE, name + " " + "ADC_D mode setting bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.USB_A_PHY, name + " " + "USB_A_PHY");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PERCNF1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const FUSEERR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ALERR, name + " " + "Efuse Autoload Error Status");
    all_zero &= check_zero(obj.ERR, name + " " + "Efuse Self Test Error Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const FUSEERR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPU1_CLA1, name + " " + "CPU1_CLA1 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPU2_CLA1, name + " " + "CPU2_CLA1 software reset bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EMIF1, name + " " + "EMIF1 software reset bit");
    all_zero &= check_zero(obj.EMIF2, name + " " + "EMIF2 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWM1, name + " " + "EPWM1 software reset bit");
    all_zero &= check_zero(obj.EPWM2, name + " " + "EPWM2 software reset bit");
    all_zero &= check_zero(obj.EPWM3, name + " " + "EPWM3 software reset bit");
    all_zero &= check_zero(obj.EPWM4, name + " " + "EPWM4 software reset bit");
    all_zero &= check_zero(obj.EPWM5, name + " " + "EPWM5 software reset bit");
    all_zero &= check_zero(obj.EPWM6, name + " " + "EPWM6 software reset bit");
    all_zero &= check_zero(obj.EPWM7, name + " " + "EPWM7 software reset bit");
    all_zero &= check_zero(obj.EPWM8, name + " " + "EPWM8 software reset bit");
    all_zero &= check_zero(obj.EPWM9, name + " " + "EPWM9 software reset bit");
    all_zero &= check_zero(obj.EPWM10, name + " " + "EPWM10 software reset bit");
    all_zero &= check_zero(obj.EPWM11, name + " " + "EPWM11 software reset bit");
    all_zero &= check_zero(obj.EPWM12, name + " " + "EPWM12 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECAP1, name + " " + "ECAP1 software reset bit");
    all_zero &= check_zero(obj.ECAP2, name + " " + "ECAP2 software reset bit");
    all_zero &= check_zero(obj.ECAP3, name + " " + "ECAP3 software reset bit");
    all_zero &= check_zero(obj.ECAP4, name + " " + "ECAP4 software reset bit");
    all_zero &= check_zero(obj.ECAP5, name + " " + "ECAP5 software reset bit");
    all_zero &= check_zero(obj.ECAP6, name + " " + "ECAP6 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EQEP1, name + " " + "EQEP1 software reset bit");
    all_zero &= check_zero(obj.EQEP2, name + " " + "EQEP2 software reset bit");
    all_zero &= check_zero(obj.EQEP3, name + " " + "EQEP3 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SD1, name + " " + "SD1 software reset bit");
    all_zero &= check_zero(obj.SD2, name + " " + "SD2 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCI_A, name + " " + "SCI_A software reset bit");
    all_zero &= check_zero(obj.SCI_B, name + " " + "SCI_B software reset bit");
    all_zero &= check_zero(obj.SCI_C, name + " " + "SCI_C software reset bit");
    all_zero &= check_zero(obj.SCI_D, name + " " + "SCI_D software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPI_A, name + " " + "SPI_A software reset bit");
    all_zero &= check_zero(obj.SPI_B, name + " " + "SPI_B software reset bit");
    all_zero &= check_zero(obj.SPI_C, name + " " + "SPI_C software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.I2C_A, name + " " + "I2C_A software reset bit");
    all_zero &= check_zero(obj.I2C_B, name + " " + "I2C_B software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.McBSP_A, name + " " + "McBSP_A software reset bit");
    all_zero &= check_zero(obj.McBSP_B, name + " " + "McBSP_B software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.USB_A, name + " " + "USB_A software reset bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES13_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADC_A, name + " " + "ADC_A software reset bit");
    all_zero &= check_zero(obj.ADC_B, name + " " + "ADC_B software reset bit");
    all_zero &= check_zero(obj.ADC_C, name + " " + "ADC_C software reset bit");
    all_zero &= check_zero(obj.ADC_D, name + " " + "ADC_D software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES13_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES14_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPSS1, name + " " + "CMPSS1 software reset bit");
    all_zero &= check_zero(obj.CMPSS2, name + " " + "CMPSS2 software reset bit");
    all_zero &= check_zero(obj.CMPSS3, name + " " + "CMPSS3 software reset bit");
    all_zero &= check_zero(obj.CMPSS4, name + " " + "CMPSS4 software reset bit");
    all_zero &= check_zero(obj.CMPSS5, name + " " + "CMPSS5 software reset bit");
    all_zero &= check_zero(obj.CMPSS6, name + " " + "CMPSS6 software reset bit");
    all_zero &= check_zero(obj.CMPSS7, name + " " + "CMPSS7 software reset bit");
    all_zero &= check_zero(obj.CMPSS8, name + " " + "CMPSS8 software reset bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES14_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SOFTPRES16_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.DAC_A, name + " " + "Buffered_DAC_A software reset bit");
    all_zero &= check_zero(obj.DAC_B, name + " " + "Buffered_DAC_B software reset bit");
    all_zero &= check_zero(obj.DAC_C, name + " " + "Buffered_DAC_C software reset bit");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SOFTPRES16_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWM1, name + " " + "EPWM1 CPU select bit");
    all_zero &= check_zero(obj.EPWM2, name + " " + "EPWM2 CPU select bit");
    all_zero &= check_zero(obj.EPWM3, name + " " + "EPWM3 CPU select bit");
    all_zero &= check_zero(obj.EPWM4, name + " " + "EPWM4 CPU select bit");
    all_zero &= check_zero(obj.EPWM5, name + " " + "EPWM5 CPU select bit");
    all_zero &= check_zero(obj.EPWM6, name + " " + "EPWM6 CPU select bit");
    all_zero &= check_zero(obj.EPWM7, name + " " + "EPWM7 CPU select bit");
    all_zero &= check_zero(obj.EPWM8, name + " " + "EPWM8 CPU select bit");
    all_zero &= check_zero(obj.EPWM9, name + " " + "EPWM9 CPU select bit");
    all_zero &= check_zero(obj.EPWM10, name + " " + "EPWM10 CPU select bit");
    all_zero &= check_zero(obj.EPWM11, name + " " + "EPWM11 CPU select bit");
    all_zero &= check_zero(obj.EPWM12, name + " " + "EPWM12 CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECAP1, name + " " + "ECAP1 CPU select bit");
    all_zero &= check_zero(obj.ECAP2, name + " " + "ECAP2 CPU select bit");
    all_zero &= check_zero(obj.ECAP3, name + " " + "ECAP3 CPU select bit");
    all_zero &= check_zero(obj.ECAP4, name + " " + "ECAP4 CPU select bit");
    all_zero &= check_zero(obj.ECAP5, name + " " + "ECAP5 CPU select bit");
    all_zero &= check_zero(obj.ECAP6, name + " " + "ECAP6 CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EQEP1, name + " " + "EQEP1 CPU select bit");
    all_zero &= check_zero(obj.EQEP2, name + " " + "EQEP2 CPU select bit");
    all_zero &= check_zero(obj.EQEP3, name + " " + "EQEP3 CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SD1, name + " " + "SD1 CPU select bit");
    all_zero &= check_zero(obj.SD2, name + " " + "SD2 CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL5_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCI_A, name + " " + "SCI_A CPU select bit");
    all_zero &= check_zero(obj.SCI_B, name + " " + "SCI_B CPU select bit");
    all_zero &= check_zero(obj.SCI_C, name + " " + "SCI_C CPU select bit");
    all_zero &= check_zero(obj.SCI_D, name + " " + "SCI_D CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL5_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPI_A, name + " " + "SPI_A CPU select bit");
    all_zero &= check_zero(obj.SPI_B, name + " " + "SPI_B CPU select bit");
    all_zero &= check_zero(obj.SPI_C, name + " " + "SPI_C CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.I2C_A, name + " " + "I2C_A CPU select bit");
    all_zero &= check_zero(obj.I2C_B, name + " " + "I2C_B CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CAN_A, name + " " + "CAN_A CPU select bit");
    all_zero &= check_zero(obj.CAN_B, name + " " + "CAN_B CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.McBSP_A, name + " " + "McBSP_A CPU select bit");
    all_zero &= check_zero(obj.McBSP_B, name + " " + "McBSP_B CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADC_A, name + " " + "ADC_A CPU select bit");
    all_zero &= check_zero(obj.ADC_B, name + " " + "ADC_B CPU select bit");
    all_zero &= check_zero(obj.ADC_C, name + " " + "ADC_C CPU select bit");
    all_zero &= check_zero(obj.ADC_D, name + " " + "ADC_D CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL12_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPSS1, name + " " + "CMPSS1 CPU select bit");
    all_zero &= check_zero(obj.CMPSS2, name + " " + "CMPSS2 CPU select bit");
    all_zero &= check_zero(obj.CMPSS3, name + " " + "CMPSS3 CPU select bit");
    all_zero &= check_zero(obj.CMPSS4, name + " " + "CMPSS4 CPU select bit");
    all_zero &= check_zero(obj.CMPSS5, name + " " + "CMPSS5 CPU select bit");
    all_zero &= check_zero(obj.CMPSS6, name + " " + "CMPSS6 CPU select bit");
    all_zero &= check_zero(obj.CMPSS7, name + " " + "CMPSS7 CPU select bit");
    all_zero &= check_zero(obj.CMPSS8, name + " " + "CMPSS8 CPU select bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL12_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUSEL14_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.DAC_A, name + " " + "Buffered_DAC_A CPU select bit");
    all_zero &= check_zero(obj.DAC_B, name + " " + "Buffered_DAC_B CPU select bit");
    all_zero &= check_zero(obj.DAC_C, name + " " + "Buffered_DAC_C CPU select bit");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSEL14_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPU2RESCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RESET, name + " " + "CPU2 Reset Control bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Key Qualifier for writes to this register");

    return all_zero;
}

bool check_zero(const CPU2RESCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RSTSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPU2RES, name + " " + "CPU2 Reset Status bit");
    all_zero &= check_zero(obj.CPU2NMIWDRST, name + " " + "Indicates whether a CPU2.NMIWD reset was issued to CPU2");
    all_zero &= check_zero(obj.CPU2HWBISTRST0, name + " " + "Indicates whether a HWBIST reset was issued to CPU2");
    all_zero &= check_zero(obj.CPU2HWBISTRST1, name + " " + "Indicates whether a HWBIST reset was issued to CPU2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const RSTSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LPMSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPU2LPMSTAT, name + " " + "CPU2 LPM Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LPMSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYSDBGCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BIT_0, name + " " + "Used in PLL startup. Only reset by POR.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYSDBGCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DEV_CFG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DEVCFGLOCK1, name + " " + "Lock bit for CPUSELx registers");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PARTIDL, name + " " + "Lower 32-bit of Device PART Identification Number");
    all_zero &= check_zero(obj.PARTIDH, name + " " + "Upper 32-bit of Device PART Identification Number");
    all_zero &= check_zero(obj.REVID, name + " " + "Device Revision Number");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.DC0, name + " " + "Device Capability: Device Information");
    all_zero &= check_zero(obj.DC1, name + " " + "Device Capability: Processing Block Customization");
    all_zero &= check_zero(obj.DC2, name + " " + "Device Capability: EMIF Customization");
    all_zero &= check_zero(obj.DC3, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC4, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC5, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC6, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC7, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC8, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC9, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC10, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC11, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC12, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC13, name + " " + "Device Capability: Peripheral Customization");
    all_zero &= check_zero(obj.DC14, name + " " + "Device Capability: Analog Modules Customization");
    all_zero &= check_zero(obj.DC15, name + " " + "Device Capability: Analog Modules Customization");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.DC17, name + " " + "Device Capability: Analog Modules Customization");
    all_zero &= check_zero(obj.DC18, name + " " + "Device Capability: CPU1 Lx SRAM Customization");
    all_zero &= check_zero(obj.DC19, name + " " + "Device Capability: CPU2 Lx SRAM Customization");
    all_zero &= check_zero(obj.DC20, name + " " + "Device Capability: GSx SRAM Customization");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.PERCNF1, name + " " + "Peripheral Configuration register");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.FUSEERR, name + " " + "e-Fuse error Status register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTPRES0, name + " " + "Processing Block Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES1, name + " " + "EMIF Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES2, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES3, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES4, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTPRES6, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES7, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES8, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES9, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTPRES11, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTPRES13, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.SOFTPRES14, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFTPRES16, name + " " + "Peripheral Software Reset register");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPUSEL0, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL1, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL2, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPUSEL4, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL5, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL6, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL7, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL8, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL9, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPUSEL11, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.CPUSEL12, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPUSEL14, name + " " + "CPU Select register for common peripherals");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.CPU2RESCTL, name + " " + "CPU2 Reset Control Register");
    all_zero &= check_zero(obj.RSTSTAT, name + " " + "Reset Status register for secondary C28x CPUs");
    all_zero &= check_zero(obj.LPMSTAT, name + " " + "LPM Status Register for secondary C28x CPUs");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.SYSDBGCTL, name + " " + "System Debug Control register");

    return all_zero;
}

bool check_zero(const CLKSEM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEM, name + " " + "Semaphore for CLKCFG Ownership by CPU1 or CPU2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Key Qualifier for writes to this register");

    return all_zero;
}

bool check_zero(const CLKSEM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLKCFGLOCK1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKSRCCTL1, name + " " + "Lock bit for CLKSRCCTL1 register");
    all_zero &= check_zero(obj.CLKSRCCTL2, name + " " + "Lock bit for CLKSRCCTL2 register");
    all_zero &= check_zero(obj.CLKSRCCTL3, name + " " + "Lock bit for CLKSRCCTL3 register");
    all_zero &= check_zero(obj.SYSPLLCTL1, name + " " + "Lock bit for SYSPLLCTL1 register");
    all_zero &= check_zero(obj.SYSPLLCTL2, name + " " + "Lock bit for SYSPLLCTL2 register");
    all_zero &= check_zero(obj.SYSPLLCTL3, name + " " + "Lock bit for SYSPLLCTL3 register");
    all_zero &= check_zero(obj.SYSPLLMULT, name + " " + "Lock bit for SYSPLLMULT register");
    all_zero &= check_zero(obj.AUXPLLCTL1, name + " " + "Lock bit for AUXPLLCTL1 register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.AUXPLLMULT, name + " " + "Lock bit for AUXPLLMULT register");
    all_zero &= check_zero(obj.SYSCLKDIVSEL, name + " " + "Lock bit for SYSCLKDIVSEL register");
    all_zero &= check_zero(obj.AUXCLKDIVSEL, name + " " + "Lock bit for AUXCLKDIVSEL register");
    all_zero &= check_zero(obj.PERCLKDIVSEL, name + " " + "Lock bit for PERCLKDIVSEL register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOSPCP, name + " " + "Lock bit for LOSPCP register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLKCFGLOCK1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLKSRCCTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OSCCLKSRCSEL, name + " " + "OSCCLK Source Select Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INTOSC2OFF, name + " " + "Internal Oscillator 2 Off Bit");
    all_zero &= check_zero(obj.XTALOFF, name + " " + "Crystal (External) Oscillator Off Bit");
    all_zero &= check_zero(obj.WDHALTI, name + " " + "Watchdog HALT Mode Ignore Bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLKSRCCTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLKSRCCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AUXOSCCLKSRCSEL, name + " " + "AUXOSCCLK Source Select Bit");
    all_zero &= check_zero(obj.CANABCLKSEL, name + " " + "CANA Bit Clock Source Select Bit");
    all_zero &= check_zero(obj.CANBBCLKSEL, name + " " + "CANB Bit Clock Source Select Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLKSRCCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLKSRCCTL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XCLKOUTSEL, name + " " + "XCLKOUT Source Select Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLKSRCCTL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYSPLLCTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PLLEN, name + " " + "SYSPLL enable/disable bit");
    all_zero &= check_zero(obj.PLLCLKEN, name + " " + "SYSPLL bypassed or included in the PLLSYSCLK path");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYSPLLCTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYSPLLMULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IMULT, name + " " + "SYSPLL Integer Multiplier");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FMULT, name + " " + "SYSPLL Fractional Multiplier");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYSPLLMULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYSPLLSTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCKS, name + " " + "SYSPLL Lock Status Bit");
    all_zero &= check_zero(obj.SLIPS, name + " " + "SYSPLL Slip Status Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYSPLLSTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXPLLCTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PLLEN, name + " " + "AUXPLL enable/disable bit");
    all_zero &= check_zero(obj.PLLCLKEN, name + " " + "AUXPLL bypassed or included in the AUXPLLCLK path");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AUXPLLCTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXPLLMULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IMULT, name + " " + "AUXPLL Integer Multiplier");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FMULT, name + " " + "AUXPLL Fractional Multiplier");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AUXPLLMULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXPLLSTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCKS, name + " " + "AUXPLL Lock Status Bit");
    all_zero &= check_zero(obj.SLIPS, name + " " + "AUXPLL Slip Status Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AUXPLLSTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYSCLKDIVSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PLLSYSCLKDIV, name + " " + "PLLSYSCLK Divide Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYSCLKDIVSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXCLKDIVSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AUXPLLDIV, name + " " + "AUXPLLCLK Divide Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AUXCLKDIVSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PERCLKDIVSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWMCLKDIV, name + " " + "EPWM Clock Divide Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EMIF1CLKDIV, name + " " + "EMIF1  Clock Divide Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.EMIF2CLKDIV, name + " " + "EMIF2 Clock Divide Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PERCLKDIVSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XCLKOUTDIVSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XCLKOUTDIV, name + " " + "XCLKOUT Divide Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XCLKOUTDIVSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LOSPCP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LSPCLKDIV, name + " " + "LSPCLK Divide Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LOSPCP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const MCDCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MCLKSTS, name + " " + "Missing Clock Status Bit");
    all_zero &= check_zero(obj.MCLKCLR, name + " " + "Missing Clock Clear Bit");
    all_zero &= check_zero(obj.MCLKOFF, name + " " + "Missing Clock Detect Off Bit");
    all_zero &= check_zero(obj.OSCOFF, name + " " + "Oscillator Clock Off Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const MCDCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const X1CNT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.X1CNT, name + " " + "X1 Counter");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const X1CNT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLK_CFG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKSEM, name + " " + "Clock Control Semaphore Register");
    all_zero &= check_zero(obj.CLKCFGLOCK1, name + " " + "Lock bit for CLKCFG registers");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLKSRCCTL1, name + " " + "Clock Source Control register-1");
    all_zero &= check_zero(obj.CLKSRCCTL2, name + " " + "Clock Source Control register-2");
    all_zero &= check_zero(obj.CLKSRCCTL3, name + " " + "Clock Source Control register-3");
    all_zero &= check_zero(obj.SYSPLLCTL1, name + " " + "SYSPLL Control register-1");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SYSPLLMULT, name + " " + "SYSPLL Multiplier register");
    all_zero &= check_zero(obj.SYSPLLSTS, name + " " + "SYSPLL Status register");
    all_zero &= check_zero(obj.AUXPLLCTL1, name + " " + "AUXPLL Control register-1");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.AUXPLLMULT, name + " " + "AUXPLL Multiplier register");
    all_zero &= check_zero(obj.AUXPLLSTS, name + " " + "AUXPLL Status register");
    all_zero &= check_zero(obj.SYSCLKDIVSEL, name + " " + "System Clock Divider Select register");
    all_zero &= check_zero(obj.AUXCLKDIVSEL, name + " " + "Auxillary Clock Divider Select register");
    all_zero &= check_zero(obj.PERCLKDIVSEL, name + " " + "Peripheral Clock Divider Selet register");
    all_zero &= check_zero(obj.XCLKOUTDIVSEL, name + " " + "XCLKOUT Divider Select register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOSPCP, name + " " + "Low Speed Clock Source Prescalar");
    all_zero &= check_zero(obj.MCDCR, name + " " + "Missing Clock Detect Control Register");
    all_zero &= check_zero(obj.X1CNT, name + " " + "10-bit Counter on X1 Clock");

    return all_zero;
}

bool check_zero(const CPUSYSLOCK1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HIBBOOTMODE, name + " " + "Lock bit for HIBBOOTMODE register");
    all_zero &= check_zero(obj.IORESTOREADDR, name + " " + "Lock bit for IORESTOREADDR Register");
    all_zero &= check_zero(obj.PIEVERRADDR, name + " " + "Lock bit for PIEVERRADDR Register");
    all_zero &= check_zero(obj.PCLKCR0, name + " " + "Lock bit for PCLKCR0 Register");
    all_zero &= check_zero(obj.PCLKCR1, name + " " + "Lock bit for PCLKCR1 Register");
    all_zero &= check_zero(obj.PCLKCR2, name + " " + "Lock bit for PCLKCR2 Register");
    all_zero &= check_zero(obj.PCLKCR3, name + " " + "Lock bit for PCLKCR3 Register");
    all_zero &= check_zero(obj.PCLKCR4, name + " " + "Lock bit for PCLKCR4 Register");
    all_zero &= check_zero(obj.PCLKCR5, name + " " + "Lock bit for PCLKCR5 Register");
    all_zero &= check_zero(obj.PCLKCR6, name + " " + "Lock bit for PCLKCR6 Register");
    all_zero &= check_zero(obj.PCLKCR7, name + " " + "Lock bit for PCLKCR7 Register");
    all_zero &= check_zero(obj.PCLKCR8, name + " " + "Lock bit for PCLKCR8 Register");
    all_zero &= check_zero(obj.PCLKCR9, name + " " + "Lock bit for PCLKCR9 Register");
    all_zero &= check_zero(obj.PCLKCR10, name + " " + "Lock bit for PCLKCR10 Register");
    all_zero &= check_zero(obj.PCLKCR11, name + " " + "Lock bit for PCLKCR11 Register");
    all_zero &= check_zero(obj.PCLKCR12, name + " " + "Lock bit for PCLKCR12 Register");
    all_zero &= check_zero(obj.PCLKCR13, name + " " + "Lock bit for PCLKCR13 Register");
    all_zero &= check_zero(obj.PCLKCR14, name + " " + "Lock bit for PCLKCR14 Register");
    all_zero &= check_zero(obj.PCLKCR15, name + " " + "Lock bit for PCLKCR15 Register");
    all_zero &= check_zero(obj.PCLKCR16, name + " " + "Lock bit for PCLKCR16 Register");
    all_zero &= check_zero(obj.SECMSEL, name + " " + "Lock bit for SECMSEL Register");
    all_zero &= check_zero(obj.LPMCR, name + " " + "Lock bit for LPMCR Register");
    all_zero &= check_zero(obj.GPIOLPMSEL0, name + " " + "Lock bit for GPIOLPMSEL0 Register");
    all_zero &= check_zero(obj.GPIOLPMSEL1, name + " " + "Lock bit for GPIOLPMSEL1 Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CPUSYSLOCK1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const IORESTOREADDR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADDR, name + " " + "restoreIO() routine address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const IORESTOREADDR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEVERRADDR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADDR, name + " " + "PIE Vector Fetch Error Handler Routine Address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PIEVERRADDR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLA1, name + " " + "CLA1 Clock Enable Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.DMA, name + " " + "DMA Clock Enable bit");
    all_zero &= check_zero(obj.CPUTIMER0, name + " " + "CPUTIMER0 Clock Enable bit");
    all_zero &= check_zero(obj.CPUTIMER1, name + " " + "CPUTIMER1 Clock Enable bit");
    all_zero &= check_zero(obj.CPUTIMER2, name + " " + "CPUTIMER2 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.HRPWM, name + " " + "HRPWM Clock Enable Bit");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.TBCLKSYNC, name + " " + "EPWM Time Base Clock sync");
    all_zero &= check_zero(obj.GTBCLKSYNC, name + " " + "EPWM Time Base Clock Global sync");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EMIF1, name + " " + "EMIF1 Clock Enable bit");
    all_zero &= check_zero(obj.EMIF2, name + " " + "EMIF2 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWM1, name + " " + "EPWM1 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM2, name + " " + "EPWM2 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM3, name + " " + "EPWM3 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM4, name + " " + "EPWM4 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM5, name + " " + "EPWM5 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM6, name + " " + "EPWM6 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM7, name + " " + "EPWM7 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM8, name + " " + "EPWM8 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM9, name + " " + "EPWM9 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM10, name + " " + "EPWM10 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM11, name + " " + "EPWM11 Clock Enable bit");
    all_zero &= check_zero(obj.EPWM12, name + " " + "EPWM12 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ECAP1, name + " " + "ECAP1 Clock Enable bit");
    all_zero &= check_zero(obj.ECAP2, name + " " + "ECAP2 Clock Enable bit");
    all_zero &= check_zero(obj.ECAP3, name + " " + "ECAP3 Clock Enable bit");
    all_zero &= check_zero(obj.ECAP4, name + " " + "ECAP4 Clock Enable bit");
    all_zero &= check_zero(obj.ECAP5, name + " " + "ECAP5 Clock Enable bit");
    all_zero &= check_zero(obj.ECAP6, name + " " + "ECAP6 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EQEP1, name + " " + "EQEP1 Clock Enable bit");
    all_zero &= check_zero(obj.EQEP2, name + " " + "EQEP2 Clock Enable bit");
    all_zero &= check_zero(obj.EQEP3, name + " " + "EQEP3 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SD1, name + " " + "SD1 Clock Enable bit");
    all_zero &= check_zero(obj.SD2, name + " " + "SD2 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCI_A, name + " " + "SCI_A Clock Enable bit");
    all_zero &= check_zero(obj.SCI_B, name + " " + "SCI_B Clock Enable bit");
    all_zero &= check_zero(obj.SCI_C, name + " " + "SCI_C Clock Enable bit");
    all_zero &= check_zero(obj.SCI_D, name + " " + "SCI_D Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPI_A, name + " " + "SPI_A Clock Enable bit");
    all_zero &= check_zero(obj.SPI_B, name + " " + "SPI_B Clock Enable bit");
    all_zero &= check_zero(obj.SPI_C, name + " " + "SPI_C Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.I2C_A, name + " " + "I2C_A Clock Enable bit");
    all_zero &= check_zero(obj.I2C_B, name + " " + "I2C_B Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR10_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CAN_A, name + " " + "CAN_A Clock Enable bit");
    all_zero &= check_zero(obj.CAN_B, name + " " + "CAN_B Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR10_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.McBSP_A, name + " " + "McBSP_A Clock Enable bit");
    all_zero &= check_zero(obj.McBSP_B, name + " " + "McBSP_B Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.USB_A, name + " " + "USB_A Clock Enable bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR12_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.uPP_A, name + " " + "uPP_A Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR12_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR13_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADC_A, name + " " + "ADC_A Clock Enable bit");
    all_zero &= check_zero(obj.ADC_B, name + " " + "ADC_B Clock Enable bit");
    all_zero &= check_zero(obj.ADC_C, name + " " + "ADC_C Clock Enable bit");
    all_zero &= check_zero(obj.ADC_D, name + " " + "ADC_D Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR13_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR14_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPSS1, name + " " + "CMPSS1 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS2, name + " " + "CMPSS2 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS3, name + " " + "CMPSS3 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS4, name + " " + "CMPSS4 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS5, name + " " + "CMPSS5 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS6, name + " " + "CMPSS6 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS7, name + " " + "CMPSS7 Clock Enable bit");
    all_zero &= check_zero(obj.CMPSS8, name + " " + "CMPSS8 Clock Enable bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR14_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCLKCR16_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.DAC_A, name + " " + "Buffered_DAC_A Clock Enable Bit");
    all_zero &= check_zero(obj.DAC_B, name + " " + "Buffered_DAC_B Clock Enable Bit");
    all_zero &= check_zero(obj.DAC_C, name + " " + "Buffered_DAC_C Clock Enable Bit");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCLKCR16_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SECMSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PF1SEL, name + " " + "Secondary Master Select for VBUS32_1 Bridge");
    all_zero &= check_zero(obj.PF2SEL, name + " " + "Secondary Master Select for VBUS32_2 Bridge");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SECMSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LPMCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LPM, name + " " + "Low Power Mode setting");
    all_zero &= check_zero(obj.QUALSTDBY, name + " " + "STANDBY Wakeup Pin Qualification Setting");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.WDINTE, name + " " + "Enable for WDINT wakeup from STANDBY");
    all_zero &= check_zero(obj.M0M1MODE, name + " " + "Configuration for M0 and M1 mode during HIB");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.IOISODIS, name + " " + "IO Isolation Disable");

    return all_zero;
}

bool check_zero(const LPMCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPIOLPMSEL0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "GPIO0 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO1, name + " " + "GPIO1 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO2, name + " " + "GPIO2 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO3, name + " " + "GPIO3 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO4, name + " " + "GPIO4 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO5, name + " " + "GPIO5 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO6, name + " " + "GPIO6 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO7, name + " " + "GPIO7 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO8, name + " " + "GPIO8 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO9, name + " " + "GPIO9 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO10, name + " " + "GPIO10 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO11, name + " " + "GPIO11 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO12, name + " " + "GPIO12 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO13, name + " " + "GPIO13 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO14, name + " " + "GPIO14 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO15, name + " " + "GPIO15 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO16, name + " " + "GPIO16 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO17, name + " " + "GPIO17 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO18, name + " " + "GPIO18 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO19, name + " " + "GPIO19 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO20, name + " " + "GPIO20 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO21, name + " " + "GPIO21 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO22, name + " " + "GPIO22 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO23, name + " " + "GPIO23 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO24, name + " " + "GPIO24 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO25, name + " " + "GPIO25 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO26, name + " " + "GPIO26 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO27, name + " " + "GPIO27 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO28, name + " " + "GPIO28 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO29, name + " " + "GPIO29 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO30, name + " " + "GPIO30 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO31, name + " " + "GPIO31 Enable for LPM Wakeup");

    return all_zero;
}

bool check_zero(const GPIOLPMSEL0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPIOLPMSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "GPIO32 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO33, name + " " + "GPIO33 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO34, name + " " + "GPIO34 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO35, name + " " + "GPIO35 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO36, name + " " + "GPIO36 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO37, name + " " + "GPIO37 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO38, name + " " + "GPIO38 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO39, name + " " + "GPIO39 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO40, name + " " + "GPIO40 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO41, name + " " + "GPIO41 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO42, name + " " + "GPIO42 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO43, name + " " + "GPIO43 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO44, name + " " + "GPIO44 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO45, name + " " + "GPIO45 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO46, name + " " + "GPIO46 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO47, name + " " + "GPIO47 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO48, name + " " + "GPIO48 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO49, name + " " + "GPIO49 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO50, name + " " + "GPIO50 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO51, name + " " + "GPIO51 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO52, name + " " + "GPIO52 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO53, name + " " + "GPIO53 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO54, name + " " + "GPIO54 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO55, name + " " + "GPIO55 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO56, name + " " + "GPIO56 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO57, name + " " + "GPIO57 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO58, name + " " + "GPIO58 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO59, name + " " + "GPIO59 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO60, name + " " + "GPIO60 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO61, name + " " + "GPIO61 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO62, name + " " + "GPIO62 Enable for LPM Wakeup");
    all_zero &= check_zero(obj.GPIO63, name + " " + "GPIO63 Enable for LPM Wakeup");

    return all_zero;
}

bool check_zero(const GPIOLPMSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TMR2CLKCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TMR2CLKSRCSEL, name + " " + "CPU Timer 2 Clock Source Select Bit");
    all_zero &= check_zero(obj.TMR2CLKPRESCALE, name + " " + "CPU Timer 2 Clock Pre-Scale Value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TMR2CLKCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RESC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.POR, name + " " + "POR Reset Cause Indication Bit");
    all_zero &= check_zero(obj.XRSn, name + " " + "XRSn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.WDRSn, name + " " + "WDRSn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.NMIWDRSn, name + " " + "NMIWDRSn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.HWBISTn, name + " " + "HWBISTn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.HIBRESETn, name + " " + "HIBRESETn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCCRESETn, name + " " + "SCCRESETn Reset Cause Indication Bit");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.XRSn_pin_status, name + " " + "XRSN Pin Status");
    all_zero &= check_zero(obj.TRSTn_pin_status, name + " " + "TRSTn Status");

    return all_zero;
}

bool check_zero(const RESC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPU_SYS_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CPUSYSLOCK1, name + " " + "Lock bit for CPUSYS registers");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.HIBBOOTMODE, name + " " + "HIB Boot Mode Register");
    all_zero &= check_zero(obj.IORESTOREADDR, name + " " + "IORestore() routine Address Register");
    all_zero &= check_zero(obj.PIEVERRADDR, name + " " + "PIE Vector Fetch Error Address register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCLKCR0, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR1, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR2, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR3, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR4, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCLKCR6, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR7, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR8, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR9, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR10, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR11, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR12, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR13, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.PCLKCR14, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCLKCR16, name + " " + "Peripheral Clock Gating Registers");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.SECMSEL,
                           name + " " +
                           "Secondary Master Select register for common peripherals: Selects between CLA & DMA");
    all_zero &= check_zero(obj.LPMCR, name + " " + "LPM Control Register");
    all_zero &= check_zero(obj.GPIOLPMSEL0, name + " " + "GPIO LPM Wakeup select registers");
    all_zero &= check_zero(obj.GPIOLPMSEL1, name + " " + "GPIO LPM Wakeup select registers");
    all_zero &= check_zero(obj.TMR2CLKCTL, name + " " + "Timer2 Clock Measurement functionality control register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.RESC, name + " " + "Reset Cause register");

    return all_zero;
}

bool check_zero(const SCSR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WDOVERRIDE, name + " " + "WD Override for WDDIS bit");
    all_zero &= check_zero(obj.WDENINT, name + " " + "WD Interrupt Enable");
    all_zero &= check_zero(obj.WDINTS, name + " " + "WD Interrupt Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCSR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const WDCNTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WDCNTR, name + " " + "WD Counter");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const WDCNTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const WDKEY_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WDKEY, name + " " + "WD KEY");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const WDKEY_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const WDCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.WDPS, name + " " + "WD Clock Prescalar");
    all_zero &= check_zero(obj.WDCHK, name + " " + "WD Check Bits");
    all_zero &= check_zero(obj.WDDIS, name + " " + "WD Disable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const WDCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const WDWCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MIN, name + " " + "WD Min Threshold setting for Windowed Watchdog functionality");
    all_zero &= check_zero(obj.FIRSTKEY, name + " " + "First Key Detect Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const WDWCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const WD_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCSR, name + " " + "System Control & Status Register");
    all_zero &= check_zero(obj.WDCNTR, name + " " + "Watchdog Counter Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.WDKEY, name + " " + "Watchdog Reset Key Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.WDCR, name + " " + "Watchdog Control Register");
    all_zero &= check_zero(obj.WDWCR, name + " " + "Watchdog Windowed Control Register");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSELLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLA1TASKSRCSEL1, name + " " + "CLA1TASKSRCSEL1 Register Lock bit");
    all_zero &= check_zero(obj.CLA1TASKSRCSEL2, name + " " + "CLA1TASKSRCSEL2 Register Lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSELLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DMACHSRCSELLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DMACHSRCSEL1, name + " " + "DMACHSRCSEL1 Register Lock bit");
    all_zero &= check_zero(obj.DMACHSRCSEL2, name + " " + "DMACHSRCSEL2 Register Lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DMACHSRCSELLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TASK1, name + " " + "Selects the Trigger Source for TASK1 of CLA1");
    all_zero &= check_zero(obj.TASK2, name + " " + "Selects the Trigger Source for TASK2 of CLA1");
    all_zero &= check_zero(obj.TASK3, name + " " + "Selects the Trigger Source for TASK3 of CLA1");
    all_zero &= check_zero(obj.TASK4, name + " " + "Selects the Trigger Source for TASK4 of CLA1");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TASK5, name + " " + "Selects the Trigger Source for TASK5 of CLA1");
    all_zero &= check_zero(obj.TASK6, name + " " + "Selects the Trigger Source for TASK6 of CLA1");
    all_zero &= check_zero(obj.TASK7, name + " " + "Selects the Trigger Source for TASK7 of CLA1");
    all_zero &= check_zero(obj.TASK8, name + " " + "Selects the Trigger Source for TASK8 of CLA1");

    return all_zero;
}

bool check_zero(const CLA1TASKSRCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DMACHSRCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CH1, name + " " + "Selects the Trigger and Sync Source CH1 of DMA");
    all_zero &= check_zero(obj.CH2, name + " " + "Selects the Trigger and Sync Source CH2 of DMA");
    all_zero &= check_zero(obj.CH3, name + " " + "Selects the Trigger and Sync Source CH3 of DMA");
    all_zero &= check_zero(obj.CH4, name + " " + "Selects the Trigger and Sync Source CH4 of DMA");

    return all_zero;
}

bool check_zero(const DMACHSRCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DMACHSRCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CH5, name + " " + "Selects the Trigger and Sync Source CH5 of DMA");
    all_zero &= check_zero(obj.CH6, name + " " + "Selects the Trigger and Sync Source CH6 of DMA");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DMACHSRCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DMA_CLA_SRC_SEL_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLA1TASKSRCSELLOCK, name + " " + "CLA1 Task Trigger Source Select Lock Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.DMACHSRCSELLOCK, name + " " + "DMA Channel Triger Source Select Lock Register");
    all_zero &= check_zero(obj.CLA1TASKSRCSEL1, name + " " + "CLA1 Task Trigger Source Select Register-1");
    all_zero &= check_zero(obj.CLA1TASKSRCSEL2, name + " " + "CLA1 Task Trigger Source Select Register-2");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.DMACHSRCSEL1, name + " " + "DMA Channel Trigger Source Select Register-1");
    all_zero &= check_zero(obj.DMACHSRCSEL2, name + " " + "DMA Channel Trigger Source Select Register-2");

    return all_zero;
}

bool check_zero(const SYNCSELECT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EPWM4SYNCIN, name + " " + "Selects Sync Input Source for EPWM4");
    all_zero &= check_zero(obj.EPWM7SYNCIN, name + " " + "Selects Sync Input Source for EPWM7");
    all_zero &= check_zero(obj.EPWM10SYNCIN, name + " " + "Selects Sync Input Source for EPWM10");
    all_zero &= check_zero(obj.ECAP1SYNCIN, name + " " + "Selects Sync Input Source for ECAP1");
    all_zero &= check_zero(obj.ECAP4SYNCIN, name + " " + "Selects Sync Input Source for ECAP4");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SYNCOUT, name + " " + "Select Syncout Source");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYNCSELECT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCOUTSELECT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PWM1SOCAEN, name + " " + "PWM1SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM2SOCAEN, name + " " + "PWM2SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM3SOCAEN, name + " " + "PWM3SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM4SOCAEN, name + " " + "PWM4SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM5SOCAEN, name + " " + "PWM5SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM6SOCAEN, name + " " + "PWM6SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM7SOCAEN, name + " " + "PWM7SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM8SOCAEN, name + " " + "PWM8SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM9SOCAEN, name + " " + "PWM9SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM10SOCAEN, name + " " + "PWM10SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM11SOCAEN, name + " " + "PWM11SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.PWM12SOCAEN, name + " " + "PWM12SOCAEN Enable for ADCSOCAO");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PWM1SOCBEN, name + " " + "PWM1SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM2SOCBEN, name + " " + "PWM2SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM3SOCBEN, name + " " + "PWM3SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM4SOCBEN, name + " " + "PWM4SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM5SOCBEN, name + " " + "PWM5SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM6SOCBEN, name + " " + "PWM6SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM7SOCBEN, name + " " + "PWM7SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM8SOCBEN, name + " " + "PWM8SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM9SOCBEN, name + " " + "PWM9SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM10SOCBEN, name + " " + "PWM10SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM11SOCBEN, name + " " + "PWM11SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.PWM12SOCBEN, name + " " + "PWM12SOCBEN Enable for ADCSOCBO");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOCOUTSELECT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYNCSOCLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SYNCSELECT, name + " " + "SYNCSEL Register Lock bit");
    all_zero &= check_zero(obj.ADCSOCOUTSELECT, name + " " + "ADCSOCOUTSELECT Register Lock bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SYNCSOCLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SYNC_SOC_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SYNCSELECT, name + " " + "Sync Input and Output Select Register");
    all_zero &= check_zero(obj.ADCSOCOUTSELECT, name + " " + "External ADC (Off Chip) SOC Select Register");
    all_zero &= check_zero(obj.SYNCSOCLOCK, name + " " + "SYNCSEL and EXTADCSOC Select Lock register");

    return all_zero;
}

bool check_zero(const TBCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CTRMODE, name + " " + "Counter Mode");
    all_zero &= check_zero(obj.PHSEN, name + " " + "Phase Load Enable");
    all_zero &= check_zero(obj.PRDLD, name + " " + "Active Period Load");
    all_zero &= check_zero(obj.SYNCOSEL, name + " " + "Sync Output Select");
    all_zero &= check_zero(obj.SWFSYNC, name + " " + "Software Force Sync Pulse");
    all_zero &= check_zero(obj.HSPCLKDIV, name + " " + "High Speed TBCLK Pre-scaler");
    all_zero &= check_zero(obj.CLKDIV, name + " " + "Time Base Clock Pre-scaler");
    all_zero &= check_zero(obj.PHSDIR, name + " " + "Phase Direction Bit");
    all_zero &= check_zero(obj.FREE_SOFT, name + " " + "Emulation Mode Bits");

    return all_zero;
}

bool check_zero(const TBCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TBCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.OSHTSYNCMODE, name + " " + "One shot sync mode");
    all_zero &= check_zero(obj.OSHTSYNC, name + " " + "One shot sync");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.SYNCOSELX, name + " " + "Syncout selection");
    all_zero &= check_zero(obj.PRDLDSYNC, name + " " + "PRD Shadow to Active Load on SYNC Event");

    return all_zero;
}

bool check_zero(const TBCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TBSTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CTRDIR, name + " " + "Counter Direction Status");
    all_zero &= check_zero(obj.SYNCI, name + " " + "External Input Sync Status");
    all_zero &= check_zero(obj.CTRMAX, name + " " + "Counter Max Latched Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TBSTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CMPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOADAMODE, name + " " + "Active Compare A Load");
    all_zero &= check_zero(obj.LOADBMODE, name + " " + "Active Compare B Load");
    all_zero &= check_zero(obj.SHDWAMODE, name + " " + "Compare A Register Block Operating Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SHDWBMODE, name + " " + "Compare B Register Block Operating Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SHDWAFULL, name + " " + "Compare A Shadow Register Full Status");
    all_zero &= check_zero(obj.SHDWBFULL, name + " " + "Compare B Shadow Register Full Status");
    all_zero &= check_zero(obj.LOADASYNC, name + " " + "Active Compare A Load on SYNC");
    all_zero &= check_zero(obj.LOADBSYNC, name + " " + "Active Compare B Load on SYNC");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CMPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CMPCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOADCMODE, name + " " + "Active Compare C Load");
    all_zero &= check_zero(obj.LOADDMODE, name + " " + "Active Compare D load");
    all_zero &= check_zero(obj.SHDWCMODE, name + " " + "Compare C Block Operating Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SHDWDMODE, name + " " + "Compare D Block Operating Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOADCSYNC, name + " " + "Active Compare C Load on SYNC");
    all_zero &= check_zero(obj.LOADDSYNC, name + " " + "Active Compare D Load on SYNC");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CMPCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OUT_MODE, name + " " + "Dead Band Output Mode Control");
    all_zero &= check_zero(obj.POLSEL, name + " " + "Polarity Select Control");
    all_zero &= check_zero(obj.IN_MODE, name + " " + "Dead Band Input Select Mode Control");
    all_zero &= check_zero(obj.LOADREDMODE, name + " " + "Active DBRED Load Mode");
    all_zero &= check_zero(obj.LOADFEDMODE, name + " " + "Active DBFED Load Mode");
    all_zero &= check_zero(obj.SHDWDBREDMODE, name + " " + "DBRED Block Operating Mode");
    all_zero &= check_zero(obj.SHDWDBFEDMODE, name + " " + "DBFED Block Operating Mode");
    all_zero &= check_zero(obj.OUTSWAP, name + " " + "Dead Band Output Swap Control");
    all_zero &= check_zero(obj.DEDB_MODE, name + " " + "Dead Band Dual-Edge B Mode Control");
    all_zero &= check_zero(obj.HALFCYCLE, name + " " + "Half Cycle Clocking Enable");

    return all_zero;
}

bool check_zero(const DBCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOADDBCTLMODE, name + " " + "DBCTL Load from Shadow Mode Select");
    all_zero &= check_zero(obj.SHDWDBCTLMODE, name + " " + "DBCTL Load mode Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DBCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LDAQAMODE, name + " " + "Action Qualifier A Load Select");
    all_zero &= check_zero(obj.LDAQBMODE, name + " " + "Action Qualifier B Load Select");
    all_zero &= check_zero(obj.SHDWAQAMODE, name + " " + "Action Qualifer A Operating Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SHDWAQBMODE, name + " " + "Action Qualifier B Operating Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.LDAQASYNC, name + " " + "AQCTLA Register Load on SYNC");
    all_zero &= check_zero(obj.LDAQBSYNC, name + " " + "AQCTLB Register Load on SYNC");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQTSRCSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.T1SEL, name + " " + "T1 Event Source Select Bits");
    all_zero &= check_zero(obj.T2SEL, name + " " + "T2 Event Source Select Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQTSRCSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PCCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CHPEN, name + " " + "PWM chopping enable");
    all_zero &= check_zero(obj.OSHTWTH, name + " " + "One-shot pulse width");
    all_zero &= check_zero(obj.CHPFREQ, name + " " + "Chopping clock frequency");
    all_zero &= check_zero(obj.CHPDUTY, name + " " + "Chopping clock Duty cycle");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PCCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const VCAPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.VCAPE, name + " " + "Valley  Capture mode");
    all_zero &= check_zero(obj.VCAPSTART, name + " " + "Valley  Capture Start");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "Capture Trigger Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.VDELAYDIV, name + " " + "Valley Delay Mode Divide Enable");
    all_zero &= check_zero(obj.EDGEFILTDLYSEL, name + " " + "Valley Switching Mode Delay Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const VCAPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const VCNTCFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.STARTEDGE, name + " " + "Counter Start Edge Selection");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.STARTEDGESTS, name + " " + "Start Edge Status Bit");
    all_zero &= check_zero(obj.STOPEDGE, name + " " + "Counter Start Edge Selection");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.STOPEDGESTS, name + " " + "Stop Edge Status Bit");

    return all_zero;
}

bool check_zero(const VCNTCFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const HRCNFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EDGMODE, name + " " + "ePWMxA Edge Mode Select Bits");
    all_zero &= check_zero(obj.CTLMODE, name + " " + "ePWMxA Control Mode Select Bits");
    all_zero &= check_zero(obj.HRLOAD, name + " " + "ePWMxA Shadow Mode Select Bits");
    all_zero &= check_zero(obj.SELOUTB, name + " " + "EPWMB Output Selection Bit");
    all_zero &= check_zero(obj.AUTOCONV, name + " " + "Autoconversion Bit");
    all_zero &= check_zero(obj.SWAPAB, name + " " + "Swap EPWMA and EPWMB Outputs Bit");
    all_zero &= check_zero(obj.EDGMODEB, name + " " + "ePWMxB Edge Mode Select Bits");
    all_zero &= check_zero(obj.CTLMODEB, name + " " + "ePWMxB Control Mode Select Bits");
    all_zero &= check_zero(obj.HRLOADB, name + " " + "ePWMxB Shadow Mode Select Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const HRCNFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const HRPWR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.CALPWRON, name + " " + "Calibration Power On");

    return all_zero;
}

bool check_zero(const HRPWR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const HRMSTEP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HRMSTEP, name + " " + "High Resolution Micro Step Value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const HRMSTEP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const HRCNFG2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EDGMODEDB, name + " " + "Dead-Band Edge-Mode Select Bits");
    all_zero &= check_zero(obj.CTLMODEDBRED, name + " " + "DBRED Control Mode Select Bits");
    all_zero &= check_zero(obj.CTLMODEDBFED, name + " " + "DBFED Control Mode Select Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const HRCNFG2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const HRPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.HRPE, name + " " + "High Resolution Period Enable");
    all_zero &= check_zero(obj.PWMSYNCSEL, name + " " + "EPWMSYNCPER Source Select");
    all_zero &= check_zero(obj.TBPHSHRLOADE, name + " " + "TBPHSHR Load Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PWMSYNCSELX, name + " " + "EPWMSYNCPER Extended Source Select Bit:");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const HRPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRREM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRREM, name + " " + "HRPWM Remainder Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TRREM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GLDCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GLD, name + " " + "Global Shadow to Active load event control");
    all_zero &= check_zero(obj.GLDMODE, name + " " + "Shadow to Active Global Load Pulse Selection");
    all_zero &= check_zero(obj.OSHTMODE, name + " " + "One Shot Load mode control bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GLDPRD, name + " " + "Global Load Strobe Period Select Register");
    all_zero &= check_zero(obj.GLDCNT, name + " " + "Global Load Strobe Counter Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GLDCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GLDCFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TBPRD_TBPRDHR, name + " " + "Global load event configuration for TBPRD:TBPRDHR");
    all_zero &= check_zero(obj.CMPA_CMPAHR, name + " " + "Global load event configuration for CMPA:CMPAHR");
    all_zero &= check_zero(obj.CMPB_CMPBHR, name + " " + "Global load event configuration for CMPB:CMPBHR");
    all_zero &= check_zero(obj.CMPC, name + " " + "Global load event configuration for CMPC");
    all_zero &= check_zero(obj.CMPD, name + " " + "Global load event configuration for CMPD");
    all_zero &= check_zero(obj.DBRED_DBREDHR, name + " " + "Global load event configuration for DBRED:DBREDHR");
    all_zero &= check_zero(obj.DBFED_DBFEDHR, name + " " + "Global load event configuration for DBFED:DBFEDHR");
    all_zero &= check_zero(obj.DBCTL, name + " " + "Global load event configuration for DBCTL");
    all_zero &= check_zero(obj.AQCTLA_AQCTLA2, name + " " + "Global load event configuration for AQCTLA/A2");
    all_zero &= check_zero(obj.AQCTLB_AQCTLB2, name + " " + "Global load event configuration for AQCTLB/B2");
    all_zero &= check_zero(obj.AQCSFRC, name + " " + "Global load event configuration for AQCSFRC");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GLDCFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EPWMXLINK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TBPRDLINK, name + " " + "TBPRD:TBPRDHR  Link");
    all_zero &= check_zero(obj.CMPALINK, name + " " + "CMPA:CMPAHR Link");
    all_zero &= check_zero(obj.CMPBLINK, name + " " + "CMPB:CMPBHR Link");
    all_zero &= check_zero(obj.CMPCLINK, name + " " + "CMPC Link");
    all_zero &= check_zero(obj.CMPDLINK, name + " " + "CMPD Link");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GLDCTL2LINK, name + " " + "GLDCTL2 Link");

    return all_zero;
}

bool check_zero(const EPWMXLINK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCTLA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ZRO, name + " " + "Action Counter = Zero");
    all_zero &= check_zero(obj.PRD, name + " " + "Action Counter = Period");
    all_zero &= check_zero(obj.CAU, name + " " + "Action Counter = Compare A Up");
    all_zero &= check_zero(obj.CAD, name + " " + "Action Counter = Compare A Down");
    all_zero &= check_zero(obj.CBU, name + " " + "Action Counter = Compare B Up");
    all_zero &= check_zero(obj.CBD, name + " " + "Action Counter = Compare B Down");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCTLA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCTLA2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.T1U, name + " " + "Action when event occurs on T1 in UP-Count");
    all_zero &= check_zero(obj.T1D, name + " " + "Action when event occurs on T1 in DOWN-Count");
    all_zero &= check_zero(obj.T2U, name + " " + "Action when event occurs on T2 in UP-Count");
    all_zero &= check_zero(obj.T2D, name + " " + "Action when event occurs on T2 in DOWN-Count");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCTLA2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCTLB_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ZRO, name + " " + "Action Counter = Zero");
    all_zero &= check_zero(obj.PRD, name + " " + "Action Counter = Period");
    all_zero &= check_zero(obj.CAU, name + " " + "Action Counter = Compare A Up");
    all_zero &= check_zero(obj.CAD, name + " " + "Action Counter = Compare A Down");
    all_zero &= check_zero(obj.CBU, name + " " + "Action Counter = Compare B Up");
    all_zero &= check_zero(obj.CBD, name + " " + "Action Counter = Compare B Down");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCTLB_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCTLB2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.T1U, name + " " + "Action when event occurs on T1 in UP-Count");
    all_zero &= check_zero(obj.T1D, name + " " + "Action when event occurs on T1 in DOWN-Count");
    all_zero &= check_zero(obj.T2U, name + " " + "Action when event occurs on T2 in UP-Count");
    all_zero &= check_zero(obj.T2D, name + " " + "Action when event occurs on T2 in DOWN-Count");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCTLB2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQSFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACTSFA, name + " " + "Action when One-time SW Force A Invoked");
    all_zero &= check_zero(obj.OTSFA, name + " " + "One-time SW Force A Output");
    all_zero &= check_zero(obj.ACTSFB, name + " " + "Action when One-time SW Force B Invoked");
    all_zero &= check_zero(obj.OTSFB, name + " " + "One-time SW Force A Output");
    all_zero &= check_zero(obj.RLDCSF, name + " " + "Reload from Shadow Options");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQSFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AQCSFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CSFA, name + " " + "Continuous Software Force on output A");
    all_zero &= check_zero(obj.CSFB, name + " " + "Continuous Software Force on output B");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AQCSFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBREDHR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.DBREDHR, name + " " + "DBREDHR High Resolution Bits");

    return all_zero;
}

bool check_zero(const DBREDHR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBRED_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DBRED, name + " " + "Rising edge delay value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DBRED_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBFEDHR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.DBFEDHR, name + " " + "DBFEDHR High Resolution Bits");

    return all_zero;
}

bool check_zero(const DBFEDHR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DBFED_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DBFED, name + " " + "Falling edge delay value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DBFED_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TBPHS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TBPHSHR, name + " " + "Extension Register for HRPWM Phase (8-bits)");
    all_zero &= check_zero(obj.TBPHS, name + " " + "Phase Offset Register");

    return all_zero;
}

bool check_zero(const TBPHS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CMPA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPAHR, name + " " + "Compare A HRPWM Extension Register");
    all_zero &= check_zero(obj.CMPA, name + " " + "Compare A Register");

    return all_zero;
}

bool check_zero(const CMPA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CMPB_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CMPBHR, name + " " + "Compare B High Resolution Bits");
    all_zero &= check_zero(obj.CMPB, name + " " + "Compare B Register");

    return all_zero;
}

bool check_zero(const CMPB_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GLDCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OSHTLD, name + " " + "Enable reload event in one shot mode");
    all_zero &= check_zero(obj.GFRCLD, name + " " + "Force reload event in one shot mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GLDCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CBC1, name + " " + "TZ1 CBC select");
    all_zero &= check_zero(obj.CBC2, name + " " + "TZ2 CBC select");
    all_zero &= check_zero(obj.CBC3, name + " " + "TZ3 CBC select");
    all_zero &= check_zero(obj.CBC4, name + " " + "TZ4 CBC select");
    all_zero &= check_zero(obj.CBC5, name + " " + "TZ5 CBC select");
    all_zero &= check_zero(obj.CBC6, name + " " + "TZ6 CBC select");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "DCAEVT2 CBC select");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "DCBEVT2 CBC select");
    all_zero &= check_zero(obj.OSHT1, name + " " + "One-shot TZ1 select");
    all_zero &= check_zero(obj.OSHT2, name + " " + "One-shot TZ2 select");
    all_zero &= check_zero(obj.OSHT3, name + " " + "One-shot TZ3 select");
    all_zero &= check_zero(obj.OSHT4, name + " " + "One-shot TZ4 select");
    all_zero &= check_zero(obj.OSHT5, name + " " + "One-shot TZ5 select");
    all_zero &= check_zero(obj.OSHT6, name + " " + "One-shot TZ6 select");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "One-shot DCAEVT1 select");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "One-shot DCBEVT1 select");

    return all_zero;
}

bool check_zero(const TZSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZDCSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Digital Compare Output A Event 1");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Digital Compare Output A Event 2");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Digital Compare Output B Event 1");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Digital Compare Output B Event 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZDCSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TZA, name + " " + "TZ1 to TZ6 Trip Action On EPWMxA");
    all_zero &= check_zero(obj.TZB, name + " " + "TZ1 to TZ6 Trip Action On EPWMxB");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "EPWMxA action on DCAEVT1");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "EPWMxA action on DCAEVT2");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "EPWMxB action on DCBEVT1");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "EPWMxB action on DCBEVT2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TZAU, name + " " + "Trip Action On EPWMxA while Count direction is UP");
    all_zero &= check_zero(obj.TZAD, name + " " + "Trip Action On EPWMxA while Count direction is DOWN");
    all_zero &= check_zero(obj.TZBU, name + " " + "Trip Action On EPWMxB while Count direction is UP");
    all_zero &= check_zero(obj.TZBD, name + " " + "Trip Action On EPWMxB while Count direction is DOWN");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETZE, name + " " + "TZCTL2 Enable");

    return all_zero;
}

bool check_zero(const TZCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCTLDCA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DCAEVT1U, name + " " + "DCAEVT1 Action On EPWMxA while Count direction is UP");
    all_zero &= check_zero(obj.DCAEVT1D, name + " " + "DCAEVT1 Action On EPWMxA while Count direction is DOWN");
    all_zero &= check_zero(obj.DCAEVT2U, name + " " + "DCAEVT2 Action On EPWMxA while Count direction is UP");
    all_zero &= check_zero(obj.DCAEVT2D, name + " " + "DCAEVT2 Action On EPWMxA while Count direction is DOWN");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZCTLDCA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCTLDCB_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DCBEVT1U, name + " " + "DCBEVT1 Action On EPWMxA while Count direction is UP");
    all_zero &= check_zero(obj.DCBEVT1D, name + " " + "DCBEVT1 Action On EPWMxA while Count direction is DOWN");
    all_zero &= check_zero(obj.DCBEVT2U, name + " " + "DCBEVT2 Action On EPWMxA while Count direction is UP");
    all_zero &= check_zero(obj.DCBEVT2D, name + " " + "DCBEVT2 Action On EPWMxA while Count direction is DOWN");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZCTLDCB_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZEINT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CBC, name + " " + "Trip Zones Cycle By Cycle Int Enable");
    all_zero &= check_zero(obj.OST, name + " " + "Trip Zones One Shot Int Enable");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Digital Compare A Event 1 Int Enable");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Digital Compare A Event 2 Int Enable");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Digital Compare B Event 1 Int Enable");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Digital Compare B Event 2 Int Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZEINT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "Global Int Status Flag");
    all_zero &= check_zero(obj.CBC, name + " " + "Trip Zones Cycle By Cycle Flag");
    all_zero &= check_zero(obj.OST, name + " " + "Trip Zones One Shot Flag");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Digital Compare A Event 1 Flag");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Digital Compare A Event 2 Flag");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Digital Compare B Event 1 Flag");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Digital Compare B Event 2 Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCBCFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CBC1, name + " " + "Latched Status Flag for CBC1 Trip Latch");
    all_zero &= check_zero(obj.CBC2, name + " " + "Latched Status Flag for CBC2 Trip Latch");
    all_zero &= check_zero(obj.CBC3, name + " " + "Latched Status Flag for CBC3 Trip Latch");
    all_zero &= check_zero(obj.CBC4, name + " " + "Latched Status Flag for CBC4 Trip Latch");
    all_zero &= check_zero(obj.CBC5, name + " " + "Latched Status Flag for CBC5 Trip Latch");
    all_zero &= check_zero(obj.CBC6, name + " " + "Latched Status Flag for CBC6 Trip Latch");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Latched Status Flag for Digital Compare Output A Event 2");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Latched Status Flag for Digital Compare Output B Event 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZCBCFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZOSTFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OST1, name + " " + "Latched Status Flag for OST1 Trip Latch");
    all_zero &= check_zero(obj.OST2, name + " " + "Latched Status Flag for OST2 Trip Latch");
    all_zero &= check_zero(obj.OST3, name + " " + "Latched Status Flag for OST3 Trip Latch");
    all_zero &= check_zero(obj.OST4, name + " " + "Latched Status Flag for OST4 Trip Latch");
    all_zero &= check_zero(obj.OST5, name + " " + "Latched Status Flag for OST5 Trip Latch");
    all_zero &= check_zero(obj.OST6, name + " " + "Latched Status Flag for OST6 Trip Latch");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Latched Status Flag for Digital Compare Output A Event 1");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Latched Status Flag for Digital Compare Output B Event 1");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZOSTFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "Global Interrupt Clear Flag");
    all_zero &= check_zero(obj.CBC, name + " " + "Cycle-By-Cycle Flag Clear");
    all_zero &= check_zero(obj.OST, name + " " + "One-Shot Flag Clear");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "DCAVET1 Flag Clear");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "DCAEVT2 Flag Clear");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "DCBEVT1 Flag Clear");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "DCBEVT2 Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CBCPULSE, name + " " + "Clear Pulse for CBC Trip Latch");

    return all_zero;
}

bool check_zero(const TZCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZCBCCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CBC1, name + " " + "Clear Flag for Cycle-By-Cycle (CBC1) Trip Latch");
    all_zero &= check_zero(obj.CBC2, name + " " + "Clear Flag for Cycle-By-Cycle (CBC2) Trip Latch");
    all_zero &= check_zero(obj.CBC3, name + " " + "Clear Flag for Cycle-By-Cycle (CBC3) Trip Latch");
    all_zero &= check_zero(obj.CBC4, name + " " + "Clear Flag for Cycle-By-Cycle (CBC4) Trip Latch");
    all_zero &= check_zero(obj.CBC5, name + " " + "Clear Flag for Cycle-By-Cycle (CBC5) Trip Latch");
    all_zero &= check_zero(obj.CBC6, name + " " + "Clear Flag for Cycle-By-Cycle (CBC6) Trip Latch");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Clear Flag forDCAEVT2 selected for CBC");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Clear Flag for DCBEVT2 selected for CBC");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZCBCCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZOSTCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OST1, name + " " + "Clear Flag for Oneshot (OST1) Trip Latch");
    all_zero &= check_zero(obj.OST2, name + " " + "Clear Flag for Oneshot (OST2) Trip Latch");
    all_zero &= check_zero(obj.OST3, name + " " + "Clear Flag for Oneshot (OST3) Trip Latch");
    all_zero &= check_zero(obj.OST4, name + " " + "Clear Flag for Oneshot (OST4) Trip Latch");
    all_zero &= check_zero(obj.OST5, name + " " + "Clear Flag for Oneshot (OST5) Trip Latch");
    all_zero &= check_zero(obj.OST6, name + " " + "Clear Flag for Oneshot (OST6) Trip Latch");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Clear Flag for DCAEVT1 selected for OST");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Clear Flag for DCBEVT1 selected for OST");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZOSTCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TZFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CBC, name + " " + "Force Trip Zones Cycle By Cycle Event");
    all_zero &= check_zero(obj.OST, name + " " + "Force Trip Zones One Shot Event");
    all_zero &= check_zero(obj.DCAEVT1, name + " " + "Force Digital Compare A Event 1");
    all_zero &= check_zero(obj.DCAEVT2, name + " " + "Force Digital Compare A Event 2");
    all_zero &= check_zero(obj.DCBEVT1, name + " " + "Force Digital Compare B Event 1");
    all_zero &= check_zero(obj.DCBEVT2, name + " " + "Force Digital Compare B Event 2");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TZFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTSEL, name + " " + "EPWMxINTn Select");
    all_zero &= check_zero(obj.INTEN, name + " " + "EPWMxINTn Enable");
    all_zero &= check_zero(obj.SOCASELCMP, name + " " + "EPWMxSOCA Compare Select");
    all_zero &= check_zero(obj.SOCBSELCMP, name + " " + "EPWMxSOCB Compare Select");
    all_zero &= check_zero(obj.INTSELCMP, name + " " + "EPWMxINT Compare Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOCASEL, name + " " + "Start of Conversion A Select");
    all_zero &= check_zero(obj.SOCAEN, name + " " + "Start of Conversion A Enable");
    all_zero &= check_zero(obj.SOCBSEL, name + " " + "Start of Conversion B Select");
    all_zero &= check_zero(obj.SOCBEN, name + " " + "Start of Conversion B Enable");

    return all_zero;
}

bool check_zero(const ETSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETPS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTPRD, name + " " + "EPWMxINTn Period Select");
    all_zero &= check_zero(obj.INTCNT, name + " " + "EPWMxINTn Counter Register");
    all_zero &= check_zero(obj.INTPSSEL, name + " " + "EPWMxINTn Pre-Scale Selection Bits");
    all_zero &= check_zero(obj.SOCPSSEL, name + " " + "EPWMxSOC A/B  Pre-Scale Selection Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOCAPRD, name + " " + "EPWMxSOCA Period Select");
    all_zero &= check_zero(obj.SOCACNT, name + " " + "EPWMxSOCA Counter Register");
    all_zero &= check_zero(obj.SOCBPRD, name + " " + "EPWMxSOCB Period Select");
    all_zero &= check_zero(obj.SOCBCNT, name + " " + "EPWMxSOCB Counter");

    return all_zero;
}

bool check_zero(const ETPS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "EPWMxINTn Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOCA, name + " " + "EPWMxSOCA Flag");
    all_zero &= check_zero(obj.SOCB, name + " " + "EPWMxSOCB Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ETFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "EPWMxINTn Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOCA, name + " " + "EPWMxSOCA Clear");
    all_zero &= check_zero(obj.SOCB, name + " " + "EPWMxSOCB Clear");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ETCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETFRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT, name + " " + "EPWMxINTn Force");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOCA, name + " " + "EPWMxSOCA Force");
    all_zero &= check_zero(obj.SOCB, name + " " + "EPWMxSOCB Force");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ETFRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETINTPS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTPRD2, name + " " + "EPWMxINTn Period Select");
    all_zero &= check_zero(obj.INTCNT2, name + " " + "EPWMxINTn Counter Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ETINTPS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETSOCPS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOCAPRD2, name + " " + "EPWMxSOCA Period Select");
    all_zero &= check_zero(obj.SOCACNT2, name + " " + "EPWMxSOCA Counter Register");
    all_zero &= check_zero(obj.SOCBPRD2, name + " " + "EPWMxSOCB Period Select");
    all_zero &= check_zero(obj.SOCBCNT2, name + " " + "EPWMxSOCB Counter Register");

    return all_zero;
}

bool check_zero(const ETSOCPS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETCNTINITCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INTINITFRC, name + " " + "EPWMxINT Counter Initialization Force");
    all_zero &= check_zero(obj.SOCAINITFRC, name + " " + "EPWMxSOCA Counter Initialization Force");
    all_zero &= check_zero(obj.SOCBINITFRC, name + " " + "EPWMxSOCB Counter Initialization Force");
    all_zero &= check_zero(obj.INTINITEN, name + " " + "EPWMxINT Counter Initialization Enable");
    all_zero &= check_zero(obj.SOCAINITEN, name + " " + "EPWMxSOCA Counter Initialization Enable");
    all_zero &= check_zero(obj.SOCBINITEN, name + " " + "EPWMxSOCB Counter Initialization Enable");

    return all_zero;
}

bool check_zero(const ETCNTINITCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ETCNTINIT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTINIT, name + " " + "EPWMxINT Counter Initialization Bits");
    all_zero &= check_zero(obj.SOCAINIT, name + " " + "EPWMxSOCA Counter Initialization Bits");
    all_zero &= check_zero(obj.SOCBINIT, name + " " + "EPWMxSOCB Counter Initialization Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ETCNTINIT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCTRIPSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DCAHCOMPSEL, name + " " + "Digital Compare A High COMP Input Select");
    all_zero &= check_zero(obj.DCALCOMPSEL, name + " " + "Digital Compare A Low COMP Input Select");
    all_zero &= check_zero(obj.DCBHCOMPSEL, name + " " + "Digital Compare B High COMP Input Select");
    all_zero &= check_zero(obj.DCBLCOMPSEL, name + " " + "Digital Compare B Low COMP Input Select");

    return all_zero;
}

bool check_zero(const DCTRIPSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCACTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EVT1SRCSEL, name + " " + "DCAEVT1 Source Signal");
    all_zero &= check_zero(obj.EVT1FRCSYNCSEL, name + " " + "DCAEVT1 Force Sync Signal");
    all_zero &= check_zero(obj.EVT1SOCE, name + " " + "DCAEVT1 SOC Enable");
    all_zero &= check_zero(obj.EVT1SYNCE, name + " " + "DCAEVT1 SYNC Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EVT2SRCSEL, name + " " + "DCAEVT2 Source Signal");
    all_zero &= check_zero(obj.EVT2FRCSYNCSEL, name + " " + "DCAEVT2 Force Sync Signal");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCACTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCBCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EVT1SRCSEL, name + " " + "DCBEVT1 Source Signal");
    all_zero &= check_zero(obj.EVT1FRCSYNCSEL, name + " " + "DCBEVT1 Force Sync Signal");
    all_zero &= check_zero(obj.EVT1SOCE, name + " " + "DCBEVT1 SOC Enable");
    all_zero &= check_zero(obj.EVT1SYNCE, name + " " + "DCBEVT1 SYNC Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EVT2SRCSEL, name + " " + "DCBEVT2 Source Signal");
    all_zero &= check_zero(obj.EVT2FRCSYNCSEL, name + " " + "DCBEVT2 Force Sync Signal");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCBCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCFCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SRCSEL, name + " " + "Filter Block Signal Source Select");
    all_zero &= check_zero(obj.BLANKE, name + " " + "Blanking Enable/Disable");
    all_zero &= check_zero(obj.BLANKINV, name + " " + "Blanking Window Inversion");
    all_zero &= check_zero(obj.PULSESEL, name + " " + "Pulse Select for Blanking & Capture Alignment");
    all_zero &= check_zero(obj.EDGEFILTSEL, name + " " + "Edge Filter Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.EDGEMODE, name + " " + "Edge Mode");
    all_zero &= check_zero(obj.EDGECOUNT, name + " " + "Edge Count");
    all_zero &= check_zero(obj.EDGESTATUS, name + " " + "Edge Status");

    return all_zero;
}

bool check_zero(const DCFCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCCAPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CAPE, name + " " + "Counter Capture Enable");
    all_zero &= check_zero(obj.SHDWMODE, name + " " + "Counter Capture Mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CAPSTS, name + " " + "Latched Status Flag for Capture Event");
    all_zero &= check_zero(obj.CAPCLR, name + " " + "DC Capture Latched Status Clear Flag");
    all_zero &= check_zero(obj.CAPMODE, name + " " + "Counter Capture Mode");

    return all_zero;
}

bool check_zero(const DCCAPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCAHTRIPSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIPINPUT1, name + " " + "Trip Input 1 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT2, name + " " + "Trip Input 2 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT3, name + " " + "Trip Input 3 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT4, name + " " + "Trip Input 4 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT5, name + " " + "Trip Input 5 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT6, name + " " + "Trip Input 6 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT7, name + " " + "Trip Input 7 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT8, name + " " + "Trip Input 8 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT9, name + " " + "Trip Input 9 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT10, name + " " + "Trip Input 10 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT11, name + " " + "Trip Input 11 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT12, name + " " + "Trip Input 12 Select to DCAH Mux");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPINPUT14, name + " " + "Trip Input 14 Select to DCAH Mux");
    all_zero &= check_zero(obj.TRIPINPUT15, name + " " + "Trip Input 15 Select to DCAH Mux");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCAHTRIPSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCALTRIPSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIPINPUT1, name + " " + "Trip Input 1 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT2, name + " " + "Trip Input 2 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT3, name + " " + "Trip Input 3 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT4, name + " " + "Trip Input 4 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT5, name + " " + "Trip Input 5 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT6, name + " " + "Trip Input 6 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT7, name + " " + "Trip Input 7 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT8, name + " " + "Trip Input 8 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT9, name + " " + "Trip Input 9 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT10, name + " " + "Trip Input 10 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT11, name + " " + "Trip Input 11 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT12, name + " " + "Trip Input 12 Select to DCAL Mux");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPINPUT14, name + " " + "Trip Input 14 Select to DCAL Mux");
    all_zero &= check_zero(obj.TRIPINPUT15, name + " " + "Trip Input 15 Select to DCAL Mux");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCALTRIPSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCBHTRIPSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIPINPUT1, name + " " + "Trip Input 1 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT2, name + " " + "Trip Input 2 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT3, name + " " + "Trip Input 3 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT4, name + " " + "Trip Input 4 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT5, name + " " + "Trip Input 5 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT6, name + " " + "Trip Input 6 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT7, name + " " + "Trip Input 7 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT8, name + " " + "Trip Input 8 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT9, name + " " + "Trip Input 9 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT10, name + " " + "Trip Input 10 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT11, name + " " + "Trip Input 11 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT12, name + " " + "Trip Input 12 Select to DCBH Mux");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPINPUT14, name + " " + "Trip Input 14 Select to DCBH Mux");
    all_zero &= check_zero(obj.TRIPINPUT15, name + " " + "Trip Input 15 Select to DCBH Mux");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCBHTRIPSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DCBLTRIPSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIPINPUT1, name + " " + "Trip Input 1 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT2, name + " " + "Trip Input 2 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT3, name + " " + "Trip Input 3 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT4, name + " " + "Trip Input 4 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT5, name + " " + "Trip Input 5 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT6, name + " " + "Trip Input 6 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT7, name + " " + "Trip Input 7 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT8, name + " " + "Trip Input 8 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT9, name + " " + "Trip Input 9 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT10, name + " " + "Trip Input 10 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT11, name + " " + "Trip Input 11 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT12, name + " " + "Trip Input 12 Select to DCBL Mux");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPINPUT14, name + " " + "Trip Input 14 Select to DCBL Mux");
    all_zero &= check_zero(obj.TRIPINPUT15, name + " " + "Trip Input 15 Select to DCBL Mux");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DCBLTRIPSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EPWM_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TBCTL, name + " " + "Time Base Control Register");
    all_zero &= check_zero(obj.TBCTL2, name + " " + "Time Base Control Register 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TBCTR, name + " " + "Time Base Counter Register");
    all_zero &= check_zero(obj.TBSTS, name + " " + "Time Base Status Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CMPCTL, name + " " + "Counter Compare Control Register");
    all_zero &= check_zero(obj.CMPCTL2, name + " " + "Counter Compare Control Register 2");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.DBCTL, name + " " + "Dead-Band Generator Control Register");
    all_zero &= check_zero(obj.DBCTL2, name + " " + "Dead-Band Generator Control Register 2");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.AQCTL, name + " " + "Action Qualifier Control Register");
    all_zero &= check_zero(obj.AQTSRCSEL, name + " " + "Action Qualifier Trigger Event Source Select Register");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.PCCTL, name + " " + "PWM Chopper Control Register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.VCAPCTL, name + " " + "Valley Capture Control Register");
    all_zero &= check_zero(obj.VCNTCFG, name + " " + "Valley Counter Config Register");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.HRCNFG, name + " " + "HRPWM Configuration Register");
    all_zero &= check_zero(obj.HRPWR, name + " " + "HRPWM Power Register");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.HRMSTEP, name + " " + "HRPWM MEP Step Register");
    all_zero &= check_zero(obj.HRCNFG2, name + " " + "HRPWM Configuration 2 Register");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.HRPCTL, name + " " + "High Resolution Period Control Register");
    all_zero &= check_zero(obj.TRREM, name + " " + "HRPWM High Resolution Remainder Register");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.GLDCTL, name + " " + "Global PWM Load Control Register");
    all_zero &= check_zero(obj.GLDCFG, name + " " + "Global PWM Load Config Register");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.EPWMXLINK, name + " " + "EPWMx Link Register");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.AQCTLA, name + " " + "Action Qualifier Control Register For Output A");
    all_zero &= check_zero(obj.AQCTLA2, name + " " + "Additional Action Qualifier Control Register For Output A");
    all_zero &= check_zero(obj.AQCTLB, name + " " + "Action Qualifier Control Register For Output B");
    all_zero &= check_zero(obj.AQCTLB2, name + " " + "Additional Action Qualifier Control Register For Output B");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.AQSFRC, name + " " + "Action Qualifier Software Force Register");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.AQCSFRC, name + " " + "Action Qualifier Continuous S/W Force Register");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.DBREDHR,
                           name + " " + "Dead-Band Generator Rising Edge Delay High Resolution Mirror Register");
    all_zero &= check_zero(
        obj.DBRED, name + " " + "Dead-Band Generator Rising Edge Delay High Resolution Mirror Register");
    all_zero &= check_zero(obj.DBFEDHR, name + " " + "Dead-Band Generator Falling Edge Delay High Resolution Register");
    all_zero &= check_zero(obj.DBFED, name + " " + "Dead-Band Generator Falling Edge Delay Count Register");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.TBPHS, name + " " + "Time Base Phase High");
    all_zero &= check_zero(obj.TBPRDHR, name + " " + "Time Base Period High Resolution Register");
    all_zero &= check_zero(obj.TBPRD, name + " " + "Time Base Period Register");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.CMPA, name + " " + "Counter Compare A Register");
    all_zero &= check_zero(obj.CMPB, name + " " + "Compare B Register");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.CMPC, name + " " + "Counter Compare C Register");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.CMPD, name + " " + "Counter Compare D Register");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.GLDCTL2, name + " " + "Global PWM Load Control Register 2");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.SWVDELVAL, name + " " + "Software Valley Mode Delay Register");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZSEL, name + " " + "Trip Zone Select Register");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZDCSEL, name + " " + "Trip Zone Digital Comparator Select Register");
    all_zero &= check_zero(obj.rsvd24, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZCTL, name + " " + "Trip Zone Control Register");
    all_zero &= check_zero(obj.TZCTL2, name + " " + "Additional Trip Zone Control Register");
    all_zero &= check_zero(obj.TZCTLDCA, name + " " + "Trip Zone Control Register Digital Compare A");
    all_zero &= check_zero(obj.TZCTLDCB, name + " " + "Trip Zone Control Register Digital Compare B");
    all_zero &= check_zero(obj.rsvd25, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZEINT, name + " " + "Trip Zone Enable Interrupt Register");
    all_zero &= check_zero(obj.rsvd26, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZFLG, name + " " + "Trip Zone Flag Register");
    all_zero &= check_zero(obj.TZCBCFLG, name + " " + "Trip Zone CBC Flag Register");
    all_zero &= check_zero(obj.TZOSTFLG, name + " " + "Trip Zone OST Flag Register");
    all_zero &= check_zero(obj.rsvd27, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZCLR, name + " " + "Trip Zone Clear Register");
    all_zero &= check_zero(obj.TZCBCCLR, name + " " + "Trip Zone CBC Clear Register");
    all_zero &= check_zero(obj.TZOSTCLR, name + " " + "Trip Zone OST Clear Register");
    all_zero &= check_zero(obj.rsvd28, name + " " + "Reserved");
    all_zero &= check_zero(obj.TZFRC, name + " " + "Trip Zone Force Register");
    all_zero &= check_zero(obj.rsvd29, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETSEL, name + " " + "Event Trigger Selection Register");
    all_zero &= check_zero(obj.rsvd30, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETPS, name + " " + "Event Trigger Pre-Scale Register");
    all_zero &= check_zero(obj.rsvd31, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETFLG, name + " " + "Event Trigger Flag Register");
    all_zero &= check_zero(obj.rsvd32, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETCLR, name + " " + "Event Trigger Clear Register");
    all_zero &= check_zero(obj.rsvd33, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETFRC, name + " " + "Event Trigger Force Register");
    all_zero &= check_zero(obj.rsvd34, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETINTPS, name + " " + "Event-Trigger Interrupt Pre-Scale Register");
    all_zero &= check_zero(obj.rsvd35, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETSOCPS, name + " " + "Event-Trigger SOC Pre-Scale Register");
    all_zero &= check_zero(obj.rsvd36, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETCNTINITCTL, name + " " + "Event-Trigger Counter Initialization Control Register");
    all_zero &= check_zero(obj.rsvd37, name + " " + "Reserved");
    all_zero &= check_zero(obj.ETCNTINIT, name + " " + "Event-Trigger Counter Initialization Register");
    all_zero &= check_zero(obj.rsvd38, name + " " + "Reserved");
    all_zero &= check_zero(obj.DCTRIPSEL, name + " " + "Digital Compare Trip Select Register");
    all_zero &= check_zero(obj.rsvd39, name + " " + "Reserved");
    all_zero &= check_zero(obj.DCACTL, name + " " + "Digital Compare A Control Register");
    all_zero &= check_zero(obj.DCBCTL, name + " " + "Digital Compare B Control Register");
    all_zero &= check_zero(obj.rsvd40, name + " " + "Reserved");
    all_zero &= check_zero(obj.DCFCTL, name + " " + "Digital Compare Filter Control Register");
    all_zero &= check_zero(obj.DCCAPCTL, name + " " + "Digital Compare Capture Control Register");
    all_zero &= check_zero(obj.DCFOFFSET, name + " " + "Digital Compare Filter Offset Register");
    all_zero &= check_zero(obj.DCFOFFSETCNT, name + " " + "Digital Compare Filter Offset Counter Register");
    all_zero &= check_zero(obj.DCFWINDOW, name + " " + "Digital Compare Filter Window Register");
    all_zero &= check_zero(obj.DCFWINDOWCNT, name + " " + "Digital Compare Filter Window Counter Register");
    all_zero &= check_zero(obj.rsvd41, name + " " + "Reserved");
    all_zero &= check_zero(obj.DCCAP, name + " " + "Digital Compare Counter Capture Register");
    all_zero &= check_zero(obj.rsvd42, name + " " + "Reserved");
    all_zero &= check_zero(obj.DCAHTRIPSEL, name + " " + "Digital Compare AH Trip Select");
    all_zero &= check_zero(obj.DCALTRIPSEL, name + " " + "Digital Compare AL Trip Select");
    all_zero &= check_zero(obj.DCBHTRIPSEL, name + " " + "Digital Compare BH Trip Select");
    all_zero &= check_zero(obj.DCBLTRIPSEL, name + " " + "Digital Compare BL Trip Select");
    all_zero &= check_zero(obj.rsvd43, name + " " + "Reserved");
    all_zero &= check_zero(obj.HWVDELVAL, name + " " + "Hardware Valley Mode Delay Register");
    all_zero &= check_zero(obj.VCNTVAL, name + " " + "Hardware Valley Counter Register");

    return all_zero;
}

bool check_zero(const DACREV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.REV, name + " " + "DAC Revision Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACREV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACREFSEL, name + " " + "DAC Reference Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOADMODE, name + " " + "DACVALA Load Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SYNCSEL, name + " " + "DAC EPWMSYNCPER Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACVALA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVALA, name + " " + "DAC Active Output Code");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACVALA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACVALS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVALS, name + " " + "DAC Shadow Output Code");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACVALS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACOUTEN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACOUTEN, name + " " + "DAC Output Code");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACOUTEN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACCTL, name + " " + "DAC Control Register Lock");
    all_zero &= check_zero(obj.DACVAL, name + " " + "DAC Value Register Lock");
    all_zero &= check_zero(obj.DACOUTEN, name + " " + "DAC Output Enable Register Lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "DAC Register Lock Key");

    return all_zero;
}

bool check_zero(const DACLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACTRIM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFSET_TRIM, name + " " + "DAC Offset Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACTRIM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DAC_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACREV, name + " " + "DAC Revision Register");
    all_zero &= check_zero(obj.DACCTL, name + " " + "DAC Control Register");
    all_zero &= check_zero(obj.DACVALA, name + " " + "DAC Value Register - Active");
    all_zero &= check_zero(obj.DACVALS, name + " " + "DAC Value Register - Shadow");
    all_zero &= check_zero(obj.DACOUTEN, name + " " + "DAC Output Enable Register");
    all_zero &= check_zero(obj.DACLOCK, name + " " + "DAC Lock Register");
    all_zero &= check_zero(obj.DACTRIM, name + " " + "DAC Trim Register");

    return all_zero;
}

bool check_zero(const SCICCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCICHAR, name + " " + "Character length control");
    all_zero &= check_zero(obj.ADDRIDLE_MODE, name + " " + "ADDR/IDLE Mode control");
    all_zero &= check_zero(obj.LOOPBKENA, name + " " + "Loop Back enable");
    all_zero &= check_zero(obj.PARITYENA, name + " " + "Parity enable");
    all_zero &= check_zero(obj.PARITY, name + " " + "Even or Odd Parity");
    all_zero &= check_zero(obj.STOPBITS, name + " " + "Number of Stop Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCICCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCICTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RXENA, name + " " + "SCI receiver enable");
    all_zero &= check_zero(obj.TXENA, name + " " + "SCI transmitter enable");
    all_zero &= check_zero(obj.SLEEP, name + " " + "SCI sleep");
    all_zero &= check_zero(obj.TXWAKE, name + " " + "Transmitter wakeup method");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SWRESET, name + " " + "Software reset");
    all_zero &= check_zero(obj.RXERRINTENA, name + " " + "Receive error interrupt enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCICTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIHBAUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BAUD, name + " " + "SCI 16-bit baud selection Registers SCIHBAUD");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCIHBAUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCILBAUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BAUD, name + " " + "SCI 16-bit baud selection Registers SCILBAUD");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCILBAUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCICTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXINTENA, name + " " + "Transmit __interrupt enable");
    all_zero &= check_zero(obj.RXBKINTENA, name + " " + "Receiver-buffer break enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TXEMPTY, name + " " + "Transmitter empty flag");
    all_zero &= check_zero(obj.TXRDY, name + " " + "Transmitter ready flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCICTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIRXST_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RXWAKE, name + " " + "Receiver wakeup detect flag");
    all_zero &= check_zero(obj.PE, name + " " + "Parity error flag");
    all_zero &= check_zero(obj.OE, name + " " + "Overrun error flag");
    all_zero &= check_zero(obj.FE, name + " " + "Framing error flag");
    all_zero &= check_zero(obj.BRKDT, name + " " + "Break-detect flag");
    all_zero &= check_zero(obj.RXRDY, name + " " + "Receiver ready flag");
    all_zero &= check_zero(obj.RXERROR, name + " " + "Receiver error flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCIRXST_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIRXEMU_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ERXDT, name + " " + "Receive emulation buffer data");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCIRXEMU_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIRXBUF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SAR, name + " " + "Receive Character bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCIFFPE, name + " " + "Receiver error flag");
    all_zero &= check_zero(obj.SCIFFFE, name + " " + "Receiver error flag");

    return all_zero;
}

bool check_zero(const SCIRXBUF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCITXBUF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXDT, name + " " + "Transmit data buffer");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCITXBUF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIFFTX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXFFIL, name + " " + "Interrupt level");
    all_zero &= check_zero(obj.TXFFIENA, name + " " + "Interrupt enable");
    all_zero &= check_zero(obj.TXFFINTCLR, name + " " + "Clear INT flag");
    all_zero &= check_zero(obj.TXFFINT, name + " " + "INT flag");
    all_zero &= check_zero(obj.TXFFST, name + " " + "FIFO status");
    all_zero &= check_zero(obj.TXFIFORESET, name + " " + "FIFO reset");
    all_zero &= check_zero(obj.SCIFFENA, name + " " + "Enhancement enable");
    all_zero &= check_zero(obj.SCIRST, name + " " + "SCI reset rx/tx channels");

    return all_zero;
}

bool check_zero(const SCIFFTX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIFFRX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RXFFIL, name + " " + "Interrupt level");
    all_zero &= check_zero(obj.RXFFIENA, name + " " + "Interrupt enable");
    all_zero &= check_zero(obj.RXFFINTCLR, name + " " + "Clear INT flag");
    all_zero &= check_zero(obj.RXFFINT, name + " " + "INT flag");
    all_zero &= check_zero(obj.RXFFST, name + " " + "FIFO status");
    all_zero &= check_zero(obj.RXFIFORESET, name + " " + "FIFO reset");
    all_zero &= check_zero(obj.RXFFOVRCLR, name + " " + "Clear overflow");
    all_zero &= check_zero(obj.RXFFOVF, name + " " + "FIFO overflow");

    return all_zero;
}

bool check_zero(const SCIFFRX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIFFCT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FFTXDLY, name + " " + "FIFO transmit delay");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CDC, name + " " + "Auto baud mode enable");
    all_zero &= check_zero(obj.ABDCLR, name + " " + "Auto baud clear");
    all_zero &= check_zero(obj.ABD, name + " " + "Auto baud detect");

    return all_zero;
}

bool check_zero(const SCIFFCT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCIPRI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FREESOFT, name + " " + "Emulation modes");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SCIPRI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SCI_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SCICCR, name + " " + "Communications control register");
    all_zero &= check_zero(obj.SCICTL1, name + " " + "Control register 1");
    all_zero &= check_zero(obj.SCIHBAUD, name + " " + "Baud rate (high) register");
    all_zero &= check_zero(obj.SCILBAUD, name + " " + "Baud rate (low) register");
    all_zero &= check_zero(obj.SCICTL2, name + " " + "Control register 2");
    all_zero &= check_zero(obj.SCIRXST, name + " " + "Receive status register");
    all_zero &= check_zero(obj.SCIRXEMU, name + " " + "Receive emulation buffer register");
    all_zero &= check_zero(obj.SCIRXBUF, name + " " + "Receive data buffer");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCITXBUF, name + " " + "Transmit data buffer");
    all_zero &= check_zero(obj.SCIFFTX, name + " " + "FIFO transmit register");
    all_zero &= check_zero(obj.SCIFFRX, name + " " + "FIFO receive register");
    all_zero &= check_zero(obj.SCIFFCT, name + " " + "FIFO control register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SCIPRI, name + " " + "SCI priority control");

    return all_zero;
}

bool check_zero(const I2COAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OAR, name + " " + "I2C Own address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2COAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CIER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ARBL, name + " " + "Arbitration-lost interrupt enable");
    all_zero &= check_zero(obj.NACK, name + " " + "No-acknowledgment interrupt enable");
    all_zero &= check_zero(obj.ARDY, name + " " + "Register-access-ready interrupt enable");
    all_zero &= check_zero(obj.RRDY, name + " " + "Receive-data-ready interrupt enable");
    all_zero &= check_zero(obj.XRDY, name + " " + "Transmit-data-ready interrupt enable");
    all_zero &= check_zero(obj.SCD, name + " " + "Stop condition detected interrupt enable");
    all_zero &= check_zero(obj.AAS, name + " " + "Addressed as slave interrupt enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CIER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CSTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ARBL, name + " " + "Arbitration-lost interrupt flag bit");
    all_zero &= check_zero(obj.NACK, name + " " + "No-acknowledgment interrupt flag bit.");
    all_zero &= check_zero(obj.ARDY, name + " " + "Register-access-ready interrupt flag bit");
    all_zero &= check_zero(obj.RRDY, name + " " + "Receive-data-ready interrupt flag bit.");
    all_zero &= check_zero(obj.XRDY, name + " " + "Transmit-data-ready interrupt flag bit.");
    all_zero &= check_zero(obj.SCD, name + " " + "Stop condition detected bit.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.AD0, name + " " + "Address 0 bits");
    all_zero &= check_zero(obj.AAS, name + " " + "Addressed-as-slave bit");
    all_zero &= check_zero(obj.XSMT, name + " " + "Transmit shift register empty bit.");
    all_zero &= check_zero(obj.RSFULL, name + " " + "Receive shift register full bit.");
    all_zero &= check_zero(obj.BB, name + " " + "Bus busy bit.");
    all_zero &= check_zero(obj.NACKSNT, name + " " + "NACK sent bit.");
    all_zero &= check_zero(obj.SDIR, name + " " + "Slave direction bit");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CSTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CDRR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA, name + " " + "Receive data");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CDRR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CSAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SAR, name + " " + "Slave Address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CSAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CDXR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DATA, name + " " + "Transmit data");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CDXR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CMDR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BC, name + " " + "Bit count bits.");
    all_zero &= check_zero(obj.FDF, name + " " + "Free Data Format");
    all_zero &= check_zero(obj.STB, name + " " + "START Byte Mode");
    all_zero &= check_zero(obj.IRS, name + " " + "I2C Module Reset");
    all_zero &= check_zero(obj.DLB, name + " " + "Digital Loopback Mode");
    all_zero &= check_zero(obj.RM, name + " " + "Repeat Mode");
    all_zero &= check_zero(obj.XA, name + " " + "Expanded Address Mode");
    all_zero &= check_zero(obj.TRX, name + " " + "Transmitter Mode");
    all_zero &= check_zero(obj.MST, name + " " + "Master Mode");
    all_zero &= check_zero(obj.STP, name + " " + "STOP Condition");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.STT, name + " " + "START condition bit");
    all_zero &= check_zero(obj.FREE, name + " " + "Debug Action");
    all_zero &= check_zero(obj.NACKMOD, name + " " + "NACK mode bit");

    return all_zero;
}

bool check_zero(const I2CMDR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CISRC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTCODE, name + " " + "Interrupt code bits.");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.WRITE_ZEROS, name + " " + "Always write all 0s to this field");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CISRC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CEMDR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BC, name + " " + "Backwards compatibility mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CEMDR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CPSC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IPSC, name + " " + "I2C Prescaler Divide Down");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CPSC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CFFTX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXFFIL, name + " " + "Transmit FIFO Interrupt Level");
    all_zero &= check_zero(obj.TXFFIENA, name + " " + "Transmit FIFO Interrupt Enable");
    all_zero &= check_zero(obj.TXFFINTCLR, name + " " + "Transmit FIFO Interrupt Flag Clear");
    all_zero &= check_zero(obj.TXFFINT, name + " " + "Transmit FIFO Interrupt Flag");
    all_zero &= check_zero(obj.TXFFST, name + " " + "Transmit FIFO Status");
    all_zero &= check_zero(obj.TXFFRST, name + " " + "Transmit FIFO Reset");
    all_zero &= check_zero(obj.I2CFFEN, name + " " + "Transmit FIFO Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CFFTX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2CFFRX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RXFFIL, name + " " + "Receive FIFO Interrupt Level");
    all_zero &= check_zero(obj.RXFFIENA, name + " " + "Receive FIFO Interrupt Enable");
    all_zero &= check_zero(obj.RXFFINTCLR, name + " " + "Receive FIFO Interrupt Flag Clear");
    all_zero &= check_zero(obj.RXFFINT, name + " " + "Receive FIFO Interrupt Flag");
    all_zero &= check_zero(obj.RXFFST, name + " " + "Receive FIFO Status");
    all_zero &= check_zero(obj.RXFFRST, name + " " + "Receive FIFO Reset");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const I2CFFRX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const I2C_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.I2COAR, name + " " + "I2C Own address");
    all_zero &= check_zero(obj.I2CIER, name + " " + "I2C Interrupt Enable");
    all_zero &= check_zero(obj.I2CSTR, name + " " + "I2C Status");
    all_zero &= check_zero(obj.I2CCLKL, name + " " + "I2C Clock low-time divider");
    all_zero &= check_zero(obj.I2CCLKH, name + " " + "I2C Clock high-time divider");
    all_zero &= check_zero(obj.I2CCNT, name + " " + "I2C Data count");
    all_zero &= check_zero(obj.I2CDRR, name + " " + "I2C Data receive");
    all_zero &= check_zero(obj.I2CSAR, name + " " + "I2C Slave address");
    all_zero &= check_zero(obj.I2CDXR, name + " " + "I2C Data Transmit");
    all_zero &= check_zero(obj.I2CMDR, name + " " + "I2C Mode");
    all_zero &= check_zero(obj.I2CISRC, name + " " + "I2C Interrupt Source");
    all_zero &= check_zero(obj.I2CEMDR, name + " " + "I2C Extended Mode");
    all_zero &= check_zero(obj.I2CPSC, name + " " + "I2C Prescaler");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.I2CFFTX, name + " " + "I2C FIFO Transmit");
    all_zero &= check_zero(obj.I2CFFRX, name + " " + "I2C FIFO Receive");

    return all_zero;
}

bool check_zero(const CLB_COUNT_RESET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Count Reset Select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Count Reset Select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Count Reset Select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_COUNT_RESET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_COUNT_MODE_1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Counter mode 1 select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Counter mode 1 select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Counter mode 1 select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_COUNT_MODE_1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_COUNT_MODE_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Counter mode 0 select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Counter mode 0 select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Counter mode 0 select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_COUNT_MODE_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_COUNT_EVENT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Counter event select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Counter event select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Counter event select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_COUNT_EVENT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTRA_IN0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "FSM extra ext input select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "FSM extra ext input select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "FSM extra ext input select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTRA_IN0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTERNAL_IN0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "FSM EXT_IN0 select input for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "FSM EXT_IN0 select input for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "FSM EXT_IN0 select input for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTERNAL_IN0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTERNAL_IN1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "FSM EXT_IN1 select input for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "FSM EXT_IN1 select input for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "FSM EXT_IN1 select input for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTERNAL_IN1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTRA_IN1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "FSM extra ext input select 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "FSM extra ext input select 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "FSM extra ext input select 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_FSM_EXTRA_IN1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Select inputs for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Select inputs for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Select inputs for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Select inputs for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Select inputs for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Select inputs for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Select inputs for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Select inputs for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Select inputs for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_0, name + " " + "Select inputs for unit 0");
    all_zero &= check_zero(obj.SEL_1, name + " " + "Select inputs for unit 1");
    all_zero &= check_zero(obj.SEL_2, name + " " + "Select inputs for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LUT4_IN3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_LUT_FN1_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FN0, name + " " + "FSM LUT output function for unit 0");
    all_zero &= check_zero(obj.FN1, name + " " + "FSM LUT output function for unit 1");

    return all_zero;
}

bool check_zero(const CLB_FSM_LUT_FN1_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_LUT_FN2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FN1, name + " " + "FSM LUT output function for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_FSM_LUT_FN2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_FN1_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FN0, name + " " + "LUT4 output function for unit 0");
    all_zero &= check_zero(obj.FN1, name + " " + "LUT4 output function for unit 1");

    return all_zero;
}

bool check_zero(const CLB_LUT4_FN1_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LUT4_FN2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FN1, name + " " + "LUT4 output function for unit 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LUT4_FN2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.S0, name + " " + "FSM next state function for S0");
    all_zero &= check_zero(obj.S1, name + " " + "FSM next state function for S1");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.S0, name + " " + "FSM next state function for S0");
    all_zero &= check_zero(obj.S1, name + " " + "FSM next state function for S1");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.S0, name + " " + "FSM next state function for S0");
    all_zero &= check_zero(obj.S1, name + " " + "FSM next state function for S1");

    return all_zero;
}

bool check_zero(const CLB_FSM_NEXT_STATE_2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_MISC_CONTROL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COUNT_ADD_SHIFT_0, name + " " + "Add/Shift for counter 0");
    all_zero &= check_zero(obj.COUNT_DIR_0, name + " " + "Direction for counter 0");
    all_zero &= check_zero(obj.COUNT_EVENT_CTRL_0, name + " " + "Event control for counter 0");
    all_zero &= check_zero(obj.COUNT_ADD_SHIFT_1, name + " " + "Add/Shift for counter 1");
    all_zero &= check_zero(obj.COUNT_DIR_1, name + " " + "Direction for counter 1");
    all_zero &= check_zero(obj.COUNT_EVENT_CTRL_1, name + " " + "Event control for counter 1");
    all_zero &= check_zero(obj.COUNT_ADD_SHIFT_2, name + " " + "Add/Shift for counter 2");
    all_zero &= check_zero(obj.COUNT_DIR_2, name + " " + "Direction for counter 2");
    all_zero &= check_zero(obj.COUNT_EVENT_CTRL_2, name + " " + "Event control for counter 2");
    all_zero &= check_zero(obj.COUNT_SERIALIZER_0, name + " " + "Serializer enable 0");
    all_zero &= check_zero(obj.COUNT_SERIALIZER_1, name + " " + "Serializer enable 1");
    all_zero &= check_zero(obj.COUNT_SERIALIZER_2, name + " " + "Serializer enable 2");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL0_0, name + " " + "FSM extra_sel0 for 0");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL1_0, name + " " + "FSM extra_sel1 for 0");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL0_1, name + " " + "FSM extra_sel0 for 1");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL1_1, name + " " + "FSM extra_sel1 for 1");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL0_2, name + " " + "FSM extra_sel0 for 2");
    all_zero &= check_zero(obj.FSM_EXTRA_SEL1_2, name + " " + "FSM extra_sel1 for 2");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_MISC_CONTROL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_5_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_5_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.IN0, name + " " + "Select value for IN0 of output LUT");
    all_zero &= check_zero(obj.IN1, name + " " + "Select value for IN1 of output LUT");
    all_zero &= check_zero(obj.IN2, name + " " + "Select value for IN2 of output LUT");
    all_zero &= check_zero(obj.FN, name + " " + "Output function for output LUT");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_OUTPUT_LUT_7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_HLC_EVENT_SEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.EVENT0_SEL, name + " " + "Event Select 0");
    all_zero &= check_zero(obj.EVENT1_SEL, name + " " + "Event Select 1");
    all_zero &= check_zero(obj.EVENT2_SEL, name + " " + "Event Select 2");
    all_zero &= check_zero(obj.EVENT3_SEL, name + " " + "Event Select 3");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_HLC_EVENT_SEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LOGIC_CONFIG_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_COUNT_RESET, name + " " + "Counter Block RESET");
    all_zero &= check_zero(obj.CLB_COUNT_MODE_1, name + " " + "Counter Block MODE_1");
    all_zero &= check_zero(obj.CLB_COUNT_MODE_0, name + " " + "Counter Block MODE_0");
    all_zero &= check_zero(obj.CLB_COUNT_EVENT, name + " " + "Counter Block EVENT");
    all_zero &= check_zero(obj.CLB_FSM_EXTRA_IN0, name + " " + "FSM Extra EXT_IN0");
    all_zero &= check_zero(obj.CLB_FSM_EXTERNAL_IN0, name + " " + "FSM EXT_IN0");
    all_zero &= check_zero(obj.CLB_FSM_EXTERNAL_IN1, name + " " + "FSM_EXT_IN1");
    all_zero &= check_zero(obj.CLB_FSM_EXTRA_IN1, name + " " + "FSM Extra_EXT_IN1");
    all_zero &= check_zero(obj.CLB_LUT4_IN0, name + " " + "LUT4_0/1/2 IN0 input source");
    all_zero &= check_zero(obj.CLB_LUT4_IN1, name + " " + "LUT4_0/1/2 IN1 input source");
    all_zero &= check_zero(obj.CLB_LUT4_IN2, name + " " + "LUT4_0/1/2 IN2 input source");
    all_zero &= check_zero(obj.CLB_LUT4_IN3, name + " " + "LUT4_0/1/2 IN3 input source");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_FSM_LUT_FN1_0, name + " " + "LUT function for FSM Unit 1 and Unit 0");
    all_zero &= check_zero(obj.CLB_FSM_LUT_FN2, name + " " + "LUT function for FSM Unit 2");
    all_zero &= check_zero(obj.CLB_LUT4_FN1_0, name + " " + "LUT function for LUT4 block of Unit 1 and 0");
    all_zero &= check_zero(obj.CLB_LUT4_FN2, name + " " + "LUT function for LUT4 block of Unit 2");
    all_zero &= check_zero(obj.CLB_FSM_NEXT_STATE_0, name + " " + "FSM Next state equations for Unit 0");
    all_zero &= check_zero(obj.CLB_FSM_NEXT_STATE_1, name + " " + "FSM Next state equations for Unit 1");
    all_zero &= check_zero(obj.CLB_FSM_NEXT_STATE_2, name + " " + "FSM Next state equations for Unit 2");
    all_zero &= check_zero(obj.CLB_MISC_CONTROL, name + " " + "Static controls for Ctr,FSM");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_0, name + " " + "Inp Sel, LUT fns for Out0");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_1, name + " " + "Inp Sel, LUT fns for Out1");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_2, name + " " + "Inp Sel, LUT fns for Out2");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_3, name + " " + "Inp Sel, LUT fns for Out3");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_4, name + " " + "Inp Sel, LUT fns for Out4");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_5, name + " " + "Inp Sel, LUT fns for Out5");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_6, name + " " + "Inp Sel, LUT fns for Out6");
    all_zero &= check_zero(obj.CLB_OUTPUT_LUT_7, name + " " + "Inp Sel, LUT fns for Out7");
    all_zero &= check_zero(obj.CLB_HLC_EVENT_SEL, name + " " + "Event Selector register for the High Level controller");

    return all_zero;
}

bool check_zero(const CLB_LOAD_EN_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOAD_EN, name + " " + "Load Enable");
    all_zero &= check_zero(obj.GLOBAL_EN, name + " " + "Global Enable");
    all_zero &= check_zero(obj.STOP, name + " " + "Debug stop control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LOAD_EN_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LOAD_ADDR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADDR, name + " " + "Indirect Address");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LOAD_ADDR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_INPUT_FILTER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FIN0, name + " " + "Input filter control 0");
    all_zero &= check_zero(obj.FIN1, name + " " + "Input filter control 1");
    all_zero &= check_zero(obj.FIN2, name + " " + "Input filter control 2");
    all_zero &= check_zero(obj.FIN3, name + " " + "Input filter control 3");
    all_zero &= check_zero(obj.FIN4, name + " " + "Input filter control 4");
    all_zero &= check_zero(obj.FIN5, name + " " + "Input filter control 5");
    all_zero &= check_zero(obj.FIN6, name + " " + "Input filter control 6");
    all_zero &= check_zero(obj.FIN7, name + " " + "Input filter control 7");
    all_zero &= check_zero(obj.SYNC0, name + " " + "Synchronizer control 0");
    all_zero &= check_zero(obj.SYNC1, name + " " + "Synchronizer control 1");
    all_zero &= check_zero(obj.SYNC2, name + " " + "Synchronizer control 2");
    all_zero &= check_zero(obj.SYNC3, name + " " + "Synchronizer control 3");
    all_zero &= check_zero(obj.SYNC4, name + " " + "Synchronizer control 4");
    all_zero &= check_zero(obj.SYNC5, name + " " + "Synchronizer control 5");
    all_zero &= check_zero(obj.SYNC6, name + " " + "Synchronizer control 6");
    all_zero &= check_zero(obj.SYNC7, name + " " + "Synchronizer control 7");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_INPUT_FILTER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_IN_MUX_SEL_0_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SEL_GP_IN_0, name + " " + "Select GP register 0");
    all_zero &= check_zero(obj.SEL_GP_IN_1, name + " " + "Select GP register 1");
    all_zero &= check_zero(obj.SEL_GP_IN_2, name + " " + "Select GP register 2");
    all_zero &= check_zero(obj.SEL_GP_IN_3, name + " " + "Select GP register 3");
    all_zero &= check_zero(obj.SEL_GP_IN_4, name + " " + "Select GP register 4");
    all_zero &= check_zero(obj.SEL_GP_IN_5, name + " " + "Select GP register 5");
    all_zero &= check_zero(obj.SEL_GP_IN_6, name + " " + "Select GP register 6");
    all_zero &= check_zero(obj.SEL_GP_IN_7, name + " " + "Select GP register 7");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_IN_MUX_SEL_0_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LCL_MUX_SEL_1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_0, name + " " + "Local Mux select 0");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_1, name + " " + "Local Mux select 1");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_2, name + " " + "Local Mux select 2");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_3, name + " " + "Local Mux select 3");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LCL_MUX_SEL_1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LCL_MUX_SEL_2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_4, name + " " + "Local Mux select 4");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_5, name + " " + "Local Mux select 5");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_6, name + " " + "Local Mux select 6");
    all_zero &= check_zero(obj.LCL_MUX_SEL_IN_7, name + " " + "Local Mux select 7");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_LCL_MUX_SEL_2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_BUF_PTR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PULL, name + " " + "Data pointer for pull");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PUSH, name + " " + "Data pointer for pull");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_BUF_PTR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_GP_REG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.REG, name + " " + "General Purpose bit register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_GP_REG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_GLBL_MUX_SEL_1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_0, name + " " + "Global Mux select 0");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_1, name + " " + "Global Mux select 1");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_2, name + " " + "Global Mux select 2");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_3, name + " " + "Global Mux select 3");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_GLBL_MUX_SEL_1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_GLBL_MUX_SEL_2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_4, name + " " + "Global Mux select 4");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_5, name + " " + "Global Mux select 5");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_6, name + " " + "Global Mux select 6");
    all_zero &= check_zero(obj.GLBL_MUX_SEL_IN_7, name + " " + "Global Mux select 7");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_GLBL_MUX_SEL_2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_INTR_TAG_REG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TAG, name + " " + "Interrupt tag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CLB_INTR_TAG_REG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK, name + " " + "LOCK enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Key for enabling write");

    return all_zero;
}

bool check_zero(const CLB_LOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_DBG_OUT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.COUNT0_MATCH2, name + " " + "COUNT_MATCH2 UNIT 0");
    all_zero &= check_zero(obj.COUNT0_ZERO, name + " " + "COUNT_ZERO UNIT 0");
    all_zero &= check_zero(obj.COUNT0_MATCH1, name + " " + "COUNT_MATCH1 UNIT 0");
    all_zero &= check_zero(obj.FSM0_S0, name + " " + "FSM_S0 UNIT 0");
    all_zero &= check_zero(obj.FSM0_S1, name + " " + "FSM_S1 UNIT 0");
    all_zero &= check_zero(obj.FSM0_LUTOUT, name + " " + "FSM_LUT_OUT UNIT 0");
    all_zero &= check_zero(obj.LUT40_OUT, name + " " + "LUT4_OUT UNIT 0");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.COUNT1_MATCH2, name + " " + "COUNT_MATCH2 UNIT 1");
    all_zero &= check_zero(obj.COUNT1_ZERO, name + " " + "COUNT_ZERO UNIT 1");
    all_zero &= check_zero(obj.COUNT1_MATCH1, name + " " + "COUNT_MATCH1 UNIT 1");
    all_zero &= check_zero(obj.FSM1_S0, name + " " + "FSM_S0 UNIT 1");
    all_zero &= check_zero(obj.FSM1_S1, name + " " + "FSM_S1 UNIT 1");
    all_zero &= check_zero(obj.FSM1_LUTOUT, name + " " + "FSM_LUT_OUT UNIT 1");
    all_zero &= check_zero(obj.LUT41_OUT, name + " " + "LUT4_OUT UNIT 1");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.COUNT2_MATCH2, name + " " + "COUNT_MATCH2 UNIT 2");
    all_zero &= check_zero(obj.COUNT2_ZERO, name + " " + "COUNT_ZERO UNIT 2");
    all_zero &= check_zero(obj.COUNT2_MATCH1, name + " " + "COUNT_MATCH1 UNIT 2");
    all_zero &= check_zero(obj.FSM2_S0, name + " " + "FSM_S0 UNIT 2");
    all_zero &= check_zero(obj.FSM2_S1, name + " " + "FSM_S1 UNIT 2");
    all_zero &= check_zero(obj.FSM2_LUTOUT, name + " " + "FSM_LUT_OUT UNIT 2");
    all_zero &= check_zero(obj.LUT42_OUT, name + " " + "LUT4_OUT UNIT 2");
    all_zero &= check_zero(obj.OUT0, name + " " + "CELL Output 0");
    all_zero &= check_zero(obj.OUT1, name + " " + "CELL Output 1");
    all_zero &= check_zero(obj.OUT2, name + " " + "CELL Output 2");
    all_zero &= check_zero(obj.OUT3, name + " " + "CELL Output 3");
    all_zero &= check_zero(obj.OUT4, name + " " + "CELL Output 4");
    all_zero &= check_zero(obj.OUT5, name + " " + "CELL Output 5");
    all_zero &= check_zero(obj.OUT6, name + " " + "CELL Output 6");
    all_zero &= check_zero(obj.OUT7, name + " " + "CELL Output 7");

    return all_zero;
}

bool check_zero(const CLB_DBG_OUT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_LOGIC_CONTROL_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLB_LOAD_EN, name + " " + "Global enable & indirect load enable control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_LOAD_ADDR, name + " " + "Indirect address");
    all_zero &= check_zero(obj.CLB_LOAD_DATA, name + " " + "Data for indirect loads");
    all_zero &= check_zero(obj.CLB_INPUT_FILTER,
                           name + " " + "Input filter selection for both edge detection and synchronizers");
    all_zero &= check_zero(obj.CLB_IN_MUX_SEL_0,
                           name + " " + "Input selection to decide between Signals and GP register");
    all_zero &= check_zero(obj.CLB_LCL_MUX_SEL_1, name + " " + "Input Mux selection for local mux");
    all_zero &= check_zero(obj.CLB_LCL_MUX_SEL_2, name + " " + "Input Mux selection for local mux");
    all_zero &= check_zero(obj.CLB_BUF_PTR, name + " " + "PUSH and PULL pointers");
    all_zero &= check_zero(obj.CLB_GP_REG, name + " " + "General purpose register for CELL inputs");
    all_zero &= check_zero(obj.CLB_OUT_EN, name + " " + "CELL output enable register");
    all_zero &= check_zero(obj.CLB_GLBL_MUX_SEL_1, name + " " + "Global Mux select for CELL inputs");
    all_zero &= check_zero(obj.CLB_GLBL_MUX_SEL_2, name + " " + "Global Mux select for CELL inputs");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_INTR_TAG_REG, name + " " + "Interrupt Tag register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_LOCK, name + " " + "Lock control register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_DBG_R0, name + " " + "R0 of High level Controller");
    all_zero &= check_zero(obj.CLB_DBG_R1, name + " " + "R1 of High level Controller");
    all_zero &= check_zero(obj.CLB_DBG_R2, name + " " + "R2 of High level Controller");
    all_zero &= check_zero(obj.CLB_DBG_R3, name + " " + "R3 of High level Controller");
    all_zero &= check_zero(obj.CLB_DBG_C0, name + " " + "Count of Unit 0");
    all_zero &= check_zero(obj.CLB_DBG_C1, name + " " + "Count of Unit 1");
    all_zero &= check_zero(obj.CLB_DBG_C2, name + " " + "Count of Unit 2");
    all_zero &= check_zero(obj.CLB_DBG_OUT, name + " " + "Outputs of various units in the Cell");

    return all_zero;
}

bool check_zero(const CLB_DATA_EXCHANGE_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLB_PUSH, name + " " + "CLB_PUSH FIFO Registers (from HLC)");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CLB_PULL, name + " " + "CLB_PULL FIFO Registers (TO HLC)");

    return all_zero;
}

bool check_zero(const AUXSIG0MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG0 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG0MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG0MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG0 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG0MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG1MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG1 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG1MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG1MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG1 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG1MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG2MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG2 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG2MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG2MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG2 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG2MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG3MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG3 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG3MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG3MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG3 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG3MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG4MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG4 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG4MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG4MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG4 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG4MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG5MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG5 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG5MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG5MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG5 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG5MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG6MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG6 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG6MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG6MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG6 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG6MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG7MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "MUX0 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 Configuration for AUXSIG7 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG7MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG7MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 Configuration for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 Configuration for AUXSIG7 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG7MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG0MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG0 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG0MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG1MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG1 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG1MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG2MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG2 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG2MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG3MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG3 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG3MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG4MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG4 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG4MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG5MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG5 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG5MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG6MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG6 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG6MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIG7MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "MUX1 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "MUX2 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "MUX3 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "MUX4 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "MUX5 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "MUX6 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "MUX7 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "MUX8 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "MUX9 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "MUX10 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "MUX11 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "MUX12 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "MUX13 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "MUX14 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "MUX15 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "MUX16 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "MUX17 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "MUX18 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "MUX19 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "MUX20 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "MUX21 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "MUX22 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "MUX23 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "MUX24 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "MUX25 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "MUX26 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "MUX27 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "MUX28 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "MUX29 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "MUX30 to drive AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "MUX31 to drive AUXSIG7 of CLB-XBAR");

    return all_zero;
}

bool check_zero(const AUXSIG7MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIGOUTINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OUT0, name + " " + "Selects polarity for AUXSIG0 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT1, name + " " + "Selects polarity for AUXSIG1 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT2, name + " " + "Selects polarity for AUXSIG2 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT3, name + " " + "Selects polarity for AUXSIG3 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT4, name + " " + "Selects polarity for AUXSIG4 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT5, name + " " + "Selects polarity for AUXSIG5 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT6, name + " " + "Selects polarity for AUXSIG6 of CLB-XBAR");
    all_zero &= check_zero(obj.OUT7, name + " " + "Selects polarity for AUXSIG7 of CLB-XBAR");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const AUXSIGOUTINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const AUXSIGLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK, name + " " + "Locks the configuration for CLB-XBAR");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Write Protection KEY");

    return all_zero;
}

bool check_zero(const AUXSIGLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CLB_XBAR_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.AUXSIG0MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-0");
    all_zero &= check_zero(obj.AUXSIG0MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-0");
    all_zero &= check_zero(obj.AUXSIG1MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-1");
    all_zero &= check_zero(obj.AUXSIG1MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-1");
    all_zero &= check_zero(obj.AUXSIG2MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-2");
    all_zero &= check_zero(obj.AUXSIG2MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-2");
    all_zero &= check_zero(obj.AUXSIG3MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-3");
    all_zero &= check_zero(obj.AUXSIG3MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-3");
    all_zero &= check_zero(obj.AUXSIG4MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-4");
    all_zero &= check_zero(obj.AUXSIG4MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-4");
    all_zero &= check_zero(obj.AUXSIG5MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-5");
    all_zero &= check_zero(obj.AUXSIG5MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-5");
    all_zero &= check_zero(obj.AUXSIG6MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-6");
    all_zero &= check_zero(obj.AUXSIG6MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-6");
    all_zero &= check_zero(obj.AUXSIG7MUX0TO15CFG, name + " " + "CLB XBAR Mux Configuration for Output-7");
    all_zero &= check_zero(obj.AUXSIG7MUX16TO31CFG, name + " " + "CLB XBAR Mux Configuration for Output-7");
    all_zero &= check_zero(obj.AUXSIG0MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-0");
    all_zero &= check_zero(obj.AUXSIG1MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-1");
    all_zero &= check_zero(obj.AUXSIG2MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-2");
    all_zero &= check_zero(obj.AUXSIG3MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-3");
    all_zero &= check_zero(obj.AUXSIG4MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-4");
    all_zero &= check_zero(obj.AUXSIG5MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-5");
    all_zero &= check_zero(obj.AUXSIG6MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-6");
    all_zero &= check_zero(obj.AUXSIG7MUXENABLE, name + " " + "CLB XBAR Mux Enable Register for Output-7");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.AUXSIGOUTINV, name + " " + "CLB XBAR Output Inversion Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.AUXSIGLOCK, name + " " + "ClbXbar Configuration Lock register");

    return all_zero;
}

bool check_zero(const GPACTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO0 to GPIO7");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO8 to GPIO15");
    all_zero &= check_zero(obj.QUALPRD2, name + " " + "Qualification sampling period for GPIO16 to GPIO23");
    all_zero &= check_zero(obj.QUALPRD3, name + " " + "Qualification sampling period for GPIO24 to GPIO31");

    return all_zero;
}

bool check_zero(const GPACTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Select input qualification type for GPIO0");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Select input qualification type for GPIO1");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Select input qualification type for GPIO2");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Select input qualification type for GPIO3");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Select input qualification type for GPIO4");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Select input qualification type for GPIO5");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Select input qualification type for GPIO6");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Select input qualification type for GPIO7");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Select input qualification type for GPIO8");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Select input qualification type for GPIO9");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Select input qualification type for GPIO10");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Select input qualification type for GPIO11");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Select input qualification type for GPIO12");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Select input qualification type for GPIO13");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Select input qualification type for GPIO14");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Select input qualification type for GPIO15");

    return all_zero;
}

bool check_zero(const GPAQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAQSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO16, name + " " + "Select input qualification type for GPIO16");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Select input qualification type for GPIO17");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Select input qualification type for GPIO18");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Select input qualification type for GPIO19");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Select input qualification type for GPIO20");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Select input qualification type for GPIO21");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Select input qualification type for GPIO22");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Select input qualification type for GPIO23");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Select input qualification type for GPIO24");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Select input qualification type for GPIO25");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Select input qualification type for GPIO26");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Select input qualification type for GPIO27");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Select input qualification type for GPIO28");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Select input qualification type for GPIO29");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Select input qualification type for GPIO30");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Select input qualification type for GPIO31");

    return all_zero;
}

bool check_zero(const GPAQSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Defines pin-muxing selection for GPIO0");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Defines pin-muxing selection for GPIO1");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Defines pin-muxing selection for GPIO2");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Defines pin-muxing selection for GPIO3");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Defines pin-muxing selection for GPIO4");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Defines pin-muxing selection for GPIO5");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Defines pin-muxing selection for GPIO6");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Defines pin-muxing selection for GPIO7");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Defines pin-muxing selection for GPIO8");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Defines pin-muxing selection for GPIO9");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Defines pin-muxing selection for GPIO10");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Defines pin-muxing selection for GPIO11");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Defines pin-muxing selection for GPIO12");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Defines pin-muxing selection for GPIO13");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Defines pin-muxing selection for GPIO14");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Defines pin-muxing selection for GPIO15");

    return all_zero;
}

bool check_zero(const GPAMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO16, name + " " + "Defines pin-muxing selection for GPIO16");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Defines pin-muxing selection for GPIO17");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Defines pin-muxing selection for GPIO18");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Defines pin-muxing selection for GPIO19");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Defines pin-muxing selection for GPIO20");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Defines pin-muxing selection for GPIO21");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Defines pin-muxing selection for GPIO22");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Defines pin-muxing selection for GPIO23");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Defines pin-muxing selection for GPIO24");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Defines pin-muxing selection for GPIO25");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Defines pin-muxing selection for GPIO26");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Defines pin-muxing selection for GPIO27");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Defines pin-muxing selection for GPIO28");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Defines pin-muxing selection for GPIO29");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Defines pin-muxing selection for GPIO30");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Defines pin-muxing selection for GPIO31");

    return all_zero;
}

bool check_zero(const GPAMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPADIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Defines direction for this pin in GPIO mode");

    return all_zero;
}

bool check_zero(const GPADIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Pull-Up Disable control for this pin");

    return all_zero;
}

bool check_zero(const GPAPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Input inversion control for this pin");

    return all_zero;
}

bool check_zero(const GPAINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Outpout Open-Drain control for this pin");

    return all_zero;
}

bool check_zero(const GPAODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Defines pin-muxing selection for GPIO0");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Defines pin-muxing selection for GPIO1");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Defines pin-muxing selection for GPIO2");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Defines pin-muxing selection for GPIO3");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Defines pin-muxing selection for GPIO4");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Defines pin-muxing selection for GPIO5");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Defines pin-muxing selection for GPIO6");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Defines pin-muxing selection for GPIO7");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Defines pin-muxing selection for GPIO8");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Defines pin-muxing selection for GPIO9");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Defines pin-muxing selection for GPIO10");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Defines pin-muxing selection for GPIO11");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Defines pin-muxing selection for GPIO12");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Defines pin-muxing selection for GPIO13");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Defines pin-muxing selection for GPIO14");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Defines pin-muxing selection for GPIO15");

    return all_zero;
}

bool check_zero(const GPAGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPAGMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO16, name + " " + "Defines pin-muxing selection for GPIO16");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Defines pin-muxing selection for GPIO17");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Defines pin-muxing selection for GPIO18");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Defines pin-muxing selection for GPIO19");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Defines pin-muxing selection for GPIO20");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Defines pin-muxing selection for GPIO21");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Defines pin-muxing selection for GPIO22");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Defines pin-muxing selection for GPIO23");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Defines pin-muxing selection for GPIO24");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Defines pin-muxing selection for GPIO25");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Defines pin-muxing selection for GPIO26");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Defines pin-muxing selection for GPIO27");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Defines pin-muxing selection for GPIO28");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Defines pin-muxing selection for GPIO29");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Defines pin-muxing selection for GPIO30");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Defines pin-muxing selection for GPIO31");

    return all_zero;
}

bool check_zero(const GPAGMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "GPIO0 Master CPU Select");
    all_zero &= check_zero(obj.GPIO1, name + " " + "GPIO1 Master CPU Select");
    all_zero &= check_zero(obj.GPIO2, name + " " + "GPIO2 Master CPU Select");
    all_zero &= check_zero(obj.GPIO3, name + " " + "GPIO3 Master CPU Select");
    all_zero &= check_zero(obj.GPIO4, name + " " + "GPIO4 Master CPU Select");
    all_zero &= check_zero(obj.GPIO5, name + " " + "GPIO5 Master CPU Select");
    all_zero &= check_zero(obj.GPIO6, name + " " + "GPIO6 Master CPU Select");
    all_zero &= check_zero(obj.GPIO7, name + " " + "GPIO7 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPACSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO8, name + " " + "GPIO8 Master CPU Select");
    all_zero &= check_zero(obj.GPIO9, name + " " + "GPIO9 Master CPU Select");
    all_zero &= check_zero(obj.GPIO10, name + " " + "GPIO10 Master CPU Select");
    all_zero &= check_zero(obj.GPIO11, name + " " + "GPIO11 Master CPU Select");
    all_zero &= check_zero(obj.GPIO12, name + " " + "GPIO12 Master CPU Select");
    all_zero &= check_zero(obj.GPIO13, name + " " + "GPIO13 Master CPU Select");
    all_zero &= check_zero(obj.GPIO14, name + " " + "GPIO14 Master CPU Select");
    all_zero &= check_zero(obj.GPIO15, name + " " + "GPIO15 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPACSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACSEL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO16, name + " " + "GPIO16 Master CPU Select");
    all_zero &= check_zero(obj.GPIO17, name + " " + "GPIO17 Master CPU Select");
    all_zero &= check_zero(obj.GPIO18, name + " " + "GPIO18 Master CPU Select");
    all_zero &= check_zero(obj.GPIO19, name + " " + "GPIO19 Master CPU Select");
    all_zero &= check_zero(obj.GPIO20, name + " " + "GPIO20 Master CPU Select");
    all_zero &= check_zero(obj.GPIO21, name + " " + "GPIO21 Master CPU Select");
    all_zero &= check_zero(obj.GPIO22, name + " " + "GPIO22 Master CPU Select");
    all_zero &= check_zero(obj.GPIO23, name + " " + "GPIO23 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPACSEL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO24, name + " " + "GPIO24 Master CPU Select");
    all_zero &= check_zero(obj.GPIO25, name + " " + "GPIO25 Master CPU Select");
    all_zero &= check_zero(obj.GPIO26, name + " " + "GPIO26 Master CPU Select");
    all_zero &= check_zero(obj.GPIO27, name + " " + "GPIO27 Master CPU Select");
    all_zero &= check_zero(obj.GPIO28, name + " " + "GPIO28 Master CPU Select");
    all_zero &= check_zero(obj.GPIO29, name + " " + "GPIO29 Master CPU Select");
    all_zero &= check_zero(obj.GPIO30, name + " " + "GPIO30 Master CPU Select");
    all_zero &= check_zero(obj.GPIO31, name + " " + "GPIO31 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPACSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPALOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Configuration Lock bit for this pin");

    return all_zero;
}

bool check_zero(const GPALOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Configuration lock commit bit for this pin");

    return all_zero;
}

bool check_zero(const GPACR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO32 to GPIO39");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO40 to GPIO47");
    all_zero &= check_zero(obj.QUALPRD2, name + " " + "Qualification sampling period for GPIO48 to GPIO55");
    all_zero &= check_zero(obj.QUALPRD3, name + " " + "Qualification sampling period for GPIO56 to GPIO63");

    return all_zero;
}

bool check_zero(const GPBCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Select input qualification type for GPIO32");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Select input qualification type for GPIO33");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Select input qualification type for GPIO34");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Select input qualification type for GPIO35");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Select input qualification type for GPIO36");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Select input qualification type for GPIO37");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Select input qualification type for GPIO38");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Select input qualification type for GPIO39");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Select input qualification type for GPIO40");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Select input qualification type for GPIO41");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Select input qualification type for GPIO42");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Select input qualification type for GPIO43");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Select input qualification type for GPIO44");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Select input qualification type for GPIO45");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Select input qualification type for GPIO46");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Select input qualification type for GPIO47");

    return all_zero;
}

bool check_zero(const GPBQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBQSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO48, name + " " + "Select input qualification type for GPIO48");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Select input qualification type for GPIO49");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Select input qualification type for GPIO50");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Select input qualification type for GPIO51");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Select input qualification type for GPIO52");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Select input qualification type for GPIO53");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Select input qualification type for GPIO54");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Select input qualification type for GPIO55");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Select input qualification type for GPIO56");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Select input qualification type for GPIO57");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Select input qualification type for GPIO58");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Select input qualification type for GPIO59");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Select input qualification type for GPIO60");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Select input qualification type for GPIO61");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Select input qualification type for GPIO62");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Select input qualification type for GPIO63");

    return all_zero;
}

bool check_zero(const GPBQSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Defines pin-muxing selection for GPIO32");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Defines pin-muxing selection for GPIO33");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Defines pin-muxing selection for GPIO34");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Defines pin-muxing selection for GPIO35");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Defines pin-muxing selection for GPIO36");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Defines pin-muxing selection for GPIO37");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Defines pin-muxing selection for GPIO38");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Defines pin-muxing selection for GPIO39");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Defines pin-muxing selection for GPIO40");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Defines pin-muxing selection for GPIO41");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Defines pin-muxing selection for GPIO42");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Defines pin-muxing selection for GPIO43");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Defines pin-muxing selection for GPIO44");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Defines pin-muxing selection for GPIO45");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Defines pin-muxing selection for GPIO46");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Defines pin-muxing selection for GPIO47");

    return all_zero;
}

bool check_zero(const GPBMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO48, name + " " + "Defines pin-muxing selection for GPIO48");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Defines pin-muxing selection for GPIO49");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Defines pin-muxing selection for GPIO50");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Defines pin-muxing selection for GPIO51");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Defines pin-muxing selection for GPIO52");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Defines pin-muxing selection for GPIO53");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Defines pin-muxing selection for GPIO54");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Defines pin-muxing selection for GPIO55");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Defines pin-muxing selection for GPIO56");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Defines pin-muxing selection for GPIO57");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Defines pin-muxing selection for GPIO58");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Defines pin-muxing selection for GPIO59");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Defines pin-muxing selection for GPIO60");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Defines pin-muxing selection for GPIO61");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Defines pin-muxing selection for GPIO62");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Defines pin-muxing selection for GPIO63");

    return all_zero;
}

bool check_zero(const GPBMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBDIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Defines direction for this pin in GPIO mode");

    return all_zero;
}

bool check_zero(const GPBDIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Pull-Up Disable control for this pin");

    return all_zero;
}

bool check_zero(const GPBPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Input inversion control for this pin");

    return all_zero;
}

bool check_zero(const GPBINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Outpout Open-Drain control for this pin");

    return all_zero;
}

bool check_zero(const GPBODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBAMSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Analog Mode select for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Analog Mode select for this pin");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd24, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd25, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd26, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd27, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd28, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd29, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd30, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPBAMSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Defines pin-muxing selection for GPIO32");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Defines pin-muxing selection for GPIO33");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Defines pin-muxing selection for GPIO34");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Defines pin-muxing selection for GPIO35");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Defines pin-muxing selection for GPIO36");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Defines pin-muxing selection for GPIO37");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Defines pin-muxing selection for GPIO38");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Defines pin-muxing selection for GPIO39");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Defines pin-muxing selection for GPIO40");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Defines pin-muxing selection for GPIO41");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Defines pin-muxing selection for GPIO42");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Defines pin-muxing selection for GPIO43");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Defines pin-muxing selection for GPIO44");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Defines pin-muxing selection for GPIO45");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Defines pin-muxing selection for GPIO46");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Defines pin-muxing selection for GPIO47");

    return all_zero;
}

bool check_zero(const GPBGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBGMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO48, name + " " + "Defines pin-muxing selection for GPIO48");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Defines pin-muxing selection for GPIO49");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Defines pin-muxing selection for GPIO50");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Defines pin-muxing selection for GPIO51");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Defines pin-muxing selection for GPIO52");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Defines pin-muxing selection for GPIO53");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Defines pin-muxing selection for GPIO54");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Defines pin-muxing selection for GPIO55");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Defines pin-muxing selection for GPIO56");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Defines pin-muxing selection for GPIO57");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Defines pin-muxing selection for GPIO58");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Defines pin-muxing selection for GPIO59");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Defines pin-muxing selection for GPIO60");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Defines pin-muxing selection for GPIO61");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Defines pin-muxing selection for GPIO62");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Defines pin-muxing selection for GPIO63");

    return all_zero;
}

bool check_zero(const GPBGMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "GPIO32 Master CPU Select");
    all_zero &= check_zero(obj.GPIO33, name + " " + "GPIO33 Master CPU Select");
    all_zero &= check_zero(obj.GPIO34, name + " " + "GPIO34 Master CPU Select");
    all_zero &= check_zero(obj.GPIO35, name + " " + "GPIO35 Master CPU Select");
    all_zero &= check_zero(obj.GPIO36, name + " " + "GPIO36 Master CPU Select");
    all_zero &= check_zero(obj.GPIO37, name + " " + "GPIO37 Master CPU Select");
    all_zero &= check_zero(obj.GPIO38, name + " " + "GPIO38 Master CPU Select");
    all_zero &= check_zero(obj.GPIO39, name + " " + "GPIO39 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPBCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO40, name + " " + "GPIO40 Master CPU Select");
    all_zero &= check_zero(obj.GPIO41, name + " " + "GPIO41 Master CPU Select");
    all_zero &= check_zero(obj.GPIO42, name + " " + "GPIO42 Master CPU Select");
    all_zero &= check_zero(obj.GPIO43, name + " " + "GPIO43 Master CPU Select");
    all_zero &= check_zero(obj.GPIO44, name + " " + "GPIO44 Master CPU Select");
    all_zero &= check_zero(obj.GPIO45, name + " " + "GPIO45 Master CPU Select");
    all_zero &= check_zero(obj.GPIO46, name + " " + "GPIO46 Master CPU Select");
    all_zero &= check_zero(obj.GPIO47, name + " " + "GPIO47 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPBCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCSEL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO48, name + " " + "GPIO48 Master CPU Select");
    all_zero &= check_zero(obj.GPIO49, name + " " + "GPIO49 Master CPU Select");
    all_zero &= check_zero(obj.GPIO50, name + " " + "GPIO50 Master CPU Select");
    all_zero &= check_zero(obj.GPIO51, name + " " + "GPIO51 Master CPU Select");
    all_zero &= check_zero(obj.GPIO52, name + " " + "GPIO52 Master CPU Select");
    all_zero &= check_zero(obj.GPIO53, name + " " + "GPIO53 Master CPU Select");
    all_zero &= check_zero(obj.GPIO54, name + " " + "GPIO54 Master CPU Select");
    all_zero &= check_zero(obj.GPIO55, name + " " + "GPIO55 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPBCSEL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO56, name + " " + "GPIO56 Master CPU Select");
    all_zero &= check_zero(obj.GPIO57, name + " " + "GPIO57 Master CPU Select");
    all_zero &= check_zero(obj.GPIO58, name + " " + "GPIO58 Master CPU Select");
    all_zero &= check_zero(obj.GPIO59, name + " " + "GPIO59 Master CPU Select");
    all_zero &= check_zero(obj.GPIO60, name + " " + "GPIO60 Master CPU Select");
    all_zero &= check_zero(obj.GPIO61, name + " " + "GPIO61 Master CPU Select");
    all_zero &= check_zero(obj.GPIO62, name + " " + "GPIO62 Master CPU Select");
    all_zero &= check_zero(obj.GPIO63, name + " " + "GPIO63 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPBCSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Configuration Lock bit for this pin");

    return all_zero;
}

bool check_zero(const GPBLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Configuration lock commit bit for this pin");

    return all_zero;
}

bool check_zero(const GPBCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO64 to GPIO71");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO72 to GPIO79");
    all_zero &= check_zero(obj.QUALPRD2, name + " " + "Qualification sampling period for GPIO80 to GPIO87");
    all_zero &= check_zero(obj.QUALPRD3, name + " " + "Qualification sampling period for GPIO88 to GPIO95");

    return all_zero;
}

bool check_zero(const GPCCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Select input qualification type for GPIO64");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Select input qualification type for GPIO65");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Select input qualification type for GPIO66");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Select input qualification type for GPIO67");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Select input qualification type for GPIO68");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Select input qualification type for GPIO69");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Select input qualification type for GPIO70");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Select input qualification type for GPIO71");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Select input qualification type for GPIO72");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Select input qualification type for GPIO73");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Select input qualification type for GPIO74");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Select input qualification type for GPIO75");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Select input qualification type for GPIO76");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Select input qualification type for GPIO77");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Select input qualification type for GPIO78");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Select input qualification type for GPIO79");

    return all_zero;
}

bool check_zero(const GPCQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCQSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO80, name + " " + "Select input qualification type for GPIO80");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Select input qualification type for GPIO81");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Select input qualification type for GPIO82");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Select input qualification type for GPIO83");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Select input qualification type for GPIO84");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Select input qualification type for GPIO85");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Select input qualification type for GPIO86");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Select input qualification type for GPIO87");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Select input qualification type for GPIO88");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Select input qualification type for GPIO89");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Select input qualification type for GPIO90");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Select input qualification type for GPIO91");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Select input qualification type for GPIO92");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Select input qualification type for GPIO93");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Select input qualification type for GPIO94");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Select input qualification type for GPIO95");

    return all_zero;
}

bool check_zero(const GPCQSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Defines pin-muxing selection for GPIO64");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Defines pin-muxing selection for GPIO65");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Defines pin-muxing selection for GPIO66");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Defines pin-muxing selection for GPIO67");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Defines pin-muxing selection for GPIO68");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Defines pin-muxing selection for GPIO69");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Defines pin-muxing selection for GPIO70");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Defines pin-muxing selection for GPIO71");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Defines pin-muxing selection for GPIO72");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Defines pin-muxing selection for GPIO73");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Defines pin-muxing selection for GPIO74");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Defines pin-muxing selection for GPIO75");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Defines pin-muxing selection for GPIO76");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Defines pin-muxing selection for GPIO77");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Defines pin-muxing selection for GPIO78");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Defines pin-muxing selection for GPIO79");

    return all_zero;
}

bool check_zero(const GPCMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO80, name + " " + "Defines pin-muxing selection for GPIO80");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Defines pin-muxing selection for GPIO81");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Defines pin-muxing selection for GPIO82");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Defines pin-muxing selection for GPIO83");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Defines pin-muxing selection for GPIO84");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Defines pin-muxing selection for GPIO85");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Defines pin-muxing selection for GPIO86");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Defines pin-muxing selection for GPIO87");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Defines pin-muxing selection for GPIO88");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Defines pin-muxing selection for GPIO89");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Defines pin-muxing selection for GPIO90");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Defines pin-muxing selection for GPIO91");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Defines pin-muxing selection for GPIO92");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Defines pin-muxing selection for GPIO93");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Defines pin-muxing selection for GPIO94");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Defines pin-muxing selection for GPIO95");

    return all_zero;
}

bool check_zero(const GPCMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCDIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Defines direction for this pin in GPIO mode");

    return all_zero;
}

bool check_zero(const GPCDIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Pull-Up Disable control for this pin");

    return all_zero;
}

bool check_zero(const GPCPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Input inversion control for this pin");

    return all_zero;
}

bool check_zero(const GPCINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Outpout Open-Drain control for this pin");

    return all_zero;
}

bool check_zero(const GPCODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Defines pin-muxing selection for GPIO64");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Defines pin-muxing selection for GPIO65");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Defines pin-muxing selection for GPIO66");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Defines pin-muxing selection for GPIO67");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Defines pin-muxing selection for GPIO68");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Defines pin-muxing selection for GPIO69");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Defines pin-muxing selection for GPIO70");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Defines pin-muxing selection for GPIO71");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Defines pin-muxing selection for GPIO72");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Defines pin-muxing selection for GPIO73");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Defines pin-muxing selection for GPIO74");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Defines pin-muxing selection for GPIO75");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Defines pin-muxing selection for GPIO76");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Defines pin-muxing selection for GPIO77");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Defines pin-muxing selection for GPIO78");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Defines pin-muxing selection for GPIO79");

    return all_zero;
}

bool check_zero(const GPCGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCGMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO80, name + " " + "Defines pin-muxing selection for GPIO80");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Defines pin-muxing selection for GPIO81");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Defines pin-muxing selection for GPIO82");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Defines pin-muxing selection for GPIO83");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Defines pin-muxing selection for GPIO84");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Defines pin-muxing selection for GPIO85");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Defines pin-muxing selection for GPIO86");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Defines pin-muxing selection for GPIO87");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Defines pin-muxing selection for GPIO88");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Defines pin-muxing selection for GPIO89");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Defines pin-muxing selection for GPIO90");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Defines pin-muxing selection for GPIO91");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Defines pin-muxing selection for GPIO92");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Defines pin-muxing selection for GPIO93");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Defines pin-muxing selection for GPIO94");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Defines pin-muxing selection for GPIO95");

    return all_zero;
}

bool check_zero(const GPCGMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "GPIO64 Master CPU Select");
    all_zero &= check_zero(obj.GPIO65, name + " " + "GPIO65 Master CPU Select");
    all_zero &= check_zero(obj.GPIO66, name + " " + "GPIO66 Master CPU Select");
    all_zero &= check_zero(obj.GPIO67, name + " " + "GPIO67 Master CPU Select");
    all_zero &= check_zero(obj.GPIO68, name + " " + "GPIO68 Master CPU Select");
    all_zero &= check_zero(obj.GPIO69, name + " " + "GPIO69 Master CPU Select");
    all_zero &= check_zero(obj.GPIO70, name + " " + "GPIO70 Master CPU Select");
    all_zero &= check_zero(obj.GPIO71, name + " " + "GPIO71 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPCCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO72, name + " " + "GPIO72 Master CPU Select");
    all_zero &= check_zero(obj.GPIO73, name + " " + "GPIO73 Master CPU Select");
    all_zero &= check_zero(obj.GPIO74, name + " " + "GPIO74 Master CPU Select");
    all_zero &= check_zero(obj.GPIO75, name + " " + "GPIO75 Master CPU Select");
    all_zero &= check_zero(obj.GPIO76, name + " " + "GPIO76 Master CPU Select");
    all_zero &= check_zero(obj.GPIO77, name + " " + "GPIO77 Master CPU Select");
    all_zero &= check_zero(obj.GPIO78, name + " " + "GPIO78 Master CPU Select");
    all_zero &= check_zero(obj.GPIO79, name + " " + "GPIO79 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPCCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCSEL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO80, name + " " + "GPIO80 Master CPU Select");
    all_zero &= check_zero(obj.GPIO81, name + " " + "GPIO81 Master CPU Select");
    all_zero &= check_zero(obj.GPIO82, name + " " + "GPIO82 Master CPU Select");
    all_zero &= check_zero(obj.GPIO83, name + " " + "GPIO83 Master CPU Select");
    all_zero &= check_zero(obj.GPIO84, name + " " + "GPIO84 Master CPU Select");
    all_zero &= check_zero(obj.GPIO85, name + " " + "GPIO85 Master CPU Select");
    all_zero &= check_zero(obj.GPIO86, name + " " + "GPIO86 Master CPU Select");
    all_zero &= check_zero(obj.GPIO87, name + " " + "GPIO87 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPCCSEL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO88, name + " " + "GPIO88 Master CPU Select");
    all_zero &= check_zero(obj.GPIO89, name + " " + "GPIO89 Master CPU Select");
    all_zero &= check_zero(obj.GPIO90, name + " " + "GPIO90 Master CPU Select");
    all_zero &= check_zero(obj.GPIO91, name + " " + "GPIO91 Master CPU Select");
    all_zero &= check_zero(obj.GPIO92, name + " " + "GPIO92 Master CPU Select");
    all_zero &= check_zero(obj.GPIO93, name + " " + "GPIO93 Master CPU Select");
    all_zero &= check_zero(obj.GPIO94, name + " " + "GPIO94 Master CPU Select");
    all_zero &= check_zero(obj.GPIO95, name + " " + "GPIO95 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPCCSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Configuration Lock bit for this pin");

    return all_zero;
}

bool check_zero(const GPCLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Configuration lock commit bit for this pin");

    return all_zero;
}

bool check_zero(const GPCCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO96 to GPIO103");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO104 to GPIO111");
    all_zero &= check_zero(obj.QUALPRD2, name + " " + "Qualification sampling period for GPIO112 to GPIO119");
    all_zero &= check_zero(obj.QUALPRD3, name + " " + "Qualification sampling period for GPIO120 to GPIO127");

    return all_zero;
}

bool check_zero(const GPDCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Select input qualification type for GPIO96");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Select input qualification type for GPIO97");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Select input qualification type for GPIO98");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Select input qualification type for GPIO99");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Select input qualification type for GPIO100");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Select input qualification type for GPIO101");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Select input qualification type for GPIO102");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Select input qualification type for GPIO103");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Select input qualification type for GPIO104");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Select input qualification type for GPIO105");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Select input qualification type for GPIO106");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Select input qualification type for GPIO107");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Select input qualification type for GPIO108");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Select input qualification type for GPIO109");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Select input qualification type for GPIO110");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Select input qualification type for GPIO111");

    return all_zero;
}

bool check_zero(const GPDQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDQSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO112, name + " " + "Select input qualification type for GPIO112");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Select input qualification type for GPIO113");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Select input qualification type for GPIO114");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Select input qualification type for GPIO115");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Select input qualification type for GPIO116");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Select input qualification type for GPIO117");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Select input qualification type for GPIO118");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Select input qualification type for GPIO119");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Select input qualification type for GPIO120");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Select input qualification type for GPIO121");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Select input qualification type for GPIO122");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Select input qualification type for GPIO123");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Select input qualification type for GPIO124");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Select input qualification type for GPIO125");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Select input qualification type for GPIO126");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Select input qualification type for GPIO127");

    return all_zero;
}

bool check_zero(const GPDQSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Defines pin-muxing selection for GPIO96");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Defines pin-muxing selection for GPIO97");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Defines pin-muxing selection for GPIO98");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Defines pin-muxing selection for GPIO99");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Defines pin-muxing selection for GPIO100");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Defines pin-muxing selection for GPIO101");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Defines pin-muxing selection for GPIO102");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Defines pin-muxing selection for GPIO103");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Defines pin-muxing selection for GPIO104");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Defines pin-muxing selection for GPIO105");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Defines pin-muxing selection for GPIO106");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Defines pin-muxing selection for GPIO107");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Defines pin-muxing selection for GPIO108");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Defines pin-muxing selection for GPIO109");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Defines pin-muxing selection for GPIO110");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Defines pin-muxing selection for GPIO111");

    return all_zero;
}

bool check_zero(const GPDMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO112, name + " " + "Defines pin-muxing selection for GPIO112");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Defines pin-muxing selection for GPIO113");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Defines pin-muxing selection for GPIO114");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Defines pin-muxing selection for GPIO115");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Defines pin-muxing selection for GPIO116");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Defines pin-muxing selection for GPIO117");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Defines pin-muxing selection for GPIO118");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Defines pin-muxing selection for GPIO119");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Defines pin-muxing selection for GPIO120");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Defines pin-muxing selection for GPIO121");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Defines pin-muxing selection for GPIO122");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Defines pin-muxing selection for GPIO123");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Defines pin-muxing selection for GPIO124");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Defines pin-muxing selection for GPIO125");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Defines pin-muxing selection for GPIO126");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Defines pin-muxing selection for GPIO127");

    return all_zero;
}

bool check_zero(const GPDMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDDIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Defines direction for this pin in GPIO mode");

    return all_zero;
}

bool check_zero(const GPDDIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Pull-Up Disable control for this pin");

    return all_zero;
}

bool check_zero(const GPDPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Input inversion control for this pin");

    return all_zero;
}

bool check_zero(const GPDINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Outpout Open-Drain control for this pin");

    return all_zero;
}

bool check_zero(const GPDODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Defines pin-muxing selection for GPIO96");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Defines pin-muxing selection for GPIO97");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Defines pin-muxing selection for GPIO98");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Defines pin-muxing selection for GPIO99");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Defines pin-muxing selection for GPIO100");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Defines pin-muxing selection for GPIO101");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Defines pin-muxing selection for GPIO102");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Defines pin-muxing selection for GPIO103");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Defines pin-muxing selection for GPIO104");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Defines pin-muxing selection for GPIO105");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Defines pin-muxing selection for GPIO106");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Defines pin-muxing selection for GPIO107");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Defines pin-muxing selection for GPIO108");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Defines pin-muxing selection for GPIO109");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Defines pin-muxing selection for GPIO110");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Defines pin-muxing selection for GPIO111");

    return all_zero;
}

bool check_zero(const GPDGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDGMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO112, name + " " + "Defines pin-muxing selection for GPIO112");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Defines pin-muxing selection for GPIO113");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Defines pin-muxing selection for GPIO114");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Defines pin-muxing selection for GPIO115");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Defines pin-muxing selection for GPIO116");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Defines pin-muxing selection for GPIO117");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Defines pin-muxing selection for GPIO118");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Defines pin-muxing selection for GPIO119");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Defines pin-muxing selection for GPIO120");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Defines pin-muxing selection for GPIO121");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Defines pin-muxing selection for GPIO122");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Defines pin-muxing selection for GPIO123");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Defines pin-muxing selection for GPIO124");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Defines pin-muxing selection for GPIO125");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Defines pin-muxing selection for GPIO126");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Defines pin-muxing selection for GPIO127");

    return all_zero;
}

bool check_zero(const GPDGMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "GPIO96 Master CPU Select");
    all_zero &= check_zero(obj.GPIO97, name + " " + "GPIO97 Master CPU Select");
    all_zero &= check_zero(obj.GPIO98, name + " " + "GPIO98 Master CPU Select");
    all_zero &= check_zero(obj.GPIO99, name + " " + "GPIO99 Master CPU Select");
    all_zero &= check_zero(obj.GPIO100, name + " " + "GPIO100 Master CPU Select");
    all_zero &= check_zero(obj.GPIO101, name + " " + "GPIO101 Master CPU Select");
    all_zero &= check_zero(obj.GPIO102, name + " " + "GPIO102 Master CPU Select");
    all_zero &= check_zero(obj.GPIO103, name + " " + "GPIO103 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPDCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO104, name + " " + "GPIO104 Master CPU Select");
    all_zero &= check_zero(obj.GPIO105, name + " " + "GPIO105 Master CPU Select");
    all_zero &= check_zero(obj.GPIO106, name + " " + "GPIO106 Master CPU Select");
    all_zero &= check_zero(obj.GPIO107, name + " " + "GPIO107 Master CPU Select");
    all_zero &= check_zero(obj.GPIO108, name + " " + "GPIO108 Master CPU Select");
    all_zero &= check_zero(obj.GPIO109, name + " " + "GPIO109 Master CPU Select");
    all_zero &= check_zero(obj.GPIO110, name + " " + "GPIO110 Master CPU Select");
    all_zero &= check_zero(obj.GPIO111, name + " " + "GPIO111 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPDCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCSEL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO112, name + " " + "GPIO112 Master CPU Select");
    all_zero &= check_zero(obj.GPIO113, name + " " + "GPIO113 Master CPU Select");
    all_zero &= check_zero(obj.GPIO114, name + " " + "GPIO114 Master CPU Select");
    all_zero &= check_zero(obj.GPIO115, name + " " + "GPIO115 Master CPU Select");
    all_zero &= check_zero(obj.GPIO116, name + " " + "GPIO116 Master CPU Select");
    all_zero &= check_zero(obj.GPIO117, name + " " + "GPIO117 Master CPU Select");
    all_zero &= check_zero(obj.GPIO118, name + " " + "GPIO118 Master CPU Select");
    all_zero &= check_zero(obj.GPIO119, name + " " + "GPIO119 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPDCSEL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO120, name + " " + "GPIO120 Master CPU Select");
    all_zero &= check_zero(obj.GPIO121, name + " " + "GPIO121 Master CPU Select");
    all_zero &= check_zero(obj.GPIO122, name + " " + "GPIO122 Master CPU Select");
    all_zero &= check_zero(obj.GPIO123, name + " " + "GPIO123 Master CPU Select");
    all_zero &= check_zero(obj.GPIO124, name + " " + "GPIO124 Master CPU Select");
    all_zero &= check_zero(obj.GPIO125, name + " " + "GPIO125 Master CPU Select");
    all_zero &= check_zero(obj.GPIO126, name + " " + "GPIO126 Master CPU Select");
    all_zero &= check_zero(obj.GPIO127, name + " " + "GPIO127 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPDCSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Configuration Lock bit for this pin");

    return all_zero;
}

bool check_zero(const GPDLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Configuration lock commit bit for this pin");

    return all_zero;
}

bool check_zero(const GPDCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO128 to GPIO135");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO136 to GPIO143");
    all_zero &= check_zero(obj.QUALPRD2, name + " " + "Qualification sampling period for GPIO144 to GPIO151");
    all_zero &= check_zero(obj.QUALPRD3, name + " " + "Qualification sampling period for GPIO152 to GPIO159");

    return all_zero;
}

bool check_zero(const GPECTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Select input qualification type for GPIO128");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Select input qualification type for GPIO129");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Select input qualification type for GPIO130");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Select input qualification type for GPIO131");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Select input qualification type for GPIO132");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Select input qualification type for GPIO133");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Select input qualification type for GPIO134");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Select input qualification type for GPIO135");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Select input qualification type for GPIO136");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Select input qualification type for GPIO137");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Select input qualification type for GPIO138");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Select input qualification type for GPIO139");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Select input qualification type for GPIO140");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Select input qualification type for GPIO141");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Select input qualification type for GPIO142");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Select input qualification type for GPIO143");

    return all_zero;
}

bool check_zero(const GPEQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEQSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO144, name + " " + "Select input qualification type for GPIO144");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Select input qualification type for GPIO145");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Select input qualification type for GPIO146");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Select input qualification type for GPIO147");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Select input qualification type for GPIO148");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Select input qualification type for GPIO149");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Select input qualification type for GPIO150");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Select input qualification type for GPIO151");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Select input qualification type for GPIO152");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Select input qualification type for GPIO153");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Select input qualification type for GPIO154");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Select input qualification type for GPIO155");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Select input qualification type for GPIO156");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Select input qualification type for GPIO157");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Select input qualification type for GPIO158");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Select input qualification type for GPIO159");

    return all_zero;
}

bool check_zero(const GPEQSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Defines pin-muxing selection for GPIO128");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Defines pin-muxing selection for GPIO129");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Defines pin-muxing selection for GPIO130");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Defines pin-muxing selection for GPIO131");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Defines pin-muxing selection for GPIO132");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Defines pin-muxing selection for GPIO133");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Defines pin-muxing selection for GPIO134");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Defines pin-muxing selection for GPIO135");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Defines pin-muxing selection for GPIO136");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Defines pin-muxing selection for GPIO137");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Defines pin-muxing selection for GPIO138");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Defines pin-muxing selection for GPIO139");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Defines pin-muxing selection for GPIO140");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Defines pin-muxing selection for GPIO141");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Defines pin-muxing selection for GPIO142");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Defines pin-muxing selection for GPIO143");

    return all_zero;
}

bool check_zero(const GPEMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO144, name + " " + "Defines pin-muxing selection for GPIO144");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Defines pin-muxing selection for GPIO145");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Defines pin-muxing selection for GPIO146");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Defines pin-muxing selection for GPIO147");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Defines pin-muxing selection for GPIO148");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Defines pin-muxing selection for GPIO149");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Defines pin-muxing selection for GPIO150");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Defines pin-muxing selection for GPIO151");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Defines pin-muxing selection for GPIO152");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Defines pin-muxing selection for GPIO153");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Defines pin-muxing selection for GPIO154");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Defines pin-muxing selection for GPIO155");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Defines pin-muxing selection for GPIO156");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Defines pin-muxing selection for GPIO157");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Defines pin-muxing selection for GPIO158");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Defines pin-muxing selection for GPIO159");

    return all_zero;
}

bool check_zero(const GPEMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEDIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Defines direction for this pin in GPIO mode");

    return all_zero;
}

bool check_zero(const GPEDIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Pull-Up Disable control for this pin");

    return all_zero;
}

bool check_zero(const GPEPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Input inversion control for this pin");

    return all_zero;
}

bool check_zero(const GPEINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Outpout Open-Drain control for this pin");

    return all_zero;
}

bool check_zero(const GPEODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Defines pin-muxing selection for GPIO128");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Defines pin-muxing selection for GPIO129");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Defines pin-muxing selection for GPIO130");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Defines pin-muxing selection for GPIO131");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Defines pin-muxing selection for GPIO132");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Defines pin-muxing selection for GPIO133");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Defines pin-muxing selection for GPIO134");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Defines pin-muxing selection for GPIO135");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Defines pin-muxing selection for GPIO136");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Defines pin-muxing selection for GPIO137");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Defines pin-muxing selection for GPIO138");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Defines pin-muxing selection for GPIO139");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Defines pin-muxing selection for GPIO140");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Defines pin-muxing selection for GPIO141");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Defines pin-muxing selection for GPIO142");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Defines pin-muxing selection for GPIO143");

    return all_zero;
}

bool check_zero(const GPEGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEGMUX2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO144, name + " " + "Defines pin-muxing selection for GPIO144");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Defines pin-muxing selection for GPIO145");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Defines pin-muxing selection for GPIO146");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Defines pin-muxing selection for GPIO147");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Defines pin-muxing selection for GPIO148");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Defines pin-muxing selection for GPIO149");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Defines pin-muxing selection for GPIO150");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Defines pin-muxing selection for GPIO151");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Defines pin-muxing selection for GPIO152");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Defines pin-muxing selection for GPIO153");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Defines pin-muxing selection for GPIO154");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Defines pin-muxing selection for GPIO155");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Defines pin-muxing selection for GPIO156");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Defines pin-muxing selection for GPIO157");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Defines pin-muxing selection for GPIO158");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Defines pin-muxing selection for GPIO159");

    return all_zero;
}

bool check_zero(const GPEGMUX2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "GPIO128 Master CPU Select");
    all_zero &= check_zero(obj.GPIO129, name + " " + "GPIO129 Master CPU Select");
    all_zero &= check_zero(obj.GPIO130, name + " " + "GPIO130 Master CPU Select");
    all_zero &= check_zero(obj.GPIO131, name + " " + "GPIO131 Master CPU Select");
    all_zero &= check_zero(obj.GPIO132, name + " " + "GPIO132 Master CPU Select");
    all_zero &= check_zero(obj.GPIO133, name + " " + "GPIO133 Master CPU Select");
    all_zero &= check_zero(obj.GPIO134, name + " " + "GPIO134 Master CPU Select");
    all_zero &= check_zero(obj.GPIO135, name + " " + "GPIO135 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPECSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO136, name + " " + "GPIO136 Master CPU Select");
    all_zero &= check_zero(obj.GPIO137, name + " " + "GPIO137 Master CPU Select");
    all_zero &= check_zero(obj.GPIO138, name + " " + "GPIO138 Master CPU Select");
    all_zero &= check_zero(obj.GPIO139, name + " " + "GPIO139 Master CPU Select");
    all_zero &= check_zero(obj.GPIO140, name + " " + "GPIO140 Master CPU Select");
    all_zero &= check_zero(obj.GPIO141, name + " " + "GPIO141 Master CPU Select");
    all_zero &= check_zero(obj.GPIO142, name + " " + "GPIO142 Master CPU Select");
    all_zero &= check_zero(obj.GPIO143, name + " " + "GPIO143 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPECSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECSEL3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO144, name + " " + "GPIO144 Master CPU Select");
    all_zero &= check_zero(obj.GPIO145, name + " " + "GPIO145 Master CPU Select");
    all_zero &= check_zero(obj.GPIO146, name + " " + "GPIO146 Master CPU Select");
    all_zero &= check_zero(obj.GPIO147, name + " " + "GPIO147 Master CPU Select");
    all_zero &= check_zero(obj.GPIO148, name + " " + "GPIO148 Master CPU Select");
    all_zero &= check_zero(obj.GPIO149, name + " " + "GPIO149 Master CPU Select");
    all_zero &= check_zero(obj.GPIO150, name + " " + "GPIO150 Master CPU Select");
    all_zero &= check_zero(obj.GPIO151, name + " " + "GPIO151 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPECSEL3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECSEL4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO152, name + " " + "GPIO152 Master CPU Select");
    all_zero &= check_zero(obj.GPIO153, name + " " + "GPIO153 Master CPU Select");
    all_zero &= check_zero(obj.GPIO154, name + " " + "GPIO154 Master CPU Select");
    all_zero &= check_zero(obj.GPIO155, name + " " + "GPIO155 Master CPU Select");
    all_zero &= check_zero(obj.GPIO156, name + " " + "GPIO156 Master CPU Select");
    all_zero &= check_zero(obj.GPIO157, name + " " + "GPIO157 Master CPU Select");
    all_zero &= check_zero(obj.GPIO158, name + " " + "GPIO158 Master CPU Select");
    all_zero &= check_zero(obj.GPIO159, name + " " + "GPIO159 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPECSEL4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPELOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Configuration Lock bit for this pin");

    return all_zero;
}

bool check_zero(const GPELOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Configuration lock commit bit for this pin");

    return all_zero;
}

bool check_zero(const GPECR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFCTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.QUALPRD0, name + " " + "Qualification sampling period for GPIO160 to GPIO167");
    all_zero &= check_zero(obj.QUALPRD1, name + " " + "Qualification sampling period for GPIO168");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFCTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFQSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Select input qualification type for GPIO160");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Select input qualification type for GPIO161");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Select input qualification type for GPIO162");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Select input qualification type for GPIO163");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Select input qualification type for GPIO164");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Select input qualification type for GPIO165");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Select input qualification type for GPIO166");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Select input qualification type for GPIO167");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Select input qualification type for GPIO168");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFQSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Defines pin-muxing selection for GPIO160");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Defines pin-muxing selection for GPIO161");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Defines pin-muxing selection for GPIO162");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Defines pin-muxing selection for GPIO163");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Defines pin-muxing selection for GPIO164");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Defines pin-muxing selection for GPIO165");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Defines pin-muxing selection for GPIO166");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Defines pin-muxing selection for GPIO167");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Defines pin-muxing selection for GPIO168");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFDIR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Defines direction for this pin in GPIO mode");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFDIR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFPUD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Pull-Up Disable control for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFPUD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Input inversion control for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFODR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Outpout Open-Drain control for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFODR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFGMUX1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Defines pin-muxing selection for GPIO160");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Defines pin-muxing selection for GPIO161");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Defines pin-muxing selection for GPIO162");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Defines pin-muxing selection for GPIO163");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Defines pin-muxing selection for GPIO164");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Defines pin-muxing selection for GPIO165");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Defines pin-muxing selection for GPIO166");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Defines pin-muxing selection for GPIO167");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Defines pin-muxing selection for GPIO168");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFGMUX1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "GPIO160 Master CPU Select");
    all_zero &= check_zero(obj.GPIO161, name + " " + "GPIO161 Master CPU Select");
    all_zero &= check_zero(obj.GPIO162, name + " " + "GPIO162 Master CPU Select");
    all_zero &= check_zero(obj.GPIO163, name + " " + "GPIO163 Master CPU Select");
    all_zero &= check_zero(obj.GPIO164, name + " " + "GPIO164 Master CPU Select");
    all_zero &= check_zero(obj.GPIO165, name + " " + "GPIO165 Master CPU Select");
    all_zero &= check_zero(obj.GPIO166, name + " " + "GPIO166 Master CPU Select");
    all_zero &= check_zero(obj.GPIO167, name + " " + "GPIO167 Master CPU Select");

    return all_zero;
}

bool check_zero(const GPFCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO168, name + " " + "GPIO168 Master CPU Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Configuration Lock bit for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Configuration lock commit bit for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPIO_CTRL_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPACTRL, name + " " + "GPIO A Qualification Sampling Period Control (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPAQSEL1, name + " " + "GPIO A Qualifier Select 1 Register (GPIO0 to 15)");
    all_zero &= check_zero(obj.GPAQSEL2, name + " " + "GPIO A Qualifier Select 2 Register (GPIO16 to 31)");
    all_zero &= check_zero(obj.GPAMUX1, name + " " + "GPIO A Mux 1 Register (GPIO0 to 15)");
    all_zero &= check_zero(obj.GPAMUX2, name + " " + "GPIO A Mux 2 Register (GPIO16 to 31)");
    all_zero &= check_zero(obj.GPADIR, name + " " + "GPIO A Direction Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPAPUD, name + " " + "GPIO A Pull Up Disable Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPAINV, name + " " + "GPIO A Input Polarity Invert Registers (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPAODR, name + " " + "GPIO A Open Drain Output Register (GPIO0 to GPIO31)");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPAGMUX1, name + " " + "GPIO A Peripheral Group Mux (GPIO0 to 15)");
    all_zero &= check_zero(obj.GPAGMUX2, name + " " + "GPIO A Peripheral Group Mux (GPIO16 to 31)");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPACSEL1, name + " " + "GPIO A Core Select Register (GPIO0 to 7)");
    all_zero &= check_zero(obj.GPACSEL2, name + " " + "GPIO A Core Select Register (GPIO8 to 15)");
    all_zero &= check_zero(obj.GPACSEL3, name + " " + "GPIO A Core Select Register (GPIO16 to 23)");
    all_zero &= check_zero(obj.GPACSEL4, name + " " + "GPIO A Core Select Register (GPIO24 to 31)");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPALOCK, name + " " + "GPIO A Lock Configuration Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPACR, name + " " + "GPIO A Lock Commit Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPBCTRL, name + " " + "GPIO B Qualification Sampling Period Control (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBQSEL1, name + " " + "GPIO B Qualifier Select 1 Register (GPIO32 to 47)");
    all_zero &= check_zero(obj.GPBQSEL2, name + " " + "GPIO B Qualifier Select 2 Register (GPIO48 to 63)");
    all_zero &= check_zero(obj.GPBMUX1, name + " " + "GPIO B Mux 1 Register (GPIO32 to 47)");
    all_zero &= check_zero(obj.GPBMUX2, name + " " + "GPIO B Mux 2 Register (GPIO48 to 63)");
    all_zero &= check_zero(obj.GPBDIR, name + " " + "GPIO B Direction Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBPUD, name + " " + "GPIO B Pull Up Disable Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPBINV, name + " " + "GPIO B Input Polarity Invert Registers (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBODR, name + " " + "GPIO B Open Drain Output Register (GPIO32 to GPIO63)");
    all_zero &= check_zero(obj.GPBAMSEL, name + " " + "GPIO B Analog Mode Select register (GPIO32 to GPIO63)");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPBGMUX1, name + " " + "GPIO B Peripheral Group Mux (GPIO32 to 47)");
    all_zero &= check_zero(obj.GPBGMUX2, name + " " + "GPIO B Peripheral Group Mux (GPIO48 to 63)");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPBCSEL1, name + " " + "GPIO B Core Select Register (GPIO32 to 39)");
    all_zero &= check_zero(obj.GPBCSEL2, name + " " + "GPIO B Core Select Register (GPIO40 to 47)");
    all_zero &= check_zero(obj.GPBCSEL3, name + " " + "GPIO B Core Select Register (GPIO48 to 55)");
    all_zero &= check_zero(obj.GPBCSEL4, name + " " + "GPIO B Core Select Register (GPIO56 to 63)");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPBLOCK, name + " " + "GPIO B Lock Configuration Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBCR, name + " " + "GPIO B Lock Commit Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPCCTRL, name + " " + "GPIO C Qualification Sampling Period Control (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCQSEL1, name + " " + "GPIO C Qualifier Select 1 Register (GPIO64 to 79)");
    all_zero &= check_zero(obj.GPCQSEL2, name + " " + "GPIO C Qualifier Select 2 Register (GPIO80  to 95)");
    all_zero &= check_zero(obj.GPCMUX1, name + " " + "GPIO C Mux 1 Register (GPIO64 to 79)");
    all_zero &= check_zero(obj.GPCMUX2, name + " " + "GPIO C Mux 2 Register (GPIO80  to 95)");
    all_zero &= check_zero(obj.GPCDIR, name + " " + "GPIO C Direction Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCPUD, name + " " + "GPIO C Pull Up Disable Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPCINV, name + " " + "GPIO C Input Polarity Invert Registers (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCODR, name + " " + "GPIO C Open Drain Output Register (GPIO64 to GPIO95)");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPCGMUX1, name + " " + "GPIO C Peripheral Group Mux (GPIO64 to 79)");
    all_zero &= check_zero(obj.GPCGMUX2, name + " " + "GPIO C Peripheral Group Mux (GPIO80  to 95)");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPCCSEL1, name + " " + "GPIO C Core Select Register (GPIO64 to 71)");
    all_zero &= check_zero(obj.GPCCSEL2, name + " " + "GPIO C Core Select Register (GPIO72 to 79)");
    all_zero &= check_zero(obj.GPCCSEL3, name + " " + "GPIO C Core Select Register (GPIO80  to 87)");
    all_zero &= check_zero(obj.GPCCSEL4, name + " " + "GPIO C Core Select Register (GPIO88 to 95)");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPCLOCK, name + " " + "GPIO C Lock Configuration Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCCR, name + " " + "GPIO C Lock Commit Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPDCTRL, name + " " + "GPIO D Qualification Sampling Period Control (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDQSEL1, name + " " + "GPIO D Qualifier Select 1 Register (GPIO96 to 111)");
    all_zero &= check_zero(obj.GPDQSEL2, name + " " + "GPIO D Qualifier Select 2 Register (GPIO112 to 127)");
    all_zero &= check_zero(obj.GPDMUX1, name + " " + "GPIO D Mux 1 Register (GPIO96 to 111)");
    all_zero &= check_zero(obj.GPDMUX2, name + " " + "GPIO D Mux 2 Register (GPIO112 to 127)");
    all_zero &= check_zero(obj.GPDDIR, name + " " + "GPIO D Direction Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDPUD, name + " " + "GPIO D Pull Up Disable Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPDINV, name + " " + "GPIO D Input Polarity Invert Registers (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDODR, name + " " + "GPIO D Open Drain Output Register (GPIO96 to GPIO127)");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPDGMUX1, name + " " + "GPIO D Peripheral Group Mux (GPIO96 to 111)");
    all_zero &= check_zero(obj.GPDGMUX2, name + " " + "GPIO D Peripheral Group Mux (GPIO112 to 127)");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPDCSEL1, name + " " + "GPIO D Core Select Register (GPIO96 to 103)");
    all_zero &= check_zero(obj.GPDCSEL2, name + " " + "GPIO D Core Select Register (GPIO104 to 111)");
    all_zero &= check_zero(obj.GPDCSEL3, name + " " + "GPIO D Core Select Register (GPIO112 to 119)");
    all_zero &= check_zero(obj.GPDCSEL4, name + " " + "GPIO D Core Select Register (GPIO120 to 127)");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPDLOCK, name + " " + "GPIO D Lock Configuration Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDCR, name + " " + "GPIO D Lock Commit Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPECTRL, name + " " + "GPIO E Qualification Sampling Period Control (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPEQSEL1, name + " " + "GPIO E Qualifier Select 1 Register (GPIO128 to 143)");
    all_zero &= check_zero(obj.GPEQSEL2, name + " " + "GPIO E Qualifier Select 2 Register (GPIO144  to 159)");
    all_zero &= check_zero(obj.GPEMUX1, name + " " + "GPIO E Mux 1 Register (GPIO128 to 143)");
    all_zero &= check_zero(obj.GPEMUX2, name + " " + "GPIO E Mux 2 Register (GPIO144  to 159)");
    all_zero &= check_zero(obj.GPEDIR, name + " " + "GPIO E Direction Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPEPUD, name + " " + "GPIO E Pull Up Disable Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPEINV, name + " " + "GPIO E Input Polarity Invert Registers (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPEODR, name + " " + "GPIO E Open Drain Output Register (GPIO128 to GPIO159)");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPEGMUX1, name + " " + "GPIO E Peripheral Group Mux (GPIO128 to 143)");
    all_zero &= check_zero(obj.GPEGMUX2, name + " " + "GPIO E Peripheral Group Mux (GPIO144  to 159)");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPECSEL1, name + " " + "GPIO E Core Select Register (GPIO128 to 135)");
    all_zero &= check_zero(obj.GPECSEL2, name + " " + "GPIO E Core Select Register (GPIO136 to 143)");
    all_zero &= check_zero(obj.GPECSEL3, name + " " + "GPIO E Core Select Register (GPIO144  to 151)");
    all_zero &= check_zero(obj.GPECSEL4, name + " " + "GPIO E Core Select Register (GPIO152 to 159)");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPELOCK, name + " " + "GPIO E Lock Configuration Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPECR, name + " " + "GPIO E Lock Commit Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPFCTRL, name + " " + "GPIO F Qualification Sampling Period Control (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFQSEL1, name + " " + "GPIO F Qualifier Select 1 Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFMUX1, name + " " + "GPIO F Mux 1 Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFDIR, name + " " + "GPIO F Direction Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFPUD, name + " " + "GPIO F Pull Up Disable Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFINV, name + " " + "GPIO F Input Polarity Invert Registers (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFODR, name + " " + "GPIO F Open Drain Output Register (GPIO160 to GPIO168)");
    all_zero &= check_zero(obj.rsvd24, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFGMUX1, name + " " + "GPIO F Peripheral Group Mux (GPIO160 to 168)");
    all_zero &= check_zero(obj.rsvd25, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFCSEL1, name + " " + "GPIO F Core Select Register (GPIO160 to 167)");
    all_zero &= check_zero(obj.GPFCSEL2, name + " " + "GPIO F Core Select Register (GPIO168)");
    all_zero &= check_zero(obj.rsvd26, name + " " + "Reserved");
    all_zero &= check_zero(obj.GPFLOCK, name + " " + "GPIO F Lock Configuration Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFCR, name + " " + "GPIO F Lock Commit Register (GPIO160 to 168)");

    return all_zero;
}

bool check_zero(const GPADAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Data Register for this pin");

    return all_zero;
}

bool check_zero(const GPADAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPASET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Output Set bit for this pin");

    return all_zero;
}

bool check_zero(const GPASET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPACLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Output Clear bit for this pin");

    return all_zero;
}

bool check_zero(const GPACLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPATOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO0, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO1, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO2, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO3, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO4, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO5, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO6, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO7, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO8, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO9, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO10, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO11, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO12, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO13, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO14, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO15, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO16, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO17, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO18, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO19, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO20, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO21, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO22, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO23, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO24, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO25, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO26, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO27, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO28, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO29, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO30, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO31, name + " " + "Output Toggle bit for this pin");

    return all_zero;
}

bool check_zero(const GPATOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBDAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Data Register for this pin");

    return all_zero;
}

bool check_zero(const GPBDAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Output Set bit for this pin");

    return all_zero;
}

bool check_zero(const GPBSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBCLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Output Clear bit for this pin");

    return all_zero;
}

bool check_zero(const GPBCLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPBTOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO32, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO33, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO34, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO35, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO36, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO37, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO38, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO39, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO40, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO41, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO42, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO43, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO44, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO45, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO46, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO47, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO48, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO49, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO50, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO51, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO52, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO53, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO54, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO55, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO56, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO57, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO58, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO59, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO60, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO61, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO62, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO63, name + " " + "Output Toggle bit for this pin");

    return all_zero;
}

bool check_zero(const GPBTOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCDAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Data Register for this pin");

    return all_zero;
}

bool check_zero(const GPCDAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Output Set bit for this pin");

    return all_zero;
}

bool check_zero(const GPCSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCCLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Output Clear bit for this pin");

    return all_zero;
}

bool check_zero(const GPCCLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPCTOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO64, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO65, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO66, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO67, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO68, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO69, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO70, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO71, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO72, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO73, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO74, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO75, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO76, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO77, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO78, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO79, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO80, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO81, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO82, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO83, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO84, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO85, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO86, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO87, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO88, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO89, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO90, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO91, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO92, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO93, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO94, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO95, name + " " + "Output Toggle bit for this pin");

    return all_zero;
}

bool check_zero(const GPCTOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDDAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Data Register for this pin");

    return all_zero;
}

bool check_zero(const GPDDAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Output Set bit for this pin");

    return all_zero;
}

bool check_zero(const GPDSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDCLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Output Clear bit for this pin");

    return all_zero;
}

bool check_zero(const GPDCLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPDTOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO96, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO97, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO98, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO99, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO100, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO101, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO102, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO103, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO104, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO105, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO106, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO107, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO108, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO109, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO110, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO111, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO112, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO113, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO114, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO115, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO116, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO117, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO118, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO119, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO120, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO121, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO122, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO123, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO124, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO125, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO126, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO127, name + " " + "Output Toggle bit for this pin");

    return all_zero;
}

bool check_zero(const GPDTOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPEDAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Data Register for this pin");

    return all_zero;
}

bool check_zero(const GPEDAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPESET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Output Set bit for this pin");

    return all_zero;
}

bool check_zero(const GPESET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPECLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Output Clear bit for this pin");

    return all_zero;
}

bool check_zero(const GPECLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPETOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO128, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO129, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO130, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO131, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO132, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO133, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO134, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO135, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO136, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO137, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO138, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO139, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO140, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO141, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO142, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO143, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO144, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO145, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO146, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO147, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO148, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO149, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO150, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO151, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO152, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO153, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO154, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO155, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO156, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO157, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO158, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO159, name + " " + "Output Toggle bit for this pin");

    return all_zero;
}

bool check_zero(const GPETOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFDAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Data Register for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFDAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFSET_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Output Set bit for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFSET_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFCLEAR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Output Clear bit for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFCLEAR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPFTOGGLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPIO160, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO161, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO162, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO163, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO164, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO165, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO166, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO167, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.GPIO168, name + " " + "Output Toggle bit for this pin");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd18, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd19, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd20, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd21, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd22, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd23, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const GPFTOGGLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const GPIO_DATA_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.GPADAT, name + " " + "GPIO A Data Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPASET, name + " " + "GPIO A Data Set Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPACLEAR, name + " " + "GPIO A Data Clear Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPATOGGLE, name + " " + "GPIO A Data Toggle Register (GPIO0 to 31)");
    all_zero &= check_zero(obj.GPBDAT, name + " " + "GPIO B Data Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBSET, name + " " + "GPIO B Data Set Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBCLEAR, name + " " + "GPIO B Data Clear Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPBTOGGLE, name + " " + "GPIO B Data Toggle Register (GPIO32 to 63)");
    all_zero &= check_zero(obj.GPCDAT, name + " " + "GPIO C Data Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCSET, name + " " + "GPIO C Data Set Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCCLEAR, name + " " + "GPIO C Data Clear Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPCTOGGLE, name + " " + "GPIO C Data Toggle Register (GPIO64 to 95)");
    all_zero &= check_zero(obj.GPDDAT, name + " " + "GPIO D Data Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDSET, name + " " + "GPIO D Data Set Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDCLEAR, name + " " + "GPIO D Data Clear Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPDTOGGLE, name + " " + "GPIO D Data Toggle Register (GPIO96 to 127)");
    all_zero &= check_zero(obj.GPEDAT, name + " " + "GPIO E Data Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPESET, name + " " + "GPIO E Data Set Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPECLEAR, name + " " + "GPIO E Data Clear Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPETOGGLE, name + " " + "GPIO E Data Toggle Register (GPIO128 to 159)");
    all_zero &= check_zero(obj.GPFDAT, name + " " + "GPIO F Data Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFSET, name + " " + "GPIO F Data Set Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFCLEAR, name + " " + "GPIO F Data Clear Register (GPIO160 to 168)");
    all_zero &= check_zero(obj.GPFTOGGLE, name + " " + "GPIO F Data Toggle Register (GPIO160 to 168)");

    return all_zero;
}

bool check_zero(const PIECTRL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENPIE, name + " " + "PIE Enable");
    all_zero &= check_zero(obj.PIEVECT, name + " " + "PIE Vector Address");

    return all_zero;
}

bool check_zero(const PIECTRL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEACK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACK1, name + " " + "Acknowledge PIE Interrupt Group 1");
    all_zero &= check_zero(obj.ACK2, name + " " + "Acknowledge PIE Interrupt Group 2");
    all_zero &= check_zero(obj.ACK3, name + " " + "Acknowledge PIE Interrupt Group 3");
    all_zero &= check_zero(obj.ACK4, name + " " + "Acknowledge PIE Interrupt Group 4");
    all_zero &= check_zero(obj.ACK5, name + " " + "Acknowledge PIE Interrupt Group 5");
    all_zero &= check_zero(obj.ACK6, name + " " + "Acknowledge PIE Interrupt Group 6");
    all_zero &= check_zero(obj.ACK7, name + " " + "Acknowledge PIE Interrupt Group 7");
    all_zero &= check_zero(obj.ACK8, name + " " + "Acknowledge PIE Interrupt Group 8");
    all_zero &= check_zero(obj.ACK9, name + " " + "Acknowledge PIE Interrupt Group 9");
    all_zero &= check_zero(obj.ACK10, name + " " + "Acknowledge PIE Interrupt Group 10");
    all_zero &= check_zero(obj.ACK11, name + " " + "Acknowledge PIE Interrupt Group 11");
    all_zero &= check_zero(obj.ACK12, name + " " + "Acknowledge PIE Interrupt Group 12");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const PIEACK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 1.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 1.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 1.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 1.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 1.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 1.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 1.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 1.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 1.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 1.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 1.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 1.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 1.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 1.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 1.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 1.16");

    return all_zero;
}

bool check_zero(const PIEIER1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 1.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 1.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 1.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 1.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 1.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 1.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 1.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 1.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 1.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 1.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 1.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 1.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 1.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 1.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 1.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 1.16");

    return all_zero;
}

bool check_zero(const PIEIFR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 2.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 2.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 2.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 2.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 2.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 2.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 2.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 2.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 2.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 2.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 2.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 2.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 2.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 2.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 2.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 2.16");

    return all_zero;
}

bool check_zero(const PIEIER2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 2.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 2.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 2.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 2.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 2.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 2.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 2.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 2.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 2.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 2.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 2.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 2.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 2.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 2.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 2.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 2.16");

    return all_zero;
}

bool check_zero(const PIEIFR2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 3.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 3.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 3.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 3.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 3.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 3.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 3.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 3.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 3.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 3.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 3.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 3.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 3.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 3.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 3.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 3.16");

    return all_zero;
}

bool check_zero(const PIEIER3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR3_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 3.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 3.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 3.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 3.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 3.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 3.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 3.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 3.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 3.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 3.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 3.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 3.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 3.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 3.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 3.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 3.16");

    return all_zero;
}

bool check_zero(const PIEIFR3_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 4.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 4.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 4.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 4.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 4.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 4.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 4.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 4.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 4.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 4.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 4.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 4.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 4.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 4.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 4.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 4.16");

    return all_zero;
}

bool check_zero(const PIEIER4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 4.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 4.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 4.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 4.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 4.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 4.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 4.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 4.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 4.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 4.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 4.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 4.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 4.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 4.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 4.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 4.16");

    return all_zero;
}

bool check_zero(const PIEIFR4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER5_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 5.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 5.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 5.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 5.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 5.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 5.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 5.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 5.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 5.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 5.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 5.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 5.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 5.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 5.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 5.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 5.16");

    return all_zero;
}

bool check_zero(const PIEIER5_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR5_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 5.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 5.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 5.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 5.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 5.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 5.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 5.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 5.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 5.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 5.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 5.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 5.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 5.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 5.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 5.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 5.16");

    return all_zero;
}

bool check_zero(const PIEIFR5_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 6.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 6.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 6.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 6.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 6.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 6.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 6.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 6.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 6.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 6.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 6.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 6.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 6.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 6.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 6.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 6.16");

    return all_zero;
}

bool check_zero(const PIEIER6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR6_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 6.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 6.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 6.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 6.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 6.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 6.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 6.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 6.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 6.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 6.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 6.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 6.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 6.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 6.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 6.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 6.16");

    return all_zero;
}

bool check_zero(const PIEIFR6_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 7.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 7.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 7.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 7.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 7.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 7.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 7.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 7.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 7.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 7.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 7.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 7.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 7.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 7.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 7.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 7.16");

    return all_zero;
}

bool check_zero(const PIEIER7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR7_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 7.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 7.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 7.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 7.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 7.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 7.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 7.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 7.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 7.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 7.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 7.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 7.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 7.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 7.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 7.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 7.16");

    return all_zero;
}

bool check_zero(const PIEIFR7_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 8.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 8.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 8.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 8.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 8.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 8.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 8.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 8.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 8.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 8.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 8.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 8.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 8.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 8.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 8.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 8.16");

    return all_zero;
}

bool check_zero(const PIEIER8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR8_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 8.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 8.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 8.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 8.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 8.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 8.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 8.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 8.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 8.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 8.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 8.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 8.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 8.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 8.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 8.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 8.16");

    return all_zero;
}

bool check_zero(const PIEIFR8_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 9.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 9.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 9.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 9.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 9.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 9.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 9.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 9.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 9.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 9.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 9.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 9.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 9.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 9.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 9.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 9.16");

    return all_zero;
}

bool check_zero(const PIEIER9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR9_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 9.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 9.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 9.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 9.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 9.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 9.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 9.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 9.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 9.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 9.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 9.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 9.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 9.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 9.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 9.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 9.16");

    return all_zero;
}

bool check_zero(const PIEIFR9_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER10_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 10.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 10.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 10.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 10.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 10.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 10.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 10.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 10.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 10.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 10.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 10.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 10.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 10.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 10.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 10.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 10.16");

    return all_zero;
}

bool check_zero(const PIEIER10_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR10_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 10.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 10.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 10.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 10.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 10.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 10.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 10.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 10.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 10.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 10.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 10.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 10.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 10.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 10.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 10.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 10.16");

    return all_zero;
}

bool check_zero(const PIEIFR10_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 11.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 11.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 11.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 11.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 11.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 11.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 11.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 11.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 11.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 11.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 11.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 11.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 11.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 11.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 11.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 11.16");

    return all_zero;
}

bool check_zero(const PIEIER11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR11_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 11.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 11.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 11.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 11.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 11.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 11.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 11.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 11.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 11.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 11.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 11.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 11.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 11.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 11.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 11.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 11.16");

    return all_zero;
}

bool check_zero(const PIEIFR11_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIER12_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Enable for Interrupt 12.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Enable for Interrupt 12.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Enable for Interrupt 12.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Enable for Interrupt 12.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Enable for Interrupt 12.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Enable for Interrupt 12.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Enable for Interrupt 12.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Enable for Interrupt 12.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Enable for Interrupt 12.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Enable for Interrupt 12.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Enable for Interrupt 12.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Enable for Interrupt 12.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Enable for Interrupt 12.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Enable for Interrupt 12.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Enable for Interrupt 12.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Enable for Interrupt 12.16");

    return all_zero;
}

bool check_zero(const PIEIER12_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIEIFR12_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INTx1, name + " " + "Flag for Interrupt 12.1");
    all_zero &= check_zero(obj.INTx2, name + " " + "Flag for Interrupt 12.2");
    all_zero &= check_zero(obj.INTx3, name + " " + "Flag for Interrupt 12.3");
    all_zero &= check_zero(obj.INTx4, name + " " + "Flag for Interrupt 12.4");
    all_zero &= check_zero(obj.INTx5, name + " " + "Flag for Interrupt 12.5");
    all_zero &= check_zero(obj.INTx6, name + " " + "Flag for Interrupt 12.6");
    all_zero &= check_zero(obj.INTx7, name + " " + "Flag for Interrupt 12.7");
    all_zero &= check_zero(obj.INTx8, name + " " + "Flag for Interrupt 12.8");
    all_zero &= check_zero(obj.INTx9, name + " " + "Flag for Interrupt 12.9");
    all_zero &= check_zero(obj.INTx10, name + " " + "Flag for Interrupt 12.10");
    all_zero &= check_zero(obj.INTx11, name + " " + "Flag for Interrupt 12.11");
    all_zero &= check_zero(obj.INTx12, name + " " + "Flag for Interrupt 12.12");
    all_zero &= check_zero(obj.INTx13, name + " " + "Flag for Interrupt 12.13");
    all_zero &= check_zero(obj.INTx14, name + " " + "Flag for Interrupt 12.14");
    all_zero &= check_zero(obj.INTx15, name + " " + "Flag for Interrupt 12.15");
    all_zero &= check_zero(obj.INTx16, name + " " + "Flag for Interrupt 12.16");

    return all_zero;
}

bool check_zero(const PIEIFR12_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PIE_CTRL_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PIECTRL, name + " " + "ePIE Control Register");
    all_zero &= check_zero(obj.PIEACK, name + " " + "Interrupt Acknowledge Register");
    all_zero &= check_zero(obj.PIEIER1, name + " " + "Interrupt Group 1 Enable Register");
    all_zero &= check_zero(obj.PIEIFR1, name + " " + "Interrupt Group 1 Flag Register");
    all_zero &= check_zero(obj.PIEIER2, name + " " + "Interrupt Group 2 Enable Register");
    all_zero &= check_zero(obj.PIEIFR2, name + " " + "Interrupt Group 2 Flag Register");
    all_zero &= check_zero(obj.PIEIER3, name + " " + "Interrupt Group 3 Enable Register");
    all_zero &= check_zero(obj.PIEIFR3, name + " " + "Interrupt Group 3 Flag Register");
    all_zero &= check_zero(obj.PIEIER4, name + " " + "Interrupt Group 4 Enable Register");
    all_zero &= check_zero(obj.PIEIFR4, name + " " + "Interrupt Group 4 Flag Register");
    all_zero &= check_zero(obj.PIEIER5, name + " " + "Interrupt Group 5 Enable Register");
    all_zero &= check_zero(obj.PIEIFR5, name + " " + "Interrupt Group 5 Flag Register");
    all_zero &= check_zero(obj.PIEIER6, name + " " + "Interrupt Group 6 Enable Register");
    all_zero &= check_zero(obj.PIEIFR6, name + " " + "Interrupt Group 6 Flag Register");
    all_zero &= check_zero(obj.PIEIER7, name + " " + "Interrupt Group 7 Enable Register");
    all_zero &= check_zero(obj.PIEIFR7, name + " " + "Interrupt Group 7 Flag Register");
    all_zero &= check_zero(obj.PIEIER8, name + " " + "Interrupt Group 8 Enable Register");
    all_zero &= check_zero(obj.PIEIFR8, name + " " + "Interrupt Group 8 Flag Register");
    all_zero &= check_zero(obj.PIEIER9, name + " " + "Interrupt Group 9 Enable Register");
    all_zero &= check_zero(obj.PIEIFR9, name + " " + "Interrupt Group 9 Flag Register");
    all_zero &= check_zero(obj.PIEIER10, name + " " + "Interrupt Group 10 Enable Register");
    all_zero &= check_zero(obj.PIEIFR10, name + " " + "Interrupt Group 10 Flag Register");
    all_zero &= check_zero(obj.PIEIER11, name + " " + "Interrupt Group 11 Enable Register");
    all_zero &= check_zero(obj.PIEIFR11, name + " " + "Interrupt Group 11 Flag Register");
    all_zero &= check_zero(obj.PIEIER12, name + " " + "Interrupt Group 12 Enable Register");
    all_zero &= check_zero(obj.PIEIFR12, name + " " + "Interrupt Group 12 Flag Register");

    return all_zero;
}

bool check_zero(const XINT1CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "XINT1 Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.POLARITY, name + " " + "XINT1 Polarity");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XINT1CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XINT2CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "XINT2 Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.POLARITY, name + " " + "XINT2 Polarity");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XINT2CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XINT3CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "XINT3 Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.POLARITY, name + " " + "XINT3 Polarity");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XINT3CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XINT4CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "XINT4 Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.POLARITY, name + " " + "XINT4 Polarity");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XINT4CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XINT5CR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "XINT5 Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.POLARITY, name + " " + "XINT5 Polarity");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const XINT5CR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const XINT_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.XINT1CR, name + " " + "XINT1 configuration register");
    all_zero &= check_zero(obj.XINT2CR, name + " " + "XINT2 configuration register");
    all_zero &= check_zero(obj.XINT3CR, name + " " + "XINT3 configuration register");
    all_zero &= check_zero(obj.XINT4CR, name + " " + "XINT4 configuration register");
    all_zero &= check_zero(obj.XINT5CR, name + " " + "XINT5 configuration register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.XINT1CTR, name + " " + "XINT1 counter register");
    all_zero &= check_zero(obj.XINT2CTR, name + " " + "XINT2 counter register");
    all_zero &= check_zero(obj.XINT3CTR, name + " " + "XINT3 counter register");

    return all_zero;
}

bool check_zero(const COMPCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMPHSOURCE, name + " " + "High Comparator Source Select");
    all_zero &= check_zero(obj.COMPHINV, name + " " + "High Comparator Invert Select");
    all_zero &= check_zero(obj.CTRIPHSEL, name + " " + "High Comparator Trip Select");
    all_zero &= check_zero(obj.CTRIPOUTHSEL, name + " " + "High Comparator Trip Output Select");
    all_zero &= check_zero(obj.ASYNCHEN, name + " " + "High Comparator Asynchronous Path Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.COMPLSOURCE, name + " " + "Low Comparator Source Select");
    all_zero &= check_zero(obj.COMPLINV, name + " " + "Low Comparator Invert Select");
    all_zero &= check_zero(obj.CTRIPLSEL, name + " " + "Low Comparator Trip Select");
    all_zero &= check_zero(obj.CTRIPOUTLSEL, name + " " + "Low Comparator Trip Output Select");
    all_zero &= check_zero(obj.ASYNCLEN, name + " " + "Low Comparator Asynchronous Path Enable");
    all_zero &= check_zero(obj.COMPDACE, name + " " + "Comparator/DAC Enable");

    return all_zero;
}

bool check_zero(const COMPCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const COMPHYSCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMPHYS, name + " " + "Comparator Hysteresis Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const COMPHYSCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const COMPSTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMPHSTS, name + " " + "High Comparator Status");
    all_zero &= check_zero(obj.COMPHLATCH, name + " " + "High Comparator Latched Status");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.COMPLSTS, name + " " + "Low Comparator Status");
    all_zero &= check_zero(obj.COMPLLATCH, name + " " + "Low Comparator Latched Status");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const COMPSTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const COMPSTSCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.HLATCHCLR, name + " " + "High Comparator Latched Status Clear");
    all_zero &= check_zero(obj.HSYNCCLREN, name + " " + "High Comparator EPWMSYNCPER Clear Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.LLATCHCLR, name + " " + "Low Comparator Latched Status Clear");
    all_zero &= check_zero(obj.LSYNCCLREN, name + " " + "Low Comparator EPWMSYNCPER Clear Enable");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const COMPSTSCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const COMPDACCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACSOURCE, name + " " + "DAC Source Control");
    all_zero &= check_zero(obj.RAMPSOURCE, name + " " + "Ramp Generator Source Control");
    all_zero &= check_zero(obj.SELREF, name + " " + "DAC Reference Select");
    all_zero &= check_zero(obj.RAMPLOADSEL, name + " " + "Ramp Load Select");
    all_zero &= check_zero(obj.SWLOADSEL, name + " " + "Software Load Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FREESOFT, name + " " + "Free/Soft Emulation Bits");

    return all_zero;
}

bool check_zero(const COMPDACCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACHVALS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVAL, name + " " + "DAC Value Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACHVALS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACHVALA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVAL, name + " " + "DAC Value Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACHVALA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACLVALS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVAL, name + " " + "DAC Value Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACLVALS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const DACLVALA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DACVAL, name + " " + "DAC Value Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const DACLVALA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RAMPDLYA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DELAY, name + " " + "Ramp Delay Value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const RAMPDLYA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const RAMPDLYS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DELAY, name + " " + "Ramp Delay Value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const RAMPDLYS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CTRIPLFILCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SAMPWIN, name + " " + "Sample Window");
    all_zero &= check_zero(obj.THRESH, name + " " + "Majority Voting Threshold");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FILINIT, name + " " + "Filter Initialization Bit");

    return all_zero;
}

bool check_zero(const CTRIPLFILCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CTRIPLFILCLKCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKPRESCALE, name + " " + "Sample Clock Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CTRIPLFILCLKCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CTRIPHFILCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SAMPWIN, name + " " + "Sample Window");
    all_zero &= check_zero(obj.THRESH, name + " " + "Majority Voting Threshold");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.FILINIT, name + " " + "Filter Initialization Bit");

    return all_zero;
}

bool check_zero(const CTRIPHFILCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CTRIPHFILCLKCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CLKPRESCALE, name + " " + "Sample Clock Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const CTRIPHFILCLKCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const COMPLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMPCTL, name + " " + "COMPCTL Lock");
    all_zero &= check_zero(obj.COMPHYSCTL, name + " " + "COMPHYSCTL Lock");
    all_zero &= check_zero(obj.DACCTL, name + " " + "DACCTL Lock");
    all_zero &= check_zero(obj.CTRIP, name + " " + "CTRIP Lock");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const COMPLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CMPSS_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.COMPCTL, name + " " + "CMPSS Comparator Control Register");
    all_zero &= check_zero(obj.COMPHYSCTL, name + " " + "CMPSS Comparator Hysteresis Control Register");
    all_zero &= check_zero(obj.COMPSTS, name + " " + "CMPSS Comparator Status Register");
    all_zero &= check_zero(obj.COMPSTSCLR, name + " " + "CMPSS Comparator Status Clear Register");
    all_zero &= check_zero(obj.COMPDACCTL, name + " " + "CMPSS DAC Control Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.DACHVALS, name + " " + "CMPSS High DAC Value Shadow Register");
    all_zero &= check_zero(obj.DACHVALA, name + " " + "CMPSS High DAC Value Active Register");
    all_zero &= check_zero(obj.RAMPMAXREFA, name + " " + "CMPSS Ramp Max Reference Active Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.RAMPMAXREFS, name + " " + "CMPSS Ramp Max Reference Shadow Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.RAMPDECVALA, name + " " + "CMPSS Ramp Decrement Value Active Register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.RAMPDECVALS, name + " " + "CMPSS Ramp Decrement Value Shadow Register");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.RAMPSTS, name + " " + "CMPSS Ramp Status Register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.DACLVALS, name + " " + "CMPSS Low DAC Value Shadow Register");
    all_zero &= check_zero(obj.DACLVALA, name + " " + "CMPSS Low DAC Value Active Register");
    all_zero &= check_zero(obj.RAMPDLYA, name + " " + "CMPSS Ramp Delay Active Register");
    all_zero &= check_zero(obj.RAMPDLYS, name + " " + "CMPSS Ramp Delay Shadow Register");
    all_zero &= check_zero(obj.CTRIPLFILCTL, name + " " + "CTRIPL Filter Control Register");
    all_zero &= check_zero(obj.CTRIPLFILCLKCTL, name + " " + "CTRIPL Filter Clock Control Register");
    all_zero &= check_zero(obj.CTRIPHFILCTL, name + " " + "CTRIPH Filter Control Register");
    all_zero &= check_zero(obj.CTRIPHFILCLKCTL, name + " " + "CTRIPH Filter Clock Control Register");
    all_zero &= check_zero(obj.COMPLOCK, name + " " + "CMPSS Lock Register");

    return all_zero;
}

bool check_zero(const TIM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LSW, name + " " + "CPU-Timer Counter Registers");
    all_zero &= check_zero(obj.MSW, name + " " + "CPU-Timer Counter Registers High");

    return all_zero;
}

bool check_zero(const TIM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const PRD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LSW, name + " " + "CPU-Timer Period Registers");
    all_zero &= check_zero(obj.MSW, name + " " + "CPU-Timer Period Registers High");

    return all_zero;
}

bool check_zero(const PRD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TSS, name + " " + "CPU-Timer stop status bit.");
    all_zero &= check_zero(obj.TRB, name + " " + "Timer reload");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SOFT, name + " " + "Emulation modes");
    all_zero &= check_zero(obj.FREE, name + " " + "Emulation modes");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.TIE, name + " " + "CPU-Timer Interrupt Enable.");
    all_zero &= check_zero(obj.TIF, name + " " + "CPU-Timer Interrupt Flag.");

    return all_zero;
}

bool check_zero(const TCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TPR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TDDR, name + " " + "CPU-Timer Divide-Down.");
    all_zero &= check_zero(obj.PSC, name + " " + "CPU-Timer Prescale Counter.");

    return all_zero;
}

bool check_zero(const TPR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TPRH_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TDDRH, name + " " + "CPU-Timer Divide-Down.");
    all_zero &= check_zero(obj.PSCH, name + " " + "CPU-Timer Prescale Counter.");

    return all_zero;
}

bool check_zero(const TPRH_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const CPUTIMER_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TIM, name + " " + "CPU-Timer, Counter Register");
    all_zero &= check_zero(obj.PRD, name + " " + "CPU-Timer, Period Register");
    all_zero &= check_zero(obj.TCR, name + " " + "CPU-Timer, Control Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TPR, name + " " + "CPU-Timer, Prescale Register");
    all_zero &= check_zero(obj.TPRH, name + " " + "CPU-Timer, Prescale Register High");

    return all_zero;
}

bool check_zero(const SPICCR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPICHAR, name + " " + "Character Length Control");
    all_zero &= check_zero(obj.SPILBK, name + " " + "SPI Loopback");
    all_zero &= check_zero(obj.HS_MODE, name + " " + "High Speed mode control");
    all_zero &= check_zero(obj.CLKPOLARITY, name + " " + "Shift Clock Polarity");
    all_zero &= check_zero(obj.SPISWRESET, name + " " + "SPI Software Reset");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPICCR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPICTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPIINTENA, name + " " + "SPI Interupt Enable");
    all_zero &= check_zero(obj.TALK, name + " " + "Master/Slave Transmit Enable");
    all_zero &= check_zero(obj.MASTER_SLAVE, name + " " + "SPI Network Mode Control");
    all_zero &= check_zero(obj.CLK_PHASE, name + " " + "SPI Clock Phase");
    all_zero &= check_zero(obj.OVERRUNINTENA, name + " " + "Overrun Interrupt Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPICTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPISTS_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.BUFFULL_FLAG, name + " " + "SPI Transmit Buffer Full Flag");
    all_zero &= check_zero(obj.INT_FLAG, name + " " + "SPI Interrupt Flag");
    all_zero &= check_zero(obj.OVERRUN_FLAG, name + " " + "SPI Receiver Overrun Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPISTS_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPIBRR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPI_BIT_RATE, name + " " + "SPI Bit Rate Control");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPIBRR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPIFFTX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXFFIL, name + " " + "TXFIFO Interrupt Level");
    all_zero &= check_zero(obj.TXFFIENA, name + " " + "TXFIFO Interrupt Enable");
    all_zero &= check_zero(obj.TXFFINTCLR, name + " " + "TXFIFO Interrupt Clear");
    all_zero &= check_zero(obj.TXFFINT, name + " " + "TXFIFO Interrupt Flag");
    all_zero &= check_zero(obj.TXFFST, name + " " + "Transmit FIFO Status");
    all_zero &= check_zero(obj.TXFIFO, name + " " + "TXFIFO Reset");
    all_zero &= check_zero(obj.SPIFFENA, name + " " + "FIFO Enhancements Enable");
    all_zero &= check_zero(obj.SPIRST, name + " " + "SPI Reset");

    return all_zero;
}

bool check_zero(const SPIFFTX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPIFFRX_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.RXFFIL, name + " " + "RXFIFO Interrupt Level");
    all_zero &= check_zero(obj.RXFFIENA, name + " " + "RXFIFO Interrupt Enable");
    all_zero &= check_zero(obj.RXFFINTCLR, name + " " + "RXFIFO Interupt Clear");
    all_zero &= check_zero(obj.RXFFINT, name + " " + "RXFIFO Interrupt Flag");
    all_zero &= check_zero(obj.RXFFST, name + " " + "Receive FIFO Status");
    all_zero &= check_zero(obj.RXFIFORESET, name + " " + "RXFIFO Reset");
    all_zero &= check_zero(obj.RXFFOVFCLR, name + " " + "Receive FIFO Overflow Clear");
    all_zero &= check_zero(obj.RXFFOVF, name + " " + "Receive FIFO Overflow Flag");

    return all_zero;
}

bool check_zero(const SPIFFRX_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPIFFCT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TXDLY, name + " " + "FIFO Transmit Delay Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPIFFCT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPIPRI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIWIRE, name + " " + "3-wire mode select bit");
    all_zero &= check_zero(obj.STEINV, name + " " + "SPISTE inversion bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.FREE, name + " " + "Free emulation mode");
    all_zero &= check_zero(obj.SOFT, name + " " + "Soft emulation mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const SPIPRI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const SPI_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SPICCR, name + " " + "SPI Configuration Control Register");
    all_zero &= check_zero(obj.SPICTL, name + " " + "SPI Operation Control Register");
    all_zero &= check_zero(obj.SPISTS, name + " " + "SPI Status Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.SPIBRR, name + " " + "SPI Baud Rate Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.SPIRXEMU, name + " " + "SPI Emulation Buffer Register");
    all_zero &= check_zero(obj.SPIRXBUF, name + " " + "SPI Serial Input Buffer Register");
    all_zero &= check_zero(obj.SPITXBUF, name + " " + "SPI Serial Output Buffer Register");
    all_zero &= check_zero(obj.SPIDAT, name + " " + "SPI Serial Data Register");
    all_zero &= check_zero(obj.SPIFFTX, name + " " + "SPI FIFO Transmit Register");
    all_zero &= check_zero(obj.SPIFFRX, name + " " + "SPI FIFO Receive Register");
    all_zero &= check_zero(obj.SPIFFCT, name + " " + "SPI FIFO Control Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.SPIPRI, name + " " + "SPI Priority Control Register");

    return all_zero;
}

bool check_zero(const ADCCTL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INTPULSEPOS, name + " " + "ADC Interrupt Pulse Position");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCPWDNZ, name + " " + "ADC Power Down");
    all_zero &= check_zero(obj.ADCBSYCHN, name + " " + "ADC Busy Channel");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCBSY, name + " " + "ADC Busy");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCCTL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCCTL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PRESCALE, name + " " + "ADC Clock Prescaler");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.RESOLUTION, name + " " + "SOC Conversion Resolution");
    all_zero &= check_zero(obj.SIGNALMODE, name + " " + "SOC Signaling Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCCTL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCBURSTCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BURSTTRIGSEL, name + " " + "SOC Burst Trigger Source Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.BURSTSIZE, name + " " + "SOC Burst Size Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.BURSTEN, name + " " + "SOC Burst Mode Enable");

    return all_zero;
}

bool check_zero(const ADCBURSTCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTFLG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCINT1, name + " " + "ADC Interrupt 1 Flag");
    all_zero &= check_zero(obj.ADCINT2, name + " " + "ADC Interrupt 2 Flag");
    all_zero &= check_zero(obj.ADCINT3, name + " " + "ADC Interrupt 3 Flag");
    all_zero &= check_zero(obj.ADCINT4, name + " " + "ADC Interrupt 4 Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTFLG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTFLGCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCINT1, name + " " + "ADC Interrupt 1 Flag Clear");
    all_zero &= check_zero(obj.ADCINT2, name + " " + "ADC Interrupt 2 Flag Clear");
    all_zero &= check_zero(obj.ADCINT3, name + " " + "ADC Interrupt 3 Flag Clear");
    all_zero &= check_zero(obj.ADCINT4, name + " " + "ADC Interrupt 4 Flag Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTFLGCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTOVF_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCINT1, name + " " + "ADC Interrupt 1 Overflow Flags");
    all_zero &= check_zero(obj.ADCINT2, name + " " + "ADC Interrupt 2 Overflow Flags");
    all_zero &= check_zero(obj.ADCINT3, name + " " + "ADC Interrupt 3 Overflow Flags");
    all_zero &= check_zero(obj.ADCINT4, name + " " + "ADC Interrupt 4 Overflow Flags");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTOVF_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTOVFCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCINT1, name + " " + "ADC Interrupt 1 Overflow Clear Bits");
    all_zero &= check_zero(obj.ADCINT2, name + " " + "ADC Interrupt 2 Overflow Clear Bits");
    all_zero &= check_zero(obj.ADCINT3, name + " " + "ADC Interrupt 3 Overflow Clear Bits");
    all_zero &= check_zero(obj.ADCINT4, name + " " + "ADC Interrupt 4 Overflow Clear Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTOVFCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTSEL1N2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT1SEL, name + " " + "ADCINT1 EOC Source Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT1E, name + " " + "ADCINT1 Interrupt Enable");
    all_zero &= check_zero(obj.INT1CONT, name + " " + "ADCINT1 Continue to Interrupt Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT2SEL, name + " " + "ADCINT2 EOC Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT2E, name + " " + "ADCINT2 Interrupt Enable");
    all_zero &= check_zero(obj.INT2CONT, name + " " + "ADCINT2 Continue to Interrupt Mode");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTSEL1N2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTSEL3N4_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.INT3SEL, name + " " + "ADCINT3 EOC Source Select");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT3E, name + " " + "ADCINT3 Interrupt Enable");
    all_zero &= check_zero(obj.INT3CONT, name + " " + "ADCINT3 Continue to Interrupt Mode");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT4SEL, name + " " + "ADCINT4 EOC Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.INT4E, name + " " + "ADCINT4 Interrupt Enable");
    all_zero &= check_zero(obj.INT4CONT, name + " " + "ADCINT4 Continue to Interrupt Mode");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCINTSEL3N4_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCPRICTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOCPRIORITY, name + " " + "SOC Priority");
    all_zero &= check_zero(obj.RRPOINTER, name + " " + "Round Robin Pointer");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOCPRICTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTSOCSEL1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC0, name + " " + "SOC0 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC1, name + " " + "SOC1 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC2, name + " " + "SOC2 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC3, name + " " + "SOC3 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC4, name + " " + "SOC4 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC5, name + " " + "SOC5 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC6, name + " " + "SOC6 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC7, name + " " + "SOC7 ADC Interrupt Trigger Select");

    return all_zero;
}

bool check_zero(const ADCINTSOCSEL1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCINTSOCSEL2_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC8, name + " " + "SOC8 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC9, name + " " + "SOC9 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC10, name + " " + "SOC10 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC11, name + " " + "SOC11 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC12, name + " " + "SOC12 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC13, name + " " + "SOC13 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC14, name + " " + "SOC14 ADC Interrupt Trigger Select");
    all_zero &= check_zero(obj.SOC15, name + " " + "SOC15 ADC Interrupt Trigger Select");

    return all_zero;
}

bool check_zero(const ADCINTSOCSEL2_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCFLG1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC0, name + " " + "SOC0 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC1, name + " " + "SOC1 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC2, name + " " + "SOC2 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC3, name + " " + "SOC3 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC4, name + " " + "SOC4 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC5, name + " " + "SOC5 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC6, name + " " + "SOC6 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC7, name + " " + "SOC7 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC8, name + " " + "SOC8 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC9, name + " " + "SOC9 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC10, name + " " + "SOC10 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC11, name + " " + "SOC11 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC12, name + " " + "SOC12 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC13, name + " " + "SOC13 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC14, name + " " + "SOC14 Start of Conversion Flag");
    all_zero &= check_zero(obj.SOC15, name + " " + "SOC15 Start of Conversion Flag");

    return all_zero;
}

bool check_zero(const ADCSOCFLG1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCFRC1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC0, name + " " + "SOC0 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC1, name + " " + "SOC1 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC2, name + " " + "SOC2 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC3, name + " " + "SOC3 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC4, name + " " + "SOC4 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC5, name + " " + "SOC5 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC6, name + " " + "SOC6 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC7, name + " " + "SOC7 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC8, name + " " + "SOC8 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC9, name + " " + "SOC9 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC10, name + " " + "SOC10 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC11, name + " " + "SOC11 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC12, name + " " + "SOC12 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC13, name + " " + "SOC13 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC14, name + " " + "SOC14 Force Start of Conversion Bit");
    all_zero &= check_zero(obj.SOC15, name + " " + "SOC15 Force Start of Conversion Bit");

    return all_zero;
}

bool check_zero(const ADCSOCFRC1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCOVF1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC0, name + " " + "SOC0 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC1, name + " " + "SOC1 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC2, name + " " + "SOC2 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC3, name + " " + "SOC3 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC4, name + " " + "SOC4 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC5, name + " " + "SOC5 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC6, name + " " + "SOC6 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC7, name + " " + "SOC7 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC8, name + " " + "SOC8 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC9, name + " " + "SOC9 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC10, name + " " + "SOC10 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC11, name + " " + "SOC11 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC12, name + " " + "SOC12 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC13, name + " " + "SOC13 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC14, name + " " + "SOC14 Start of Conversion Overflow Flag");
    all_zero &= check_zero(obj.SOC15, name + " " + "SOC15 Start of Conversion Overflow Flag");

    return all_zero;
}

bool check_zero(const ADCSOCOVF1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOCOVFCLR1_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.SOC0, name + " " + "SOC0 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC1, name + " " + "SOC1 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC2, name + " " + "SOC2 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC3, name + " " + "SOC3 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC4, name + " " + "SOC4 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC5, name + " " + "SOC5 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC6, name + " " + "SOC6 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC7, name + " " + "SOC7 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC8, name + " " + "SOC8 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC9, name + " " + "SOC9 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC10, name + " " + "SOC10 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC11, name + " " + "SOC11 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC12, name + " " + "SOC12 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC13, name + " " + "SOC13 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC14, name + " " + "SOC14 Clear Start of Conversion Overflow Bit");
    all_zero &= check_zero(obj.SOC15, name + " " + "SOC15 Clear Start of Conversion Overflow Bit");

    return all_zero;
}

bool check_zero(const ADCSOCOVFCLR1_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC0CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC0 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC0 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC0 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC0CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC1CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC1 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC1 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC1 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC1CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC2CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC2 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC2 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC2 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC2CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC3CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC3 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC3 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC3 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC3CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC4CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC4 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC4 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC4 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC4CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC5CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC5 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC5 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC5 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC5CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC6CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC6 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC6 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC6 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC6CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC7CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC7 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC7 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC7 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC7CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC8CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC8 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC8 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC8 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC8CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC9CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC9 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC9 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC9 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC9CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC10CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC10 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC10 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC10 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC10CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC11CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC11 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC11 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC11 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC11CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC12CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC12 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC12 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC12 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC12CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC13CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC13 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC13 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC13 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC13CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC14CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC14 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC14 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC14 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC14CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCSOC15CTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ACQPS, name + " " + "SOC15 Acquisition Prescale");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.CHSEL, name + " " + "SOC15 Channel Select");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIGSEL, name + " " + "SOC15 Trigger Source Select");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCSOC15CTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCEVTSTAT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPB1TRIPHI, name + " " + "Post Processing Block 1 Trip High Flag");
    all_zero &= check_zero(obj.PPB1TRIPLO, name + " " + "Post Processing Block 1 Trip Low Flag");
    all_zero &= check_zero(obj.PPB1ZERO, name + " " + "Post Processing Block 1 Zero Crossing Flag");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB2TRIPHI, name + " " + "Post Processing Block 2 Trip High Flag");
    all_zero &= check_zero(obj.PPB2TRIPLO, name + " " + "Post Processing Block 2 Trip Low Flag");
    all_zero &= check_zero(obj.PPB2ZERO, name + " " + "Post Processing Block 2 Zero Crossing Flag");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB3TRIPHI, name + " " + "Post Processing Block 3 Trip High Flag");
    all_zero &= check_zero(obj.PPB3TRIPLO, name + " " + "Post Processing Block 3 Trip Low Flag");
    all_zero &= check_zero(obj.PPB3ZERO, name + " " + "Post Processing Block 3 Zero Crossing Flag");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB4TRIPHI, name + " " + "Post Processing Block 4 Trip High Flag");
    all_zero &= check_zero(obj.PPB4TRIPLO, name + " " + "Post Processing Block 4 Trip Low Flag");
    all_zero &= check_zero(obj.PPB4ZERO, name + " " + "Post Processing Block 4 Zero Crossing Flag");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCEVTSTAT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCEVTCLR_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPB1TRIPHI, name + " " + "Post Processing Block 1 Trip High Clear");
    all_zero &= check_zero(obj.PPB1TRIPLO, name + " " + "Post Processing Block 1 Trip Low Clear");
    all_zero &= check_zero(obj.PPB1ZERO, name + " " + "Post Processing Block 1 Zero Crossing Clear");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB2TRIPHI, name + " " + "Post Processing Block 2 Trip High Clear");
    all_zero &= check_zero(obj.PPB2TRIPLO, name + " " + "Post Processing Block 2 Trip Low Clear");
    all_zero &= check_zero(obj.PPB2ZERO, name + " " + "Post Processing Block 2 Zero Crossing Clear");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB3TRIPHI, name + " " + "Post Processing Block 3 Trip High Clear");
    all_zero &= check_zero(obj.PPB3TRIPLO, name + " " + "Post Processing Block 3 Trip Low Clear");
    all_zero &= check_zero(obj.PPB3ZERO, name + " " + "Post Processing Block 3 Zero Crossing Clear");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB4TRIPHI, name + " " + "Post Processing Block 4 Trip High Clear");
    all_zero &= check_zero(obj.PPB4TRIPLO, name + " " + "Post Processing Block 4 Trip Low Clear");
    all_zero &= check_zero(obj.PPB4ZERO, name + " " + "Post Processing Block 4 Zero Crossing Clear");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCEVTCLR_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCEVTSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPB1TRIPHI, name + " " + "Post Processing Block 1 Trip High Event Enable");
    all_zero &= check_zero(obj.PPB1TRIPLO, name + " " + "Post Processing Block 1 Trip Low Event Enable");
    all_zero &= check_zero(obj.PPB1ZERO, name + " " + "Post Processing Block 1 Zero Crossing Event Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB2TRIPHI, name + " " + "Post Processing Block 2 Trip High Event Enable");
    all_zero &= check_zero(obj.PPB2TRIPLO, name + " " + "Post Processing Block 2 Trip Low Event Enable");
    all_zero &= check_zero(obj.PPB2ZERO, name + " " + "Post Processing Block 2 Zero Crossing Event Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB3TRIPHI, name + " " + "Post Processing Block 3 Trip High Event Enable");
    all_zero &= check_zero(obj.PPB3TRIPLO, name + " " + "Post Processing Block 3 Trip Low Event Enable");
    all_zero &= check_zero(obj.PPB3ZERO, name + " " + "Post Processing Block 3 Zero Crossing Event Enable");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB4TRIPHI, name + " " + "Post Processing Block 4 Trip High Event Enable");
    all_zero &= check_zero(obj.PPB4TRIPLO, name + " " + "Post Processing Block 4 Trip Low Event Enable");
    all_zero &= check_zero(obj.PPB4ZERO, name + " " + "Post Processing Block 4 Zero Crossing Event Enable");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCEVTSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCEVTINTSEL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPB1TRIPHI, name + " " + "Post Processing Block 1 Trip High Interrupt Enable");
    all_zero &= check_zero(obj.PPB1TRIPLO, name + " " + "Post Processing Block 1 Trip Low Interrupt Enable");
    all_zero &= check_zero(obj.PPB1ZERO, name + " " + "Post Processing Block 1 Zero Crossing Interrupt Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB2TRIPHI, name + " " + "Post Processing Block 2 Trip High Interrupt Enable");
    all_zero &= check_zero(obj.PPB2TRIPLO, name + " " + "Post Processing Block 2 Trip Low Interrupt Enable");
    all_zero &= check_zero(obj.PPB2ZERO, name + " " + "Post Processing Block 2 Zero Crossing Interrupt Enable");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB3TRIPHI, name + " " + "Post Processing Block 3 Trip High Interrupt Enable");
    all_zero &= check_zero(obj.PPB3TRIPLO, name + " " + "Post Processing Block 3 Trip Low Interrupt Enable");
    all_zero &= check_zero(obj.PPB3ZERO, name + " " + "Post Processing Block 3 Zero Crossing Interrupt Enable");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.PPB4TRIPHI, name + " " + "Post Processing Block 4 Trip High Interrupt Enable");
    all_zero &= check_zero(obj.PPB4TRIPLO, name + " " + "Post Processing Block 4 Trip Low Interrupt Enable");
    all_zero &= check_zero(obj.PPB4ZERO, name + " " + "Post Processing Block 4 Zero Crossing Interrupt Enable");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCEVTINTSEL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCOSDETECT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DETECTCFG, name + " " + "ADC Opens and Shorts Detect Configuration");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCOSDETECT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCCOUNTER_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.FREECOUNT, name + " " + "ADC Free Running Counter Value");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCCOUNTER_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCREV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TYPE, name + " " + "ADC Type");
    all_zero &= check_zero(obj.REV, name + " " + "ADC Revision");

    return all_zero;
}

bool check_zero(const ADCREV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCOFFTRIM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFTRIM, name + " " + "ADC Offset Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCOFFTRIM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB1CONFIG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CONFIG, name + " " + "ADC Post Processing Block 1 Configuration");
    all_zero &= check_zero(obj.TWOSCOMPEN, name + " " + "ADC Post Processing Block 1 Two's Complement Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB1CONFIG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB1STAMP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DLYSTAMP, name + " " + "ADC Post Processing Block 1 Delay Time Stamp");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB1STAMP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB1OFFCAL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFCAL, name + " " + "ADC Post Processing Block Offset Correction");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB1OFFCAL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB1TRIPHI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITHI, name + " " + "ADC Post Processing Block 1 Trip High Limit");
    all_zero &= check_zero(obj.HSIGN, name + " " + "High Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB1TRIPHI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB1TRIPLO_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITLO, name + " " + "ADC Post Processing Block 1 Trip Low Limit");
    all_zero &= check_zero(obj.LSIGN, name + " " + "Low Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.REQSTAMP, name + " " + "ADC Post Processing Block 1 Request Time Stamp");

    return all_zero;
}

bool check_zero(const ADCPPB1TRIPLO_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2CONFIG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CONFIG, name + " " + "ADC Post Processing Block 2 Configuration");
    all_zero &= check_zero(obj.TWOSCOMPEN, name + " " + "ADC Post Processing Block 2 Two's Complement Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB2CONFIG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2STAMP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DLYSTAMP, name + " " + "ADC Post Processing Block 2 Delay Time Stamp");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB2STAMP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2OFFCAL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFCAL, name + " " + "ADC Post Processing Block Offset Correction");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB2OFFCAL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2TRIPHI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITHI, name + " " + "ADC Post Processing Block 2 Trip High Limit");
    all_zero &= check_zero(obj.HSIGN, name + " " + "High Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB2TRIPHI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2TRIPLO_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITLO, name + " " + "ADC Post Processing Block 2 Trip Low Limit");
    all_zero &= check_zero(obj.LSIGN, name + " " + "Low Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.REQSTAMP, name + " " + "ADC Post Processing Block 2 Request Time Stamp");

    return all_zero;
}

bool check_zero(const ADCPPB2TRIPLO_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3CONFIG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CONFIG, name + " " + "ADC Post Processing Block 3 Configuration");
    all_zero &= check_zero(obj.TWOSCOMPEN, name + " " + "ADC Post Processing Block 3 Two's Complement Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB3CONFIG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3STAMP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DLYSTAMP, name + " " + "ADC Post Processing Block 3 Delay Time Stamp");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB3STAMP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3OFFCAL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFCAL, name + " " + "ADC Post Processing Block Offset Correction");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB3OFFCAL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3TRIPHI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITHI, name + " " + "ADC Post Processing Block 3 Trip High Limit");
    all_zero &= check_zero(obj.HSIGN, name + " " + "High Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB3TRIPHI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3TRIPLO_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITLO, name + " " + "ADC Post Processing Block 3 Trip Low Limit");
    all_zero &= check_zero(obj.LSIGN, name + " " + "Low Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.REQSTAMP, name + " " + "ADC Post Processing Block 3 Request Time Stamp");

    return all_zero;
}

bool check_zero(const ADCPPB3TRIPLO_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4CONFIG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.CONFIG, name + " " + "ADC Post Processing Block 4 Configuration");
    all_zero &= check_zero(obj.TWOSCOMPEN, name + " " + "ADC Post Processing Block 4 Two's Complement Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB4CONFIG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4STAMP_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.DLYSTAMP, name + " " + "ADC Post Processing Block 4 Delay Time Stamp");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB4STAMP_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4OFFCAL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.OFFCAL, name + " " + "ADC Post Processing Block Offset Correction");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB4OFFCAL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4TRIPHI_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITHI, name + " " + "ADC Post Processing Block 4 Trip High Limit");
    all_zero &= check_zero(obj.HSIGN, name + " " + "High Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ADCPPB4TRIPHI_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4TRIPLO_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LIMITLO, name + " " + "ADC Post Processing Block 4 Trip Low Limit");
    all_zero &= check_zero(obj.LSIGN, name + " " + "Low Limit Sign Bit");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.REQSTAMP, name + " " + "ADC Post Processing Block 4 Request Time Stamp");

    return all_zero;
}

bool check_zero(const ADCPPB4TRIPLO_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADC_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCCTL1, name + " " + "ADC Control 1 Register");
    all_zero &= check_zero(obj.ADCCTL2, name + " " + "ADC Control 2 Register");
    all_zero &= check_zero(obj.ADCBURSTCTL, name + " " + "ADC Burst Control Register");
    all_zero &= check_zero(obj.ADCINTFLG, name + " " + "ADC Interrupt Flag Register");
    all_zero &= check_zero(obj.ADCINTFLGCLR, name + " " + "ADC Interrupt Flag Clear Register");
    all_zero &= check_zero(obj.ADCINTOVF, name + " " + "ADC Interrupt Overflow Register");
    all_zero &= check_zero(obj.ADCINTOVFCLR, name + " " + "ADC Interrupt Overflow Clear Register");
    all_zero &= check_zero(obj.ADCINTSEL1N2, name + " " + "ADC Interrupt 1 and 2 Selection Register");
    all_zero &= check_zero(obj.ADCINTSEL3N4, name + " " + "ADC Interrupt 3 and 4 Selection Register");
    all_zero &= check_zero(obj.ADCSOCPRICTL, name + " " + "ADC SOC Priority Control Register");
    all_zero &= check_zero(obj.ADCINTSOCSEL1, name + " " + "ADC Interrupt SOC Selection 1 Register");
    all_zero &= check_zero(obj.ADCINTSOCSEL2, name + " " + "ADC Interrupt SOC Selection 2 Register");
    all_zero &= check_zero(obj.ADCSOCFLG1, name + " " + "ADC SOC Flag 1 Register");
    all_zero &= check_zero(obj.ADCSOCFRC1, name + " " + "ADC SOC Force 1 Register");
    all_zero &= check_zero(obj.ADCSOCOVF1, name + " " + "ADC SOC Overflow 1 Register");
    all_zero &= check_zero(obj.ADCSOCOVFCLR1, name + " " + "ADC SOC Overflow Clear 1 Register");
    all_zero &= check_zero(obj.ADCSOC0CTL, name + " " + "ADC SOC0 Control Register");
    all_zero &= check_zero(obj.ADCSOC1CTL, name + " " + "ADC SOC1 Control Register");
    all_zero &= check_zero(obj.ADCSOC2CTL, name + " " + "ADC SOC2 Control Register");
    all_zero &= check_zero(obj.ADCSOC3CTL, name + " " + "ADC SOC3 Control Register");
    all_zero &= check_zero(obj.ADCSOC4CTL, name + " " + "ADC SOC4 Control Register");
    all_zero &= check_zero(obj.ADCSOC5CTL, name + " " + "ADC SOC5 Control Register");
    all_zero &= check_zero(obj.ADCSOC6CTL, name + " " + "ADC SOC6 Control Register");
    all_zero &= check_zero(obj.ADCSOC7CTL, name + " " + "ADC SOC7 Control Register");
    all_zero &= check_zero(obj.ADCSOC8CTL, name + " " + "ADC SOC8 Control Register");
    all_zero &= check_zero(obj.ADCSOC9CTL, name + " " + "ADC SOC9 Control Register");
    all_zero &= check_zero(obj.ADCSOC10CTL, name + " " + "ADC SOC10 Control Register");
    all_zero &= check_zero(obj.ADCSOC11CTL, name + " " + "ADC SOC11 Control Register");
    all_zero &= check_zero(obj.ADCSOC12CTL, name + " " + "ADC SOC12 Control Register");
    all_zero &= check_zero(obj.ADCSOC13CTL, name + " " + "ADC SOC13 Control Register");
    all_zero &= check_zero(obj.ADCSOC14CTL, name + " " + "ADC SOC14 Control Register");
    all_zero &= check_zero(obj.ADCSOC15CTL, name + " " + "ADC SOC15 Control Register");
    all_zero &= check_zero(obj.ADCEVTSTAT, name + " " + "ADC Event Status Register");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCEVTCLR, name + " " + "ADC Event Clear Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCEVTSEL, name + " " + "ADC Event Selection Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCEVTINTSEL, name + " " + "ADC Event Interrupt Selection Register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCOSDETECT, name + " " + "ADC Open and Shorts Detect Register");
    all_zero &= check_zero(obj.ADCCOUNTER, name + " " + "ADC Counter Register");
    all_zero &= check_zero(obj.ADCREV, name + " " + "ADC Revision Register");
    all_zero &= check_zero(obj.ADCOFFTRIM, name + " " + "ADC Offset Trim Register");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCPPB1CONFIG, name + " " + "ADC PPB1 Config Register");
    all_zero &= check_zero(obj.ADCPPB1STAMP, name + " " + "ADC PPB1 Sample Delay Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB1OFFCAL, name + " " + "ADC PPB1 Offset Calibration Register");
    all_zero &= check_zero(obj.ADCPPB1OFFREF, name + " " + "ADC PPB1 Offset Reference Register");
    all_zero &= check_zero(obj.ADCPPB1TRIPHI, name + " " + "ADC PPB1 Trip High Register");
    all_zero &= check_zero(obj.ADCPPB1TRIPLO, name + " " + "ADC PPB1 Trip Low/Trigger Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB2CONFIG, name + " " + "ADC PPB2 Config Register");
    all_zero &= check_zero(obj.ADCPPB2STAMP, name + " " + "ADC PPB2 Sample Delay Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB2OFFCAL, name + " " + "ADC PPB2 Offset Calibration Register");
    all_zero &= check_zero(obj.ADCPPB2OFFREF, name + " " + "ADC PPB2 Offset Reference Register");
    all_zero &= check_zero(obj.ADCPPB2TRIPHI, name + " " + "ADC PPB2 Trip High Register");
    all_zero &= check_zero(obj.ADCPPB2TRIPLO, name + " " + "ADC PPB2 Trip Low/Trigger Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB3CONFIG, name + " " + "ADC PPB3 Config Register");
    all_zero &= check_zero(obj.ADCPPB3STAMP, name + " " + "ADC PPB3 Sample Delay Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB3OFFCAL, name + " " + "ADC PPB3 Offset Calibration Register");
    all_zero &= check_zero(obj.ADCPPB3OFFREF, name + " " + "ADC PPB3 Offset Reference Register");
    all_zero &= check_zero(obj.ADCPPB3TRIPHI, name + " " + "ADC PPB3 Trip High Register");
    all_zero &= check_zero(obj.ADCPPB3TRIPLO, name + " " + "ADC PPB3 Trip Low/Trigger Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB4CONFIG, name + " " + "ADC PPB4 Config Register");
    all_zero &= check_zero(obj.ADCPPB4STAMP, name + " " + "ADC PPB4 Sample Delay Time Stamp Register");
    all_zero &= check_zero(obj.ADCPPB4OFFCAL, name + " " + "ADC PPB4 Offset Calibration Register");
    all_zero &= check_zero(obj.ADCPPB4OFFREF, name + " " + "ADC PPB4 Offset Reference Register");
    all_zero &= check_zero(obj.ADCPPB4TRIPHI, name + " " + "ADC PPB4 Trip High Register");
    all_zero &= check_zero(obj.ADCPPB4TRIPLO, name + " " + "ADC PPB4 Trip Low/Trigger Time Stamp Register");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.ADCINLTRIM1, name + " " + "ADC Linearity Trim 1 Register");
    all_zero &= check_zero(obj.ADCINLTRIM2, name + " " + "ADC Linearity Trim 2 Register");
    all_zero &= check_zero(obj.ADCINLTRIM3, name + " " + "ADC Linearity Trim 3 Register");
    all_zero &= check_zero(obj.ADCINLTRIM4, name + " " + "ADC Linearity Trim 4 Register");
    all_zero &= check_zero(obj.ADCINLTRIM5, name + " " + "ADC Linearity Trim 5 Register");
    all_zero &= check_zero(obj.ADCINLTRIM6, name + " " + "ADC Linearity Trim 6 Register");

    return all_zero;
}

bool check_zero(const ADCPPB1RESULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPBRESULT, name + " " + "ADC Post Processing Block Result");
    all_zero &= check_zero(obj.SIGN, name + " " + "Sign Extended Bits");

    return all_zero;
}

bool check_zero(const ADCPPB1RESULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB2RESULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPBRESULT, name + " " + "ADC Post Processing Block Result");
    all_zero &= check_zero(obj.SIGN, name + " " + "Sign Extended Bits");

    return all_zero;
}

bool check_zero(const ADCPPB2RESULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB3RESULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPBRESULT, name + " " + "ADC Post Processing Block Result");
    all_zero &= check_zero(obj.SIGN, name + " " + "Sign Extended Bits");

    return all_zero;
}

bool check_zero(const ADCPPB3RESULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADCPPB4RESULT_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.PPBRESULT, name + " " + "ADC Post Processing Block Result");
    all_zero &= check_zero(obj.SIGN, name + " " + "Sign Extended Bits");

    return all_zero;
}

bool check_zero(const ADCPPB4RESULT_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ADC_RESULT_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ADCRESULT0, name + " " + "ADC Result 0 Register");
    all_zero &= check_zero(obj.ADCRESULT1, name + " " + "ADC Result 1 Register");
    all_zero &= check_zero(obj.ADCRESULT2, name + " " + "ADC Result 2 Register");
    all_zero &= check_zero(obj.ADCRESULT3, name + " " + "ADC Result 3 Register");
    all_zero &= check_zero(obj.ADCRESULT4, name + " " + "ADC Result 4 Register");
    all_zero &= check_zero(obj.ADCRESULT5, name + " " + "ADC Result 5 Register");
    all_zero &= check_zero(obj.ADCRESULT6, name + " " + "ADC Result 6 Register");
    all_zero &= check_zero(obj.ADCRESULT7, name + " " + "ADC Result 7 Register");
    all_zero &= check_zero(obj.ADCRESULT8, name + " " + "ADC Result 8 Register");
    all_zero &= check_zero(obj.ADCRESULT9, name + " " + "ADC Result 9 Register");
    all_zero &= check_zero(obj.ADCRESULT10, name + " " + "ADC Result 10 Register");
    all_zero &= check_zero(obj.ADCRESULT11, name + " " + "ADC Result 11 Register");
    all_zero &= check_zero(obj.ADCRESULT12, name + " " + "ADC Result 12 Register");
    all_zero &= check_zero(obj.ADCRESULT13, name + " " + "ADC Result 13 Register");
    all_zero &= check_zero(obj.ADCRESULT14, name + " " + "ADC Result 14 Register");
    all_zero &= check_zero(obj.ADCRESULT15, name + " " + "ADC Result 15 Register");
    all_zero &= check_zero(obj.ADCPPB1RESULT, name + " " + "ADC Post Processing Block 1 Result Register");
    all_zero &= check_zero(obj.ADCPPB2RESULT, name + " " + "ADC Post Processing Block 2 Result Register");
    all_zero &= check_zero(obj.ADCPPB3RESULT, name + " " + "ADC Post Processing Block 3 Result Register");
    all_zero &= check_zero(obj.ADCPPB4RESULT, name + " " + "ADC Post Processing Block 4 Result Register");

    return all_zero;
}

bool check_zero(const INTOSC1TRIM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.VALFINETRIM, name + " " + "Oscillator Value Fine Trim Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INTOSC1TRIM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const INTOSC2TRIM_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.VALFINETRIM, name + " " + "Oscillator Value Fine Trim Bits");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const INTOSC2TRIM_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TSNSCTL_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.ENABLE, name + " " + "Temperature Sensor Enable");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TSNSCTL_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const LOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.TSNSCTL, name + " " + "Temperature Sensor Control Register Lock");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd5, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd6, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd7, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd8, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd9, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd10, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd11, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd12, name + " " + "Reserved");
    all_zero &= check_zero(obj.ANAREFTRIMA, name + " " + "Analog Reference A Trim Register Lock");
    all_zero &= check_zero(obj.ANAREFTRIMB, name + " " + "Analog Reference B Trim Register Lock");
    all_zero &= check_zero(obj.ANAREFTRIMC, name + " " + "Analog Reference C Trim Register Lock");
    all_zero &= check_zero(obj.ANAREFTRIMD, name + " " + "Analog Reference D Trim Register Lock");
    all_zero &= check_zero(obj.rsvd13, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd14, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd15, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd16, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd17, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const LOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ANAREFTRIMA_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BGVALTRIM, name + " " + "Bandgap Value Trim");
    all_zero &= check_zero(obj.BGSLOPETRIM, name + " " + "Bandgap Slope Trim");
    all_zero &= check_zero(obj.IREFTRIM, name + " " + "Reference Current Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ANAREFTRIMA_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ANAREFTRIMB_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BGVALTRIM, name + " " + "Bandgap Value Trim");
    all_zero &= check_zero(obj.BGSLOPETRIM, name + " " + "Bandgap Slope Trim");
    all_zero &= check_zero(obj.IREFTRIM, name + " " + "Reference Current Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ANAREFTRIMB_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ANAREFTRIMC_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BGVALTRIM, name + " " + "Bandgap Value Trim");
    all_zero &= check_zero(obj.BGSLOPETRIM, name + " " + "Bandgap Slope Trim");
    all_zero &= check_zero(obj.IREFTRIM, name + " " + "Reference Current Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ANAREFTRIMC_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ANAREFTRIMD_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.BGVALTRIM, name + " " + "Bandgap Value Trim");
    all_zero &= check_zero(obj.BGSLOPETRIM, name + " " + "Bandgap Slope Trim");
    all_zero &= check_zero(obj.IREFTRIM, name + " " + "Reference Current Trim");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const ANAREFTRIMD_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const ANALOG_SUBSYS_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.INTOSC1TRIM, name + " " + "Internal Oscillator 1 Trim Register");
    all_zero &= check_zero(obj.INTOSC2TRIM, name + " " + "Internal Oscillator 2 Trim Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TSNSCTL, name + " " + "Temperature Sensor Control Register");
    all_zero &= check_zero(obj.rsvd3, name + " " + "Reserved");
    all_zero &= check_zero(obj.LOCK, name + " " + "Lock Register");
    all_zero &= check_zero(obj.rsvd4, name + " " + "Reserved");
    all_zero &= check_zero(obj.ANAREFTRIMA, name + " " + "Analog Reference Trim A Register");
    all_zero &= check_zero(obj.ANAREFTRIMB, name + " " + "Analog Reference Trim B Register");
    all_zero &= check_zero(obj.ANAREFTRIMC, name + " " + "Analog Reference Trim C Register");
    all_zero &= check_zero(obj.ANAREFTRIMD, name + " " + "Analog Reference Trim D Register");

    return all_zero;
}

bool check_zero(const TRIP4MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP4 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP4MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP4MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP4 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP4MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP5MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP5 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP5MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP5MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP5 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP5MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP7MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP7 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP7MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP7MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP7 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP7MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP8MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP8 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP8MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP8MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP8 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP8MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP9MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP9 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP9MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP9MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP9 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP9MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP10MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP10 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP10MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP10MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP10 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP10MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP11MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP11 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP11MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP11MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP11 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP11MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP12MUX0TO15CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "Mux0 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 Configuration for TRIP12 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP12MUX0TO15CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP12MUX16TO31CFG_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 Configuration for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 Configuration for TRIP12 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP12MUX16TO31CFG_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP4MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP4 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP4MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP5MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP5 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP5MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP7MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP7 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP7MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP8MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP8 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP8MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP9MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP9 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP9MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP10MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP10 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP10MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP11MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP11 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP11MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIP12MUXENABLE_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.MUX0, name + " " + "mux0 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX1, name + " " + "Mux1 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX2, name + " " + "Mux2 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX3, name + " " + "Mux3 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX4, name + " " + "Mux4 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX5, name + " " + "Mux5 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX6, name + " " + "Mux6 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX7, name + " " + "Mux7 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX8, name + " " + "Mux8 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX9, name + " " + "Mux9 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX10, name + " " + "Mux10 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX11, name + " " + "Mux11 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX12, name + " " + "Mux12 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX13, name + " " + "Mux13 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX14, name + " " + "Mux14 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX15, name + " " + "Mux15 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX16, name + " " + "Mux16 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX17, name + " " + "Mux17 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX18, name + " " + "Mux18 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX19, name + " " + "Mux19 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX20, name + " " + "Mux20 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX21, name + " " + "Mux21 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX22, name + " " + "Mux22 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX23, name + " " + "Mux23 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX24, name + " " + "Mux24 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX25, name + " " + "Mux25 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX26, name + " " + "Mux26 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX27, name + " " + "Mux27 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX28, name + " " + "Mux28 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX29, name + " " + "Mux29 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX30, name + " " + "Mux30 to drive TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.MUX31, name + " " + "Mux31 to drive TRIP12 of EPWM-XBAR");

    return all_zero;
}

bool check_zero(const TRIP12MUXENABLE_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIPOUTINV_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIP4, name + " " + "Selects polarity for TRIP4 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP5, name + " " + "Selects polarity for TRIP5 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP7, name + " " + "Selects polarity for TRIP7 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP8, name + " " + "Selects polarity for TRIP8 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP9, name + " " + "Selects polarity for TRIP9 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP10, name + " " + "Selects polarity for TRIP10 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP11, name + " " + "Selects polarity for TRIP11 of EPWM-XBAR");
    all_zero &= check_zero(obj.TRIP12, name + " " + "Selects polarity for TRIP12 of EPWM-XBAR");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");

    return all_zero;
}

bool check_zero(const TRIPOUTINV_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const TRIPLOCK_BITS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.LOCK, name + " " + "Locks the configuration for EPWM-XBAR");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.KEY, name + " " + "Write protection KEY");

    return all_zero;
}

bool check_zero(const TRIPLOCK_REG &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.all, name + " " + "all");
    all_zero &= check_zero(obj.bit, name + " " + "bit");

    return all_zero;
}

bool check_zero(const EPWM_XBAR_REGS &obj, const std::string &name) {
    bool all_zero = true;

    all_zero &= check_zero(obj.TRIP4MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP4");
    all_zero &= check_zero(obj.TRIP4MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP4");
    all_zero &= check_zero(obj.TRIP5MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP5");
    all_zero &= check_zero(obj.TRIP5MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP5");
    all_zero &= check_zero(obj.TRIP7MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP7");
    all_zero &= check_zero(obj.TRIP7MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP7");
    all_zero &= check_zero(obj.TRIP8MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP8");
    all_zero &= check_zero(obj.TRIP8MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP8");
    all_zero &= check_zero(obj.TRIP9MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP9");
    all_zero &= check_zero(obj.TRIP9MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP9");
    all_zero &= check_zero(obj.TRIP10MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP10");
    all_zero &= check_zero(obj.TRIP10MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP10");
    all_zero &= check_zero(obj.TRIP11MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP11");
    all_zero &= check_zero(obj.TRIP11MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP11");
    all_zero &= check_zero(obj.TRIP12MUX0TO15CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP12");
    all_zero &= check_zero(obj.TRIP12MUX16TO31CFG, name + " " + "ePWM XBAR Mux Configuration for TRIP12");
    all_zero &= check_zero(obj.TRIP4MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP4");
    all_zero &= check_zero(obj.TRIP5MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP5");
    all_zero &= check_zero(obj.TRIP7MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP7");
    all_zero &= check_zero(obj.TRIP8MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP8");
    all_zero &= check_zero(obj.TRIP9MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP9");
    all_zero &= check_zero(obj.TRIP10MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP10");
    all_zero &= check_zero(obj.TRIP11MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP11");
    all_zero &= check_zero(obj.TRIP12MUXENABLE, name + " " + "ePWM XBAR Mux Enable for TRIP12");
    all_zero &= check_zero(obj.rsvd1, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPOUTINV, name + " " + "ePWM XBAR Output Inversion Register");
    all_zero &= check_zero(obj.rsvd2, name + " " + "Reserved");
    all_zero &= check_zero(obj.TRIPLOCK, name + " " + "ePWM XBAR Configuration Lock register");

    return all_zero;
}
